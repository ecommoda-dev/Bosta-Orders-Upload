<div dir="rtl" style="text-align: right;">

# Bosta-Orders-Upload — وثيقة المواصفات

![version](https://img.shields.io/badge/version-v0.2.0-blue)

> **الحالة:** مرحلة تجميع بيانات — **مفيش كود اتكتب ولا نداء إنشاء فعلي اتنفّذ**.
> الملف ده هو العقد المرجعي اللي هيتبني عليه الكود بعد ما البنود المفتوحة
> في **§10** تتقفل.
>
> **تحديث v0.2.0 (06-09-2026):** الـ `api.yaml` الرسمي اتقرا **كامل** من
> نسخة محليّة، وصفحة الويبهوك الرسمية اتقرت كاملة، ونداءات شوبيفاي الحية
> اتنفّذت واتحققت. **معظم بنود §10 اتقفلت** — والباقي متعلّم صراحةً.
> التغييرات الكاسرة عن v0.1.0 متجمّعة في **§0**.
>
> **مصدر كل قيمة هنا** إمّا تجربة حية موثّقة في `Bosta_create_delivery_api.md`،
> أو مهارة من مهارات EcomModa (المصدر مذكور جنب كل بند). **أي حاجة مش
> متحققة مكتوب صراحةً إنها مش متحققة.**

---

## ٠. 🔴 تغييرات كاسرة عن v0.1.0 — اقراها قبل أي كود

كل بند هنا **بيبطّل قرار كان مكتوب في v0.1.0**. كتابة الكود على النسخة
القديمة = فشل، وأغلبه **فشل صامت**.

| # | كان في v0.1.0 | بقى في v0.2.0 | الدليل |
|---|---|---|---|
| ١ | `specs.packageType: "Small"` | `specs.packageType: "Parcel"` + `specs.size: "SMALL"` | `api.yaml` — حقلين منفصلين، و`"Small"` مش قيمة موثّقة في أي منهم |
| ٢ | `custom.bosta_tracking_number` نوعه `single_line_text_field` | 🔴 **`number_integer`** | تعريف حي على المتجر — اتأكد بنداء مباشر |
| ٣ | نجاح الرفع = **`201`** | **`res.ok`** (التوثيق بيقول `200`، التجربة الحية رجّعت `201`) | `api.yaml` بيوثّق `'200': Created` |
| ٤ | `errorCode` رقم (`=== 3003`) | **نص** — قارن بـ `String(errorCode)` | `api.yaml`: `errorCode: type: string` |
| ٥ | عقدين بس للعنوان | **تلات أشكال موثّقة** — فيهم `districtId` لوحده (أدق وأبسط) | `oneOf` في `AddressCreateDelivery` — §٥.١ |
| ٦ | `flexShippingInfo` شكله مش معروف | `{ isOrderEligible, amountToBeCollected }` | `api.yaml` سطر 1270 |
| ٧ | «مفيش ويبهوك بوسطة شغّال — الساحة فاضية» | 🔴 **فيه ويبهوك حساب مسجّل وشغّال** | سكرين شوت من أحمد 06-09 |
| ٨ | `receiver.fullName` لوحدها كافية | الإلزامي الموثّق **`firstName` + `phone`** | `api.yaml` — `required: [firstName, phone]` |

---

## ١. الغرض

بديل زرار **Send to Bosta** بتاع بلجن بوسطة على شوبيفاي. الأداة بتعرض
الأوردرات المؤهَّلة للشحن عبر بوسطة، والموظف بيرفعها **جماعيًا** بنداء واحد،
والأداة بتسجّل النتيجة على شوبيفاي وفي D1.

**حدود الأداة:** شغلها بيخلص **عند الرفع**. متابعة حالة الشحنة بعد كده
**مش من شغلها** — ولا الطباعة، ولا تقديم مرحلة الأوردر.

---

## ٢. الهوية والثوابت التشغيلية

| البند | القيمة | المصدر |
|---|---|---|
| اسم الأداة | `Bosta-Orders-Upload` | مقرَّر |
| الريبو | `github.com/ecommoda-dev/Bosta-Orders-Upload` | اتعمل 06-09-2026 |
| قيمة `tool` في D1 | `bosta_orders_upload` | مقرَّر — 🔴 **لسه مش مسجّل** في `ecommoda-constants` §7 |
| السر | `BOSTA_API_KEY` | سر مستقل على الـ Worker ده، بنفس اسم المتغيّر |
| `SHOP_DOMAIN` | `6c7e1a-53.myshopify.com` | `ecommoda-constants` §1 — في `[vars]` |
| Shopify API version | `2026-01` | `ecommoda-constants` §1 — صريحة دايمًا |
| D1 binding | `DB` | إلزامي، أي اسم تاني = كتابة فاشلة بصمت |

### ثوابت بوسطة (مؤكَّدة 05-09-2026)

```
Base URL            : https://app.bosta.co/api/v2
Auth                : Authorization: {API_KEY}      ← مفتاح خام، بدون "Bearer"
businessLocationId  : GeZMkbD7o                     ← كلية البنات - مصر الجديدة
countryId (مصر)     : 60e4482c7cb7d4bc4849c4d5
type                : 10                            ← Package Delivery (Send)
```

> ⚠️ الحساب عنده **4** مواقع استلام، المستخدَم واحد بس. أي موقع تاني
> **يتسجّل في `ecommoda-constants` §3 الأول** قبل الاستخدام.

---

## ٣. المدخل — استعلام شوبيفاي

الأداة بتعرض الأوردرات اللي بتطابق **التلات شروط مع بعض**:

```
metafields.custom.zone:'Other_Regions'
AND created_at:>=2026-08-01
AND (metafields.custom.manual_status:'Confirmed'
     OR metafields.custom.manual_status:'Confirmed + Edit')
```

**القيم حرفية** — `Other_Regions` بـ `_` ، و`Confirmed + Edit` بمسافات حوالين
الـ `+`. فرق حرف واحد = صفر صف **من غير أي خطأ**
(`ecommoda-order-lifecycle` Step 2 · `ecommoda-constants` §1).

**تاريخ البداية:** ١ أغسطس ٢٠٢٦ بتوقيت المتجر (القاهرة).

### ٣.١ فخاخ الاستعلام — لازم تتحقق قبل الكود

| # | الفخ | ليه خطير |
|---|---|---|
| ١ | `'Confirmed'` ممكن يجيب صفوف `'Confirmed + Edit'` معاها (أو العكس) | ✅ **اتقفل — مفيش تداخل.** الإثبات في §٣.٤ |
| ٢ | فلترة الميتافيلد بتشتغل **بس** لو `adminFilterable` مفعّلة على التعريف | ✅ **اتقفل** — `zone` · `manual_status` · `courier` كلهم مفعّلين (متحقَّق حيًا 06-09-2026) |
| ٣ | `metafield:` بدل `metafields.` | بيتحوّل بصمت لبحث نصي كامل، مفيش خطأ (`shopify-graphql-helper` Step 3) |

### ٣.٢ الأوردر اللي اترفع قبل كده

**بيفضل ظاهر في القايمة** ومعاه **تنبيه بصري** جنبه (مش بيتشال).

الأداة بتعتبر الأوردر مرفوع لو عنده `custom.bosta_tracking_number` **أو**
التاج `Bosta_Uploaded_S1`. سلوك الرفع عليه → §10 بند ص-٧ (**لسه مفتوح**).

### ٣.٣ الترقيم (Pagination)

`orders(first: 250, after: $cursor)` — أقصى صفحة 250، والترقيم **تسلسلي**
مفيش جلب متوازي (`shopify-graphql-helper` Step 4).

### ٣.٤ ✅ فخ التداخل — اتقفل بإثبات مباشر

**ده كان موصوف كـ"أخطر فخ صامت في الأداة". النتيجة: مفيش تداخل.**

#### ليه الحساب العددي لوحده مكانش كافي

الفحص البديهي هو: `'Confirmed'` لوحده + `'Confirmed + Edit'` لوحده = المدمج؟

```
المدمج               → 19
'Confirmed' لوحده    → 19
'Confirmed + Edit'   → 0        ← والمشكلة هنا
19 + 0 = 19  ✅
```

🔴 **بس الحساب ده مبيثبتش حاجة.** `'Confirmed + Edit'` رجّع **صفر على
مستوى المتجر كله من غير أي فلتر تاريخ أو زون** — يعني **مفيش ولا أوردر
واحد في تاريخ المتجر كله بالقيمة دي**. والمعادلة `س + 0 = س` بتتحقق
تلقائيًا مهما كان سلوك الفلتر. **الفحص ده يعدّي حتى لو الفلتر مكسور تمامًا.**

والقيمة **مش قيمة وهمية** — هي فعلاً في قايمة الاختيارات المعرَّفة:

```
["New Order","Confirmed","Confirmed + Edit","Ready","Shipped","In-Return",
 "Delivered","Returned","Cancelled","Pending Edit","WhatsApp-Confirmed",
 "WhatsApp-CANCELLED"]
```

يعني الموظف **يقدر يستخدمها في أي لحظة**، والفلتر ساعتها بيبقى غير مختبَر.

#### الإثبات الفعلي — على قيم فيها داتا حقيقية

الفحص الصح هو نختبر **سلوك المطابقة نفسه** على قيم شبيهة عندها داتا:

| الاستعلام | العدد | بيثبت إيه |
|---|---|---|
| `manual_status:'New Order'` | **34** | القيمة اللي فيها **مسافة** بتتطابق كاملة ✅ |
| `manual_status:'New'` | **0** | 🟢 **مفيش تطابق جزئي على أول كلمة** |
| `manual_status:'Order'` | **0** | 🟢 **مفيش تطابق جزئي على تاني كلمة** |
| `zone:'Cairo+Giza'` | **7138** | القيمة اللي فيها **`+`** بتتطابق كاملة ✅ |
| `zone:'Cairo'` | **0** | 🟢 **الـ `+` مش بيقسّم القيمة لتوكنز** |

✅ **الاستنتاج المثبَت:** فلتر الميتافيلد بين علامتي تنصيص بيطابق **القيمة
كاملة مطابقة تامة** — لا بيقسّم على المسافات ولا على الـ `+`، ولا بيعمل
تطابق جزئي. **يعني `'Confirmed'` مستحيل يرجّع صفوف `'Confirmed + Edit'`،
والعكس.** الاستعلام في §٣ سليم زي ما هو.

#### 🟡 البند الوحيد اللي لسه مفتوح

`'Confirmed + Edit'` فيه **مسافة و`+` مع بعض** — والاتنين اتأكدوا
**منفصلين** (`New Order` للمسافة، `Cairo+Giza` للـ `+`) مش مع بعض، لأن
مفيش أي داتا بالقيمة دي نختبر عليها.

**التحقق النهائي الرخيص (يتعمل مرة واحدة قبل الإطلاق):**

```
1. حط 'Confirmed + Edit' على أوردر اختبار واحد يدويًا.
2. شغّل:  metafields.custom.manual_status:'Confirmed + Edit'
   → لازم يرجّع 1.  لو رجّع 0 → الفلتر مكسور على القيمة دي تحديدًا.
3. شغّل:  metafields.custom.manual_status:'Confirmed'
   → لازم **ما يرجّعش** الأوردر ده.
4. رجّع القيمة زي ما كانت.
```

🔴 **من غير الخطوة دي، أول أوردر `Confirmed + Edit` في المتجر ممكن يختفي
من القايمة من غير أي خطأ** — والأداة تفضل شغّالة عادي.

---

## ٤. بناء الـ payload — خريطة الحقول

| حقل بوسطة | المصدر من شوبيفاي | ملاحظة |
|---|---|---|
| `type` | ثابت `10` | توصيل عادي |
| `cod` | **`totalOutstandingSet`** | المستحق على العميل. مفيش أوردرات مدفوعة أونلاين حاليًا. ⚠️ بيرجع **سالب** لو فيه استرداد مستحق → `Math.abs()`. حد أقصى موثّق **30,000 جنيه** |
| `goodsInfo.amount` | **`currentSubtotalPriceSet`** | 🔵 شرح كامل في §٤.١ |
| `receiver.firstName` | أول كلمة من `shippingAddress.name` | 🔴 **إلزامي في الـ schema** — مينفعش نبعت `fullName` لوحدها |
| `receiver.lastName` | باقي `shippingAddress.name` | اختياري |
| `receiver.fullName` | `shippingAddress.name` كامل | اختياري — يتبعت **مع** `firstName` مش بدلها |
| `receiver.phone` | `shippingAddress.phone` | 🔴 **لازم تطبيع** — المتجر فيه الشكلين مع بعض، §٤.٣ |
| `receiver.secondPhone` | `order.phone` لو **مختلف فعليًا** بعد التطبيع | ✅ مطلوب — بيقلّل فشل التوصيل. نسبة الحدوث ~3% |
| `dropOffAddress.city` | من `province` عبر جدول §3.5 | 🔴 **`city` مش `cityName`** — §٥.٢ |
| `dropOffAddress.cityId` | من نفس الجدول | يتبعت في العقد الموثّق بس |
| `dropOffAddress.districtName` | من مطابقة المنطقة | العقد الموثّق بس — §٥ |
| `dropOffAddress.firstLine` | `{address1}, {city}- {province}` | **لازم أكتر من ٥ حروف** |
| `specs.packageType` | ثابت `"Parcel"` | 🔴 **اتغيّر** — `"Small"` مش قيمة موثّقة. §٠ بند ١ |
| `specs.size` | ثابت `"SMALL"` | 🔴 **حقل منفصل** عن `packageType` — بالكابيتال |
| `specs.packageDetails.itemsCount` | مجموع `currentQuantity` | يعكس تعديلات الأوردر |
| `specs.packageDetails.description` | `(SKU / variant / size) x qty` | نفس شكل البلجن |
| `businessLocationId` | ثابت `GeZMkbD7o` | |
| `businessReference` | `"#" + رقم الأوردر` | 🔴 **بالهاش** — §٥.٣ |
| `uniqueBusinessReference` | رقم الأوردر **بدون هاش** | حماية بوسطة من التكرار |
| `allowToOpenPackage` | ثابت `true` | على **كل** الأوردرات. أثر مالي **EGP 7/شحنة** — قرار تشغيلي مقصود، متتشالش كـ"تنضيف كود" |
| `flexShippingInfo` | `{ isOrderEligible: true, amountToBeCollected: 100 }` | ✅ الشكل **اتأكد** من الـ schema — §٤.٢ |
| `notes` | `Order.note` من شوبيفاي | ✅ الاسم **`notes`** مؤكَّد. `deliveryNotes` **مش موجود** في الـ spec خالص |

### ٤.١ 🔵 يعني إيه `goodsInfo.amount`؟

**دي قيمة البضاعة نفسها — مش الفلوس اللي العميل هيدفعها.**

الحقلين مختلفين تمامًا وبيتبعتوا مع بعض:

| | `cod` | `goodsInfo.amount` |
|---|---|---|
| معناه | الفلوس اللي المندوب هيحصّلها من العميل | القيمة المعلَنة للبضاعة جوه الطرد |
| بيشمل | البضاعة + الشحن + أي رسوم | البضاعة بس |
| مثال #53642 | **275** | **200** |

**ليه بوسطة طالباها؟** عشان تعرف الطرد قيمته كام لو **ضاع أو اتلف** —
دي أساس التعويض. الداشبورد بيرفض الشحنة من غيرها برسالة صريحة
`"Item value is required"`.

**فمصدرها الصح `currentSubtotalPriceSet`** (إجمالي البضاعة بعد أي تعديل على
الأوردر) — **مش** `totalPriceSet` اللي فيه الشحن، و**مش** `subtotalPriceSet`
اللي مش بيعكس التعديلات. ده مهم بالذات عندنا لأن `Confirmed + Edit` **دي
بالظبط أوردرات متعدّلة** (`shopify-graphql-helper` Step 2).

> ✅ **اتأكد (v0.2.0):** `goodsInfo` فيه **خاصية واحدة بس** هي `amount`،
> و`GET /pricing/insuranceFeeEstimate` **endpoint منفصل تمامًا** بيتنادى
> صراحةً بباراميتر `goodsValue` — **مفيش حساب تلقائي وقت الإنشاء**. يعني
> القيمة **مالهاش أثر مالي** غير كونها أساس التعويض. إحنا مش بننادي الـ
> endpoint ده، فمفيش قلق.

### ٤.٢ ✅ `flexShippingInfo` — الشكل اتأكد

**الغرض (من أحمد، 06-09-2026):** المبلغ اللي العميل بيدفعه كمصاريف شحن **لو
رفض استلام الأوردر** — يعني بيدفع حتى لو مستلمش. **مطلوب على كل الأوردرات
بقيمة 100.**

**الـ schema الرسمي** (`api.yaml` سطر 1270) — الوصف الحرفي:
*"If the order is eligible, the specified amount will be collected from the
customer if they refuse to receive the shipment."*

```json
"flexShippingInfo": { "isOrderEligible": true, "amountToBeCollected": 100 }
```

- الخاصيتين **مش إلزاميتين** في الـ schema (مفيش `required` جواه).
- **مفيش أي حقل تاني في الـ spec كله فيه كلمة flex** غير دول — فيه schema
  اسمه `BusinessFlexShip` بس ده **بتاع اشتراك البيزنس في الخدمة**
  (`isAllowedToSubscribe` / `isSubscribed`)، **مش بتاع الشحنة**. متتلخبطش بيه.
- الحقل **متاح في `/deliveries/bulk`** برضه، لأن الـ bulk بيستخدم نفس
  الـ schema لكل عنصر — لكن شوف §٦.١، إحنا مش هنستخدم bulk أصلاً.

⚠️ **لسه غير متحقق حيًا:** إن بوسطة **بتقبل** القيمة دي على حسابنا فعلاً.
الحقل مشروط بـ *"if the order is eligible"* — والأهلية دي **قرار من بوسطة
مش منّنا**. لو الحساب مش مشترك في خدمة Flex Shipping، **الأرجح إن الحقل
هيتجاهَل بصمت** (مش هيرجّع خطأ). **لازم يتأكد على أول شحنة اختبار**:
ترفع شحنة، وتقرأها تاني بـ `GET /deliveries/{id}`، وتشوف القيمة اترجّعت ولا
اختفت. من غير الفحص ده، ممكن كل الشحنات تترفع من غير الرسم ده من غير ما
حد ياخد باله.

### ٤.٣ 🔴 تطبيع أرقام التليفون — إلزامي

الأرقام على المتجر **مخزّنة بشكلين مختلفين في نفس الحقل** (متحقَّق حيًا على
عينة من الأوردرات المؤهَّلة):

```
#53388  shippingAddress.phone = "01009619555"     ← محلي
#53699  shippingAddress.phone = "+201033337575"   ← دولي
```

مثال بوسطة في التوثيق محلي (`'01065685435'`)، والتجربة الحية قبلت الشكلين.
بس **المقارنة بين `phone` و`secondPhone` لازم تتعمل بعد التطبيع** — من غير
كده `+201009619555` و`01009619555` هيتحسبوا **رقمين مختلفين**، والأداة
هتبعت نفس الرقم مرتين على كل أوردر تقريبًا.

```javascript
const norm = p => String(p||'').replace(/\D/g,'').replace(/^20/,'').replace(/^0/,'');
// secondPhone يتبعت بس لو norm(order.phone) !== norm(shippingAddress.phone)
```

---

## ٥. منطق أشكال العنوان — القلب التقني للأداة

### ٥.١ الأشكال الموثّقة — **تلاتة مش اتنين**

الـ schema بتاع `AddressCreateDelivery` فيه قيد `oneOf` **ماكانش معروف في
v0.1.0**، وهو اللي بيفسّر سلوك العقدين رسميًا:

```yaml
AddressCreateDelivery:
  required: [firstLine, city]      # الاتنين دول إلزاميين دايمًا
  oneOf:
    - required: [districtId]                 # ← الشكل (أ)
    - required: [districtName, cityId]       # ← الشكل (ب)
```

يعني **أي عنوان موثّق لازم يستوفي واحد من الشكلين دول**:

```
(أ) موثّق — بالـ ID المباشر  ← 🟢 الأفضل، مش مستخدَم في v0.1.0
    dropOffAddress: { city, districtId, firstLine }

(ب) موثّق — بالاسم + مدينة   ← ده اللي v0.1.0 سمّاه "العقد ②"
    dropOffAddress: { city, cityId, districtName, firstLine }

(ج) غير موثّق (مجرَّب 201)   ← ده اللي v0.1.0 سمّاه "العقد ①"
    dropOffAddress: { city, firstLine }
    ⚠️ بيخالف الـ oneOf صراحةً — عشان كده الـ endpoint الموثّق بيرفضه
```

**ده بيحسم ليه العقد الموثّق بيرفض العنوان من غير منطقة:** مش سلوك عشوائي،
ده `oneOf` مكتوب في العقد. والشكل (ج) شغّال على المسار غير الموثّق بس،
وده **تساهل غير موثّق** ممكن يتقفل في أي وقت من غير إنذار.

### ٥.١.١ 🟢 الشكل (أ) هو الافتراضي الجديد — ليه

`GET /cities/getAllDistricts` (§٥.٦) **بيرجّع `districtId` لكل منطقة**.
يعني بعد ما نطابق المنطقة، إحنا **عندنا الـ ID أصلاً** — مفيش أي سبب نبعت
الاسم النصي بدل الـ ID.

| | الشكل (أ) `districtId` | الشكل (ب) `districtName` + `cityId` |
|---|---|---|
| الحقول | 1 | 2 |
| خطر إملاء الاسم | ❌ مفيش | 🔴 موجود (اسم عربي/إنجليزي، مسافات، تشكيل) |
| مصدره | نفس الكتالوج اللي بنطابق عليه | نفس الكتالوج بس بنرمي الـ ID ونبعت الاسم |
| مثال بوسطة نفسه | ✅ ده اللي في كل أمثلة `api.yaml` والـ bulk | مذكور كبديل بس |

🔴 **قاعدة:** لما تكون المطابقة نجحت → **ابعت `districtId`**. `districtName`
يفضل بس كخطة بديلة لو الكتالوج رجع منطقة من غير `_id` (حالة نظرية).
ده **بيشيل فخ إملائي كامل** كان مبني عليه التصميم القديم.

### ٥.٢ 🔴 الفخ الأخطر: `city` مش `cityName`

```javascript
{ cityName: 'Giza', cityId: '…' }   // ❌ 400 — الڤاليديتور مش شايفه أصلاً
{ city:     'Giza' }                // ✅ 201
```

الشكل الملتقَط من داشبورد بوسطة بيبعت `cityName` **و** `districtId` مع بعض —
`districtId` هو اللي كان بيعدّي، و`cityName` كان **بيتجاهل بصمت** طول الوقت.
أي نسخ حرفي من الداشبورد بيقع في الفخ ده.

### ٥.٣ 🔴 الفخ التاني: `businessReference` بالهاش

```javascript
businessReference       : '#' + orderNumber   // إلزامي بالهاش
uniqueBusinessReference : String(orderNumber) // بدون هاش
```

من غير الهاش الشحنة بتترفع **بنجاح (201)** بس **كل** أدوات EcomModa
(السكانرات، أدوات الحالة) **مش هتلاقيها** — فشل صامت كامل، مش error.
الحقلين **مع بعض دايمًا**، مش أحدهما بديل.

### ٥.٤ خوارزمية اختيار شكل العنوان لكل أوردر

```
1. حوّل province → cityId عبر جدول ecommoda-constants §3.5   (حتمي، قايمة مقفولة)
2. حاول تطابق المنطقة من نص العنوان الحر على كتالوج بوسطة:
   districtName + districtOtherName (عربي) + أسماء الزون
   مفلتَر إلزاميًا على dropOffAvailability === true
   ⚠️ نص العنوان الحر عربي غالبًا (shippingAddress.city = "البحيرة" مثلاً)
      فالمطابقة لازم تشمل الاسم العربي، مش الإنجليزي بس
3. مطابقة واحدة مؤكدة        → الشكل (أ): { city, districtId, firstLine }
                                ← الـ ID من نفس الكتالوج، §٥.١.١
4. أكتر من مطابقة محتملة     → الشكل (ج) (محافظة بس) + عمود في الجدول
                                الموظف يقدر يعدّله يدويًا لو حب
5. مفيش مطابقة              → الشكل (ج) (محافظة بس) — زي البلجن بالظبط
6. الشكل (أ) رجّع 400 errorCode "3003" (District Not Found)
   → ارجع تلقائي للشكل (ج)
```

🔴 **فخ في خطوة 6:** `errorCode` في التوثيق **نصّ مش رقم**
(`api.yaml`: `errorCode: { type: string }`). المقارنة الصح:

```javascript
if (String(body?.errorCode) === '3003') { /* fallback */ }   // ✅
if (body?.errorCode === 3003)           { /* مش هيتنفّذ */ }  // ❌ فشل صامت
```

لو المقارنة اتكتبت بالرقم، **الرجوع التلقائي مش هيشتغل أبدًا** والشحنة
هتتحسب فشل بدل ما تعدّي على مسار المحافظة.

🟢 **نقطة الأمان اللي بتخلي المطابقة متفائلة:** اسم منطقة غلط بيرجّع **400
صريح**، مش بيعدّي بصمت. يعني **مفيش خطر إن شحنة تتفرز على منطقة غلط بسبب
مطابقة خاطئة** — وده كان أكبر مخاوف التصميم.

**حالة "مفيش مطابقة" مش حالة فشل** — دي بالظبط سلوك البلجن الحالي، والشحنة
بتدخل مسار *Unclear Addresses* بتاع بوسطة والأوبريشن بيحددوا المنطقة بمكالمة.

### ٥.٥ حالتين خاصتين في جدول المحافظات

| province شوبيفاي | التعامل |
|---|---|
| `6th of October` | `cityId` بتاع **Giza** + `districtName` يطابق زون `6 October` |
| `Helwan` | `cityId` بتاع **Cairo** + `districtName` يطابق زون `Helwan` |

شوبيفاي لسه مدرّج محافظات **اتلغت إداريًا سنة 2011**، وبوسطة ماشية على
التقسيم الحالي (28 مدينة). أي كود بيطابق `province` بالاسم حرفيًا **هيفشل**
على الحالتين دول — و٦ أكتوبر منطقة مبيعات كبيرة.

⚠️ بوسطة عندها مدينة مالهاش مقابل في شوبيفاي: **`North Coast`** — لو نص
العنوان فيه "الساحل الشمالي" والـ `province` بتاعه Matrouh أو Alexandria،
الكود لازم ياخد باله إنها ممكن تتطابق كـ**مدينة منفصلة** مش زون.

#### 🔴 نوع تالت من عدم التطابق — **فروق الإملاء**

مش بس محافظات ملغية. فيه محافظات **الاسم بيتكتب مختلف** على المنصتين:

| شوبيفاي (`province`) | بوسطة | الفرق |
|---|---|---|
| `Beheira` | `Behira` | حرف `e` زيادة — ✅ متحقَّق حيًا (أوردر #53388 على شوبيفاي، و`Behira` في كتالوج بوسطة §٥.٦) |

🔴 **النتيجة:** أي كود بيطابق `province` **بالاسم** — حتى بعد معالجة
الحالتين الملغيتين فوق — **هيفشل على البحيرة**، وهي محافظة كبيرة في
`Other_Regions`.

✅ **الحل المعتمد:** المطابقة **متتعملش بالاسم أبدًا**. جدول
`ecommoda-constants` §3.5 بيحوّل `province` → `cityId` **حتميًا بقايمة
مقفولة**، والاسم مبيتقارنش. الجدول ده **لازم يتراجع بند بند** على أسماء
بوسطة الـ 28 قبل الكود — الفرق ده دليل إن فيه فروق تانية محتملة لسه
ماتكشفتش.

### ٥.٦ كتالوج المدن

```
GET /api/v2/cities/getAllDistricts?countryId=60e4482c7cb7d4bc4849c4d5
```

بيشتغل بالمفتاح الخام (مش محتاج جلسة داشبورد). 28 مدينة، أكبرها Behira 647
منطقة · Cairo 592 · Dakahlia 447 · Giza 331.

الكتالوج **ثابت نسبيًا → يتكاش في KV**، مش نداء لكل أوردر. الفلترة على
`dropOffAvailability === true` **إلزامية**.

---

## ٦. الرفع الجماعي

### ٦.١ 🔴 مينفعش نستخدم `/deliveries/bulk`

`POST /deliveries/bulk` بيشتغل على **العقد الموثّق** (`apiVersion=1`)، والعقد
ده **بيرفض أي شحنة من غير منطقة**. وإحنا متوقعين إن نسبة معتبرة من
الأوردرات هتمشي بمسار المحافظة بس (الشكل (ج)) — **دول مينفعش يدخلوا نفس
النداء**.

#### 🔴 سبب تاني مستقل — الرد مفيهوش `trackingNumber`

الـ schema بتاع رد الـ bulk (`api.yaml` سطر ~10390) بيرجّع **IDs بس**:

```json
{ "success": true, "data": ["WiCEy7t2qhaKl8i5DQkDU", "NF0CYnKoc1iQ2GreLeM16"] }
```

بينما رد النداء الفردي بيرجّع الأوبچكت كامل ومعاه **`trackingNumber`**.

وإحنا **محتاجين `trackingNumber` بالذات** عشان نكتبه في
`custom.bosta_tracking_number` (§٧ أكشن ٢). يعني حتى لو استخدمنا bulk،
هنحتاج نداء قراءة إضافي لكل شحنة عشان نجيب رقم التتبع — وده بيلغي أي
مكسب من الـ bulk أصلاً.

**وكمان:** مفيش `maxItems` ولا أي حد أقصى موثّق على المصفوفة، والـ `400`
مالهوش schema مفصّل — **مفيش أي دليل على نجاح جزئي موثّق**. يعني دفعة
فيها شحنة غلط ممكن تسقّط الدفعة كلها من غير ما نعرف مين اللي فشل.

**فالتنفيذ = حلقة نداءات فردية** بحد أقصى للتوازي، والأداة بتختار الشكل لكل
أوردر. تجربة الموظف نفس الشيء (زرار واحد، جدول نتايج) — والميزة الإضافية إن
كل صف بيجيب نتيجته الخاصة **ومعاه رقم تتبعه**، بدل نجاح/فشل جماعي.

### ٦.٢ نتيجة كل صف = **تلات حالات مش اتنين**

```
✅ نجح      — رد ناجح، ومعاه trackingNumber + _id
❌ فشل      — رد بوسطة بالنص، والـ errorCode لو موجود
⏭️ اتخطّى   — الأوردر مستوفيش شرط، أو اتخطّى بقرار
```

#### 🔴 فخ صامت: **متتحققش من النجاح بـ `=== 201`**

| المصدر | كود النجاح |
|---|---|
| `api.yaml` (`POST /deliveries`) | **`200`** — `description: Created` |
| التجربة الحية الموثّقة | **`201`** |

الاتنين موجودين فعلًا. لو الكود اتكتب `if (res.status === 201)` وبوسطة
رجّعت `200` زي ما التوثيق بيقول:

> الشحنة **بتترفع بنجاح على بوسطة**، والأداة بتحسبها **فشل**، والموظف
> بيعيد الرفع → **شحنة مكررة بفلوس حقيقية**.

✅ **القاعدة:** `if (res.ok && body?.success)` — ومنها ناخد
`body.data.trackingNumber`. **أي تحقق برقم كود صريح ممنوع.**

قاعدة إلزامية من `ecommoda-worker-builder` §5A بند ④ — "نجح/فشل" بس بيخفي
الحالة التالتة وبيدّي إحساس كاذب بالنجاح.

### ٦.٣ التوازي وحدود الاستهلاك

🟡 حدود استهلاك API بوسطة **مش معروفة** → §10 بند ب-٩. لحد ما نعرفها،
التوازي يبدأ **متحفّظ** ويتظبط بعد قياس حي.

---

## ٧. الأكشنز بعد الرفع على شوبيفاي

بعد كل **نجاح** (201) وبس، الأداة بتعمل تلاتة على نفس الأوردر:

| # | الأكشن | القيمة | النوع |
|---|---|---|---|
| ١ | `custom.courier` | `Bosta` | `single_line_text_field` ✅ متحقَّق حيًا |
| ٢ | `custom.bosta_tracking_number` | `trackingNumber` من رد بوسطة | 🔴 **`number_integer`** — متحقَّق حيًا |
| ٣ | تاج | `Bosta_Uploaded_S1` | `tagsAdd` — بيمنع التكرار تلقائيًا |

#### 🔴 أخطر بند في القسم ده — نوع `bosta_tracking_number`

التعريف الحي على المتجر (نداء مباشر 06-09-2026):

```
custom.courier                → single_line_text_field
custom.bosta_tracking_number  → number_integer      ← مش نص!
```

v0.1.0 كانت مخططة تكتبه `single_line_text_field`. `metafieldsSet` بيطلب
**تطابق النوع بالحرف مع التعريف الحي**، واختلاف النوع **بيسقّط النداء
كله** — بما فيه كتابة `custom.courier` اللي في نفس النداء.

```javascript
{
  ownerId, namespace: "custom", key: "bosta_tracking_number",
  type: "number_integer",          // ← لازم يطابق التعريف الحي
  value: String(trackingNumber),   // metafieldsSet بياخد value نص دايمًا
}
```

⚠️ **نتيجة تانية للنوع الرقمي:** لو بوسطة رجّعت رقم تتبع فيه **أي حرف أو
رمز**، الكتابة هتفشل. الأرقام المرصودة كلها رقمية (`'5108002'` ·
`48089608`)، بس الكود لازم يتحقق `/^\d+$/` قبل الكتابة ويعرض الصف كـ
«رفع ✅ / كتابة ❌» بدل ما يفشل النداء المجمّع كله.

### ٧.١ اللي **مش** بيتغيّر

🔴 **`custom.manual_status` بتفضل زي ما هي.** الأداة **متلمسش** S1 خالص.

السبب: حسب `ecommoda-order-lifecycle` (`zone-routing.md` §2.2) الانتقال
`Confirmed → Ready` بيحصل عند **الطباعة**، مش عند الرفع. لو الأداة قدّمت
الحالة، هتكسر بوابة الطباعة وتخلي أوردر يعدّي من غير ما يتطبع.

🔴 **`custom.zone` متتكتبش ولا تتصحّح أبدًا.** دي قرار بشري عن توفّر المندوب،
مش استنتاج من العنوان. أي كتابة تلقائية عليها بتمسح قرار مقصود
(`zone-routing.md` §3، قاعدة 2).

### ٧.٢ قواعد الكتابة على شوبيفاي

- `metafieldsSet` = upsert تلقائي، مفيش حاجة اسمها فحص قبلها.
- 🔴 **أبدًا `value: ''`** لمسح ميتافيلد — شوبيفاي بترفض القيمة الفاضية،
  والنداء **مجمّع**، فرفض بند واحد **بيسقّط كل البنود التانية في نفس
  النداء** بما فيهم اللي نجحت. (باگ حقيقي حصل في Order Status Updater
  v3.3.0–v3.5.0 — `shopify-graphql-helper` Step 6.)
- `type` في `metafieldsSet` لازم **يطابق التعريف الحي بالحرف** وإلا النداء
  كله بيفشل → §10 بند ش-٣.
- **متتحققش من الكتابة بإعادة بحث** — فهرس البحث بيتأخر. اقرا الميتافيلد
  مباشرة من الأوردر.
- التاجات **حساسة لحالة الحروف**.

### ٧.٣ ترتيب التنفيذ وحالة الفشل الجزئي

الرفع على بوسطة **بيحصل الأول**، والكتابة على شوبيفاي بعده. لو الرفع نجح
والكتابة فشلت، **الشحنة بقت موجودة فعلاً على بوسطة** — الصف لازم يتعرض
بحالته الحقيقية (رفع ✅ / كتابة ❌) مش يتحسب فشل كامل، عشان محدش يعيد الرفع
ويعمل شحنة مكررة.

---

## ٨. السجل في D1

```
tool : bosta_orders_upload
```

🔴 **لسه مش مسجّل في `ecommoda-constants` §7** — لازم يتسجّل هو وقيم الـ
`type` بتاعته **قبل أول `writeLog`**، مش بعده.

**قيم `type` المقترحة** (تتأكد وتتسجّل قبل الكود):

```
uploaded · upload_failed · skipped · shopify_write_failed · login · logout
```

### ٨.١ تتبع العقد المستخدَم — في `extra` مش عمود جديد

```javascript
extra: { contract_used: 'documented' | 'undocumented',
         tracking_number, bosta_id, district_sent, city_sent }
```

**السبب:** جدول `logs` **مشترك بين ٢٥ أداة**، و`ALTER TABLE` عليه مخاطرة مش
مبررة. الجدول أصلاً فيه عمود `extra` (TEXT/JSON) **مخصص للداتا الخاصة بكل
أداة** وبيتسلسل تلقائيًا. نفس الفايدة، صفر مخاطرة على باقي الأدوات.
✅ **موافقة أحمد 06-09-2026** — ده بيعدّل بند ٩ في الورقة الأصلية.

### ٨.٢ فشل D1 لازم يبان

```javascript
// ❌ await writeLog(env.DB, {…}).catch(() => {});     ← بيبلع الفشل
try { await writeLog(env.DB, {…}); } catch (e) { /* يتعرض */ }
```

---

## ٩. الويبهوك — الوضع والقرار

### ٩.١🔴 الوضع الحقيقي — **الساحة مش فاضية** (تصحيح كاسر)

v0.1.0 كانت بتقول «مفيش ويبهوك بوسطة شغّال — الساحة فاضية». **ده غلط.**

أحمد فتح **Settings → API Integration** بنفسه (الصفحة محميّة بـ OTP)
وبعت سكرين شوت مباشر 06-09-2026:

```
Webhook URL       : https://bosta-webhook.ecommoda24.worke…   ← مسجّل فعلاً
Authorization Key : **********RET                             ← متحطوط فعلاً
```

يعني **فيه ويبهوك على مستوى الحساب شغّال ومسجّل**، بيشاور على Worker على
حساب Cloudflare بتاع EcomModa.

ودي **مش تفصيلة جانبية**، لأن صفحة بوسطة الرسمية بتقول بالحرف:

> *"Once added to your account, the webhook is securely saved in Bosta's
> database, so it does not need to be included in the order creation API
> request."*

**يعني كل شحنة هنرفعها من الأداة دي هتبعت تحديثات حالتها للـ Worker ده
تلقائيًا** — من غير ما نبعت `webhookUrl` ولا نعمل أي حاجة. الأداة **مش
هتكون معزولة** زي ما v0.1.0 افترضت.

#### 🔴 إلزامي قبل أي قرار ويبهوك في الأداة دي

1. **نحدد الـ Worker ده إيه بالظبط** (الاسم مقطوع بعد `ecommoda24.worke…`)
   وهل لسه منشور وشغّال، ولا اتمسح والرابط بقى ميت — نفس عيلة عطل
   `PREREG_WORKER_URL` في `architecture-decisions.md` §4.
2. **لو شغّال:** يتقفل بالظبط بيستقبل إيه وبيعمل بيه إيه — قبل ما نفترض إن
   محدش بيسمع على تحديثات الحالة.
3. 🔴 **مينفعش نسجّل ويبهوك تاني ولا نستبدل الرابط ده** قبل ما نعرف ده إيه.
   الاستبدال بيوقف أي حاجة شغّالة عليه دلوقتي **من غير أي تحذير**.

⚠️ الميتافيلدات `custom.bosta_webhook` · `bosta_order_type` ·
`bosta_number_of_attempts` · `bosta_last_sync` · `bosta_last_sync_time`
موجودة في التعريفات — وكان متفترض إنها بقايا تجربة وقفت. **الافتراض ده
بقى مشكوك فيه**: ممكن الـ Worker المسجّل يكون هو اللي بيكتبها دلوقتي.
يتأكد في نفس التحقيق.

### ٩.٢ التلات أنواع — لازم يفضلوا منفصلين

| # | النوع | الوصف | القرار |
|---|---|---|---|
| ① | ويبهوك بوسطة على مستوى الحساب | URL واحد للحساب كله، بيتظبط في داشبورد بوسطة | 🔴 **مسجّل وشغّال** (§٩.١). **مش في نطاق الأداة دي، ومينفعش نلمسه** |
| ② | ويبهوك لكل شحنة | `webhookUrl` + `webhookCustomHeaders` جوه payload الإنشاء نفسه | 🟡 **نفس الآلية مش نظام تاني** — §٩.٣ |
| ③ | ويبهوك شوبيفاي `ORDERS_UPDATED` | يخلي القايمة تتحدث لحظيًا بدل استعلام عند الفتح | ❌ مش محتاجينه — الموظف بيفتح الأداة ويستعلم. يتأجّل |

⚠️ **تصحيح مهم:** الـ tag اسمه `notifications` في توثيق بوسطة **مش** ويبهوكات
الشحنات — ده مركز إشعارات الداشبورد بس. ويبهوك الشحنة الفردية بيتبعت
**كحقل جوه payload الإنشاء**، مش عن طريق endpoint تسجيل منفصل.

### ٩.٣ الخيار ② — الشكل اتأكد، والتصنيف اتصحّح

#### الحقول (من `api.yaml` + الصفحة الرسمية)

```json
{
  "webhookUrl": "https://YourEndpointUrl",
  "webhookCustomHeaders": { "Authorization": "Basic abc123" }
}
```

- `webhookUrl` — `string`، مفيش قيود `format` موثّقة.
- `webhookCustomHeaders` — **`object` مش array**، ومفتوح (`Authorization`
  مجرد مثال، مش الحقل الوحيد المسموح). مفيش حد أقصى للحجم موثّق.

#### 🟡 تصحيح على تصنيف §٩.٢

الصفحة الرسمية بتوصف ① و② إنهم **نفس المسار الداخلي**، مش نظامين منفصلين:
لو الويبهوك متسجّل على الحساب، **مش لازم يتبعت في نداء الإنشاء**، و`webhookUrl`
في الـ payload هو **البديل** لما ميكونش متسجّل — مش إضافة عليه.

> *"In case you **have not** added your webhook to your account… while
> creating your order… you need to set the following parameter."*

🔴 **ومعنى ده عملي وخطير:** إحنا **عندنا ويبهوك حساب مسجّل بالفعل** (§٩.١).
يعني إحنا **مش محتاجين نبعت `webhookUrl` أصلاً** — والتحديثات **هتروح
للـ Worker القديم سواء بعتنا الحقل أو لأ**. مفيش «ساحة فاضية» نجرّب فيها.

#### سلوك الويبهوك — مؤكَّد من الصفحة الرسمية

- 🔴 **بيتفعّل عند تغيير حالة الشحنة بس — مش عند الإنشاء.** فالأداة دي
  (اللي شغلها بيخلص عند الرفع) **مش هتستقبل أي حاجة** من الويبهوك على
  الشحنة اللي لسه رافعها.
- شكل الـ payload الوارد (`POST` على الـ endpoint بتاعنا):

  ```json
  { "_id": "string", "trackingNumber": "string", "state": 24,
    "type": "SEND | EXCHANGE | CUSTOMER_RETURN_PICKUP | RTO | SIGN_AND_RETURN | FXF_SEND",
    "cod": 0, "timeStamp": 1689252908261, "isConfirmedDelivery": true,
    "deliveryPromiseDate": "13-07-2023", "exceptionReason": "…",
    "exceptionCode": 3, "businessReference": "…", "numberOfAttempts": 0 }
  ```

  ⚠️ `cod` بييجي **في حالة Delivered بس**، و`exceptionReason`/`exceptionCode`
  **في حالة Exception بس**. و**`trackingNumber` موصوف `String` في جدول
  الأنواع بس بييجي `48089608` رقم في المثال الرسمي نفسه** — أي مستقبِل
  لازم يعامله `String(...)`.
- ✅ **جدول الحالات كامل** (`state`) موثّق رسميًا: 10 · 11 · 20 · 21 · 22 ·
  23 · 24 · 25 · 30 · 40 · 41 · 45 · 46 · 47 · 48 · 49 · 60 · 100 · 101 ·
  102 · 103 · 104 · 105 — يتراجع عليه `STATE_MAP` في `bosta-api-helper`.
- ✅ **أكواد الاستثناء (NDR) كاملة**: أمامي 1·2·3·4·5·6·7·8·12·13·14·100·101
  · رجوع 20·21·22·23·24·25·26·100·101.
- 🟡 **لسه ناقص:** سياسة إعادة المحاولة، مهلة الاستجابة، وكود الرد المتوقَّع
  من الـ endpoint بتاعنا. **مش موجودين في أي مصدر رسمي اتفتح** (لا الـ
  `api.yaml` ولا الصفحة). البند ده **يفضل مفتوح** — بس هو **مش حاجز**
  للأداة دي لأنها مش هتستقبل ويبهوك أصلاً.

**القرار الحالي (متأكَّد):** `webhookUrl` و`webhookCustomHeaders`
**متتبعتش من الأداة دي خالص** — لا في النسخة الأولى ولا كتجربة — لحد ما
تحقيق §٩.١ يخلص. السبب اتغيّر عن v0.1.0: مش «عشان الشكل مش معروف»
(بقى معروف)، لكن **عشان فيه مستقبِل شغّال بالفعل ومش عارفين هو إيه**.

---

## ١٠. حالة البيانات — اللي اتقفل واللي لسه

> v0.1.0 كان فيها **٢٢ بند مفتوح**. دلوقتي **١٩ اتقفلوا بدليل**، و**٣ لسه
> مفتوحين** — كلهم متعلّمين تحت. المصادر: `api.yaml` الرسمي (اتقرا كامل من
> نسخة محليّة)، صفحة `get-delivery-status-via-webhook` الرسمية، نداءات
> شوبيفاي حية، وسكرين شوت من أحمد لصفحة محميّة بـ OTP.

### أ) من `api.yaml` — ✅ كلها اتقفلت

| # | المطلوب | النتيجة |
|---|---|---|
| **ب-١** | schema بتاع `flexShippingInfo` | ✅ `{ isOrderEligible: bool, amountToBeCollected: number }` — مفيش حقل `flex` تاني للشحنة. §٤.٢ |
| **ب-٢** | schema بتاع `webhookUrl` / `webhookCustomHeaders` + الـ payload الوارد | ✅ `string` + `object` مفتوح. الـ payload وجدول الحالات وأكواد الـ NDR كاملين في §٩.٣. 🟡 **إعادة المحاولة والمهلة وكود الرد لسه مش موثّقين في أي مصدر** — بند مفتوح (م-٢) بس **مش حاجز** |
| **ب-٣** | عقد `/deliveries/bulk` + الحد الأقصى + الفشل الجزئي | ✅ **مفيش `maxItems`**، والرد **IDs بس من غير `trackingNumber`**، والـ `400` مالهوش schema. **قرار §٦.١ اتأكد ومعاه سبب تاني مستقل** |
| **ب-٤** | schema بتاع `receiver` | ✅ **مفيش تناقض** — `fullName` و`firstName`/`lastName` **كلهم موجودين مع بعض** كحقول اختيارية. الإلزامي: **`firstName` + `phone`** بس. `secondPhone` نص عادي |
| **ب-٥** | اسم حقل الملاحظات | ✅ **`notes`**. `deliveryNotes` **مش موجود في الـ spec خالص**. `returnNotes` موجود لكن لـ Exchange/CRP بس |
| **ب-٦** | قيم `specs` المسموحة | ✅ `size` و`packageType` **حقلين منفصلين**. `"Small"` **مش قيمة موثّقة في أي منهم** → **§٠ بند ١** |
| **ب-٧** | schema بتاع `goodsInfo` + أثر التأمين | ✅ خاصية واحدة بس (`amount`). `insuranceFeeEstimate` **endpoint منفصل بنداء `GET` صريح** — **مفيش حساب تلقائي**، فمفيش أثر مالي غير التعويض |
| **ب-٨** | قايمة `errorCode` الكاملة | 🟡 **مفيش قايمة موحّدة** — الموثّق: `1000` · `1007` · `1008` · `1028` + `70000–70006` (Bosta Box بس). أكوادنا (3002 · 3003 · 3009 · 11000) **مش في التوثيق** ومصدرها التجربة الحية بس. 🔴 **بس اتكشف الأهم: النوع `string` مش `number`** → §٥.٤ |
| **ب-٩** | حدود الاستهلاك | ✅ **صفر ذكر في الملف كله** (بحث عن rate limit · throttle · per minute · 429). **يفضل غير معروف رسميًا**، والتوازي المتحفّظ في §٦.٣ صح |
| **ب-١٠** | نوع `cod` | ✅ **`number`**، والوصف الحرفي بيأكد الحد: *"The limit is 30,000 EGP"*، وبيقدر يبقى سالب لـ CRP |

### ب) من شوبيفاي — ✅ كلها اتقفلت بنداءات حية

| # | المطلوب | النتيجة |
|---|---|---|
| **ش-١** | فحص التداخل | ✅ **مفيش تداخل — مثبَت.** الحساب العددي مكانش كافي؛ الإثبات الحقيقي في **§٣.٤** |
| **ش-٢** | `adminFilterable` | ✅ مفعّلة على `zone` · `manual_status` · `courier` |
| **ش-٣** | الـ `type` الحرفي | 🔴 `courier` = `single_line_text_field` · `bosta_tracking_number` = **`number_integer`** → §٧ |
| **ش-٤** | عدد الأوردرات المطابقة | ✅ **19** وقت الفحص (06-09 بعد الضهر). ⚠️ **الرقم بيتحرك خلال اليوم** — كان 33 الصبح. مش خطأ: الموظفين بينقلوا الأوردرات لـ `Ready` أول بأول. **الأداة لازم تستعلم لحظيًا، ومتعتمدش على عدد محفوظ** |
| **ش-٥** | مكان ملاحظات العميل | ✅ **`Order.note`** (مفرد). `customAttributes` **مفيهاش ولا ملاحظة عميل واحدة** — اللي فيها `n9_bundle_discounts` / `n9_volume_discounts` من أبلكيشن خصومات، **مالهاش علاقة بالشحن** |
| **ش-٦** | حقول التليفون | ✅ `shippingAddress.phone` = **100%** امتلاء (المصدر الأساسي) · `order.phone` 31/33 · `customer.phone` 30/33. **رقمين مختلفين فعليًا في ~3%** بس. 🔴 **الأرقام مخزّنة بشكلين** (`01…` و`+201…`) → التطبيع إلزامي، §٤.٣ |
| **ش-٧** | تصادم التاج | ✅ `Bosta_Uploaded_S1` = **صفر استخدام**. ⚠️ **بس فيه تاجات بوسطة تانية شغّالة فعلاً**: `Bosta` و`bosta_synced` (على آلاف الأوردرات). التاج بتاعنا مميّز، بس أي أداة بتفلتر بـ `tag:Bosta*` هتلقطه — يتراجع قبل الإطلاق |
| **ش-٨** | عينة أوردرات حقيقية | ✅ اتجمعت. **أهم اكتشاف منها:** `shippingAddress.city` **نص حر وعربي غالبًا** (`البحيرة` · `العبور` · `مدينة السادات`) بينما `province` إنجليزي — ده بيأكد إن المطابقة لازم تمشي من `province` عبر الجدول، والنص العربي يتستخدم لمطابقة المنطقة بس |

### ج) من بوسطة — نداءات حية

| # | المطلوب | النتيجة |
|---|---|---|
| **ب-١١** | تأكيد خانة الويبهوك | 🔴 **اتقفل — والنتيجة عكس الافتراض.** فيه ويبهوك مسجّل وشغّال، مش فاضي → **§٩.١** |
| **ب-١٢** | صلاحية `GeZMkbD7o` | ✅ نداء حي على `/pickup-locations` رجّع 4 مواقع، و`GeZMkbD7o` = «كليه البنات - مصر الجديده» **لسه صالح** |

### د) 🔴 البنود اللي **لسه مفتوحة** — دي اللي بتمنع الكود

| # | البند | الحالة والأثر |
|---|---|---|
| **م-١** | 🔴 **الـ Worker المسجّل على ويبهوك حساب بوسطة — إيه هو ولسه شغّال ولا لأ؟** | **حاجز على أي قرار ويبهوك.** مش حاجز على الرفع نفسه. §٩.١ |
| **م-٢** | 🟡 سياسة إعادة المحاولة + المهلة + كود الرد المتوقَّع للويبهوك | **مش موجودين في أي مصدر رسمي.** مش حاجز للأداة دي (مش هتستقبل ويبهوك) — بس لازم يتقفل قبل أي تراكر |
| **م-٣** | 🟡 التحقق النهائي من فلتر `'Confirmed + Edit'` على أوردر اختبار | **رخيص ولازم يتعمل مرة قبل الإطلاق** — الخطوات في §٣.٤ |
| **ص-٧** | الأوردر المرفوع قبل كده لو الموظف رفعه تاني — الأداة تعمل إيه؟ | ⚠️ **لسه مفتوح — قرار تشغيلي من أحمد.** `uniqueBusinessReference` هيرفض التكرار (400 · 11000) بس ده رد فعل مش تصميم |
| **ص-٨** | تأكيد قيم `type` في D1 وتسجيلها في `ecommoda-constants` §7 | ⚠️ **لسه مفتوح** — لازم قبل أول `writeLog` |
| **ص-٩** | مراجعة جدول `province → cityId` بند بند على أسماء بوسطة الـ 28 | ⚠️ **جديد.** فرق `Beheira`/`Behira` دليل إن فيه فروق تانية محتملة — §٥.٥ |
| **ص-١٠** | التحقق الحي إن بوسطة **بتقبل** `flexShippingInfo` على حسابنا | ⚠️ **جديد.** الحقل مشروط بـ eligibility من بوسطة — §٤.٢ |

## ١١. قرارات محسومة — سجل

| # | القرار | التاريخ |
|---|---|---|
| ١ | `allowToOpenPackage: true` على **كل** الأوردرات (EGP 7/شحنة) | 05-09 · أعيد تأكيده 06-09 |
| ٢ | FlexShip = **100** على **كل** الأوردرات | 06-09 |
| ٣ | ~~`packageType: "Small"`~~ → **`packageType: "Parcel"` + `size: "SMALL"`** | 06-09 · 🔴 **اتصحّح 06-09 (v0.2.0)** |
| ٤ | `cod` = `totalOutstandingSet` · مفيش أوردرات مدفوعة أونلاين | 06-09 |
| ٥ | الأداة **متغيّرش** `manual_status` | 06-09 |
| ٦ | `receiver.secondPhone` يتبعت لو موجود | 06-09 |
| ٧ | ملاحظات العميل تتنقل من `Order.note` | 06-09 |
| ٨ | المطابقة الغامضة → مسار المحافظة + تعديل يدوي اختياري | 06-09 |
| ٩ | الأوردر المرفوع يفضل ظاهر **بتنبيه** | 06-09 |
| ١٠ | **مفيش إلغاء ولا تراجع** في النسخة الأولى | 06-09 |
| ١١ | تتبع العقد في `extra` مش عمود D1 جديد | 06-09 |
| ١٢ | الرفع الجماعي = حلقة نداءات فردية (§٦.١) | 06-09 |
| ١٣ | `?action=diag` و`?action=get_config` من أول نسخة | 06-09 |
| ١٤ | مسار هجين للمنطقة — مطابقة لما تنفع، محافظة لما متنفعش | 05-09 |
| ١٥ | **`districtId` هو الشكل الافتراضي** للعنوان لما المطابقة تنجح — مش `districtName` | 06-09 (v0.2.0) · §٥.١.١ |
| ١٦ | التحقق من النجاح بـ **`res.ok`** مش `=== 201` | 06-09 (v0.2.0) · §٦.٢ |
| ١٧ | `errorCode` يتقارن **كنص** (`String(...)`) | 06-09 (v0.2.0) · §٥.٤ |
| ١٨ | `custom.bosta_tracking_number` يتكتب **`number_integer`** | 06-09 (v0.2.0) · §٧ |
| ١٩ | `receiver.firstName` **إلزامي** — `fullName` لوحدها متكفيش | 06-09 (v0.2.0) · §٤ |
| ٢٠ | تطبيع أرقام التليفون قبل مقارنة `phone`/`secondPhone` | 06-09 (v0.2.0) · §٤.٣ |
| ٢١ | **متتبعتش `webhookUrl` من الأداة دي** — لحد ما تحقيق §٩.١ يخلص | 06-09 (v0.2.0) |

---

## ١٢. خارج نطاق النسخة الأولى

- إلغاء أو تعديل شحنة من جوه الأداة (`terminate` · `PUT`).
- متابعة حالة الشحنة بعد الرفع (شغل تراكر منفصل).
- ويبهوك بأي شكل (§٩) — مؤجَّل، مش مرفوض.
- طباعة البوليصة (`bosta-awb-api.md` — أداة تانية).
- تقديم `manual_status` أو أي كتابة على `custom.zone`.
- مواقع استلام غير `GeZMkbD7o`.

---

## ١٣. المصادر

| المصدر | بيغطّي |
|---|---|
| 🆕 `docs.bosta.co/api/api.yaml` (نسخة كاملة) | كل schema رسمي: `flexShippingInfo` · `receiver` · `specs` · `goodsInfo` · `notes` · `cod` · قيد `oneOf` للعنوان · عقد الـ bulk · أكواد الأخطاء |
| 🆕 `docs.bosta.co/docs/how-to/get-delivery-status-via-webhook` | سلوك الويبهوك، شكل الـ payload، **جدول الحالات كامل**، أكواد الـ NDR |
| 🆕 نداءات شوبيفاي حية 06-09-2026 | أنواع الميتافيلدات، `adminFilterable`، إثبات §٣.٤، عينات الأوردرات، حقول التليفون |
| `Bosta_create_delivery_api.md` v1.4.0 | التجارب الحية، مصفوفة §15، العقدين، الفخاخ |
| `bosta-api-helper` Step 8 · 8a | منطق العقدين، فخ المدينة، فخ الهاش، المسارات غير المستخدمة |
| `ecommoda-constants` §1 · §3 · §3.5 · §7 | الثوابت، جدول province↔city، جدول `tool`/`type` |
| `ecommoda-order-lifecycle` + `zone-routing.md` | قيم S1 الحرفية، معنى الزون، بوابة الطباعة |
| `shopify-graphql-helper` Step 3 · 4 · 6 · 8 | الفلاتر، الترقيم، الميتافيلدات وأنواعها، التاجات |
| `ecommoda-worker-builder` Step 5A · 7 · 8 | عقد النداءات، التلات حالات، D1، الأسرار |

---

آخر تحديث: 06-09-2026 — v0.2.0

</div>
