<div dir="rtl" style="text-align: right;">

# رفع أوردرات بوسطة (`Bosta-Orders-Upload`)

![version](https://img.shields.io/badge/version-v1.6.0-blue)

**بتعمل إيه:** بتعرض الأوردرات المؤهَّلة للشحن عبر بوسطة، والموظف بيرفعها جماعيًا بضغطة واحدة، والأداة بتكتب رقم التتبع والكوريَر والتاج على شوبيفاي وبتسجّل النتيجة في D1.
**مين بيستخدمها:** العمليات · المخزن
**الإصدار:** Worker `v1.4.0` · الواجهة `v1.6.0`   ← الاتنين مستقلين، طبيعي يختلفوا

> **العقد المرجعي الكامل في `SPEC.md`** — كل قرار وفخ في الأداة دي مشروح هناك بمصدره. أي تعديل يتراجع عليه الأول.

## الروابط

```
الواجهة    : https://ecommoda-dev.github.io/Bosta-Orders-Upload/
الـ Worker : https://bosta-orders-upload-worker.ecommoda-dev.workers.dev
اسم الـ Worker في الداشبورد: bosta-orders-upload-worker     ← لازم يطابق name في wrangler.toml
```

## الـ Endpoints

| `?action=` | بيعمل إيه |
|---|---|
| `get_orders` | الأوردرات المؤهَّلة + خطة العنوان لكل أوردر + حارس فلتر الحالة |
| `get_districts` | من غير `cityId`: كل مدن بوسطة (لمنتقي المدينة) · مع `cityId`: مناطقها (`dropOffAvailability === true` فقط) |
| `upload` | رفع دفعة (٢٥ أوردر كحد أقصى) + الكتابة على شوبيفاي + السجل |
| `diag` · `get_config` | الفحص الذاتي · نسخة الـ Worker |
| `get_logs` · `get_logs_count` · `get_logs_export` | سجل العمليات |

## D1

```
tool  : bosta_orders_upload
type  : uploaded · upload_failed · shopify_write_failed · skipped · login · logout
```

> 🔴 **بند حاجز على أول `writeLog`:** الصف ده **لسه مش مسجَّل** في جدول
> `ecommoda-constants` §7 (بند 18 هناك مفتوح). قيم الـ `type` متأكدة ومكتوبة
> كملحوظة تحت الجدول، لكن قيمة `tool` نفسها لسه محتاجة تتكتب كصف. القاعدة 7
> في `ecommoda-worker-builder` بتقول التسجيل **قبل** أول كتابة مش بعدها —
> يتسجّل قبل أول تشغيل حي.

⚠️ **`upload_failed` منفصلة عمدًا عن `shopify_write_failed`.** الأولى = الشحنة
ما اترفعتش أصلًا. التانية = الشحنة **موجودة فعلًا** عند بوسطة ومعاها رقم تتبع،
والكتابة الرجعية على شوبيفاي هي اللي فشلت. خلطهم بيخلي أي إعادة محاولة تعمل
**شحنة مكررة بفلوس حقيقية**، مش مجرد صف سجل ناقص.

## المضبوط فعليًا في الداشبورد

```
Bindings : DB → ecommoda-dev-logs
           CATALOG_KV (اختياري — من غيره الكاش بيشتغل على Cache API تلقائيًا)
Secrets  : WORKER_SECRET · CLIENT_ID · CLIENT_SECRET · BOSTA_API_KEY
Vars     : SHOP_DOMAIN                    ← من [vars] في wrangler.toml
Build watch paths : * (الافتراضي)
```

> `BOSTA_API_KEY` **يتشارك** مع باقي أدوات بوسطة — EcomModa عندها حساب بوسطة
> واحد (`ecommoda-constants` §3). مفيش `_1ry`/`_2ry` هنا — دول بتوع Khiam Store.

⚠️ بعد إضافة أي سر: Deployments → Version History → ⋯ → **Promote version**.
من غيرها القيمة بتفضل `undefined` وكل نداء بيرجّع `Unauthorized`.

## CORS

`ALLOWED_ORIGINS` صارمة (`https://ecommoda-dev.github.io`) — الأداة **بتكتب**
على شوبيفاي وبتنشئ شحنات بفلوس حقيقية.

## خط الأساس بعد النقل

```
لسه ما اتشغّلتش حيًا. أول تشغيل: سجّل هنا عدد الأوردرات المؤهَّلة وقت الفتح،
ونسبة الصفوف اللي مطابقة المنطقة نجحت فيها (mode = district)، و**عدد صفوف
🟠 المدينة مشكوك فيها** — التالت ده هو اللي بيقول أنهي حالات تستاهل تدخل
جدول المحافظات بدل تدخّل يدوي كل مرة (اقراه من `city_overridden` في D1).
مرجع وقت كتابة SPEC: 19 أوردر مؤهَّل (06-09-2026 بعد الضهر) — والرقم بيتحرك
خلال اليوم لأن الموظفين بينقلوا الأوردرات لـ Ready أول بأول.
مرجع تاني: 34 صف (07-09-2026) — منهم أوردرين العبور (#53698 · #53699).
```

## الاختبارات

```
node tests/address-matching.test.cjs   # ترجيح مطابقة المنطقة — حالات حقيقية
node tests/row-state.test.cjs          # حالة الصف واستثناء «تحديد الكل»
node tests/zone-label.test.cjs         # عرض الزون و«بلا زون» الصريحة
node tests/zone-cross-city.test.cjs    # اقتراح زون العبور تحت القاهرة
node tests/zone-picker-flow.test.cjs   # رحلة الضغطة الواحدة في النافذة
node tests/bulk-and-pin.test.cjs       # «رفع الكل» على الفلتر + تثبيت المنطقة المطابقة
node tests/cairo-time.test.cjs         # إزاحة القاهرة المحسوبة + حارس التاريخ غير الصالح
node tests/worker-logs.test.cjs        # ترتيب السجل (قايمة مقفولة) · clampInt · حدود يوم القاهرة
```

كلها بتشتغل بـ Node عادي من غير أي تنصيب. `address-matching` بيحمّل
`index.js` في sandbox على كتالوج مصغّر بأسماء بوسطة الحقيقية، و`row-state`
بيحمّل الـ `<script>` بتاع `index.html` على DOM مزيّف.

## فخاخ الأداة دي

- 🔴 **«تم جزئيًا» (أصفر) مش فشل.** معناها الشحنة **موجودة عند بوسطة فعلًا**
  ومعاها رقم تتبع، وحاجة بعدها ما تمّتش. إعادة الرفع بتعمل شحنة تانية.
- 🔴 **`custom.bosta_tracking_number` نوعه `number_integer` مش نص.**
  `metafieldsSet` بيطلب تطابق النوع بالحرف مع التعريف الحي، واختلافه **بيسقّط
  النداء كله** — بما فيه كتابة `custom.courier` اللي في نفس النداء.
  الأداة بتتحقق `/^\d+$/` قبل الكتابة وبتعرض الصف «رفع ✅ / كتابة ⚠» بدل ما
  تسقّط النداء. الفحص الذاتي (`?action=diag`) بيقارن النوع الحي بالمتوقّع.
- 🔴 **`businessReference` بالهاش و`uniqueBusinessReference` بدونه — الاتنين مع
  بعض دايمًا.** من غير الهاش الشحنة بترفع بنجاح بس **كل** أدوات EcomModa
  (السكانرات وأدوات الحالة) مش هتلاقيها. فشل صامت كامل، مش error.
- 🔴 **`dropOffAddress.city` مش `cityName`.** الڤاليديتور مش شايف `cityName`
  أصلاً وبيتجاهله بصمت — أي نسخ حرفي من داشبورد بوسطة بيقع في الفخ ده.
- 🔴 **النجاح بيتفحص بـ `res.ok && body.success` — ممنوع `=== 201`.** التوثيق
  الرسمي بيقول `200` والتجربة الحية رجّعت `201`. التحقق برقم صريح معناه شحنة
  اترفعت فعلًا واتحسبت فشل، والموظف يعيد الرفع.
- 🔴 **`errorCode` نص مش رقم** (`String(body.errorCode) === '3003'`). المقارنة
  بالرقم معناها إن الرجوع التلقائي لمسار المحافظة **عمره ما هيشتغل**.
- 🔴 **جدول المحافظات حتمي ومقفول** (`ecommoda-constants` §3.5). ممنوع أي
  مطابقة نصية على اسم المحافظة كخطة بديلة — فيه خمس محافظات على الأقل اسمها
  مختلف بين المنصتين (Beheira/Behira · Qalyubia/El Kalioubia · …). المحافظة
  اللي مش في الجدول **بتوقف الصف بتاعها برسالة صريحة**، مش بتخمين.
- 🔴 **ترجيح المطابقة مش «الأطول يكسب» — والقاعدة القديمة كانت بتنتج منطقة
  غلط بعلامة خضرا.** `كفر الشيخ مركز قلين` كانت بترفع على **Kafr ElSheikh**
  بدل **Qalin** لأن اسم المحافظة أطول. الترتيب دلوقتي: المنطقة اللي اسمها اسم
  المحافظة **بتتأخّر** ← خانة `city` تغلب `address1` ← التطابق الكامل يغلب
  الجزئي ← الطول بيفصل **بس** عند الاحتواء (مدينة نصر / نصر). ومطابقتين
  **منفصلتين** = **غموض معلَن**، مش اختيار بالطول. (SPEC §٥.٤.١ ·
  `tests/address-matching.test.cjs`)
- 🔴 **المدينة الغلط مش fallback محايد زي المنطقة الناقصة.** «🏙️ المحافظة بس»
  = المدينة صح والمنطقة ناقصة. «🟠 المدينة مشكوك فيها» = **المدينة نفسها غلط**،
  والمدينة بتحدد **الفرع والتسعيرة**. بيحصل لسببين: تصنيف بوسطة مش التقسيم
  الإداري (**العبور** إداريًا القليوبية وعند بوسطة تحت **القاهرة**)، أو العميل
  غلط في اختيار المحافظة (`شبين الكوم` على الغربية · `دمياط الجديدة` على
  القاهرة). الصفوف دي **مستثناة من «تحديد الكل»**. (SPEC §٥.٤.٢)
- 🎯 **الزون هو اللي بيحلّ العبور — والمطابقة بتشوفه من v1.3.0.** اسم المنطقة
  عند بوسطة تسمية إدارية مركّبة (`المنطقة 01 (العبور)`) محدش بيكتبها، لكن
  **الزون اسمه `العبور`** وهو اللي العميل كاتبه. متحقَّق حيًا 07-09-2026:
  تغطية الزون **١٠٠٪** (القاهرة 590/590 · القليوبية 207/207).
  🔴 **الزون بيحسم المدينة مش المنطقة** — زون العبور فيه ٦ مناطق، واختيار
  واحدة منها تلقائيًا تخمين. الاقتراح بيظبّط المدينة ويقصّر القايمة، والموظف
  بيختار. و`localZones` (زون في **نفس** المدينة) **مش حالة شك** — دي اختصار،
  وخلطها بـ`cityDoubt` كان هيولّع 🟠 على صفوف سليمة. (SPEC §٥.٤.١ب)
- 🔴 **مطابقة المنطقة بتسأل «هل العنوان فيه اسم المنطقة؟» — والاتجاه ده هو
  حدّها، وهو السبب الجذري لحالة العبور.** بتنجح لما اسم المنطقة عند بوسطة اسم
  مكان بسيط العميل بيكتبه (`مدينة السادات` · `قلين` · `بلطيم`)، وبتفشل لما يكون
  **تسمية إدارية مركّبة** محدش بيكتبها (`المنطقة 01 (العبور)` ·
  `العبور الجديده - احياء العبور الجديده`). والقيد بيتوارث لكاشف المدينة لأنه
  بيستخدم نفس الدالة.
  ✅ **الحد ده اتعالج في v1.3.0 بمطابقة الزون** (البند اللي فوق) — مش بتغيير
  اتجاه المطابقة. اتجاه مطابقة **المنطقة** لسه زي ما هو، وده مقصود: قلبه كان
  هيخلي أي كلمة في العنوان تطابق عشرات المناطق.
- 🔴 **اسم الزون بيتعرض كامل — القص كان بيخفي اللي بيفرّق.** الشبشة كانت
  `max-width:150px` مع `ellipsis`، والجزء المقصوص هو بالظبط اللي بيميّز زون عن
  زون (`برج العرب الجديده - احياء…`). والزون هو اللي **بيحسم المدينة**، فقراءته
  ناقصة = قرار مدينة على معلومة ناقصة. (v1.4.0)
- 🔴 **المنطقة المطابقة كانت مدفونة في مكانها الأبجدي وسط ٤٠٠ صف.** التمييز
  الوحيد كان لون أزرق باهت، فالموظف كان بيدوّر **بالبحث** على المنطقة اللي
  المطابقة لقتها له أصلًا — وده العكس المطلوب من نافذة بتعرض نتيجة مطابقة.
  دلوقتي بتتثبّت **أول القايمة** بشريط أخضر وبادج «✅ المطابقة الحالية» وسبب
  المطابقة تحتها، وبتفضل مميّزة في مكانها الأبجدي كمان. (v1.5.0)
- 🔴 **الوقت: إزاحة القاهرة بتتحسب، مابتتكتبش ثابت.** كان مكتوب
  `+ 3 * 60 * 60 * 1000`. مصر بتوقف التوقيت الصيفي **29-10-2026**، ومن ساعتها
  كل وقت في الأداة كان هيغلط بساعة **من غير أي رسالة** وأي فلتر تاريخ هيزحف.
  الدوال دلوقتي حرفية من `ecommoda-constants` §13 (`Intl` بـ `Africa/Cairo`).
  🔴 **ومعاها حارس إلزامي:** `Intl.formatToParts` **بترمي** `RangeError` على
  تاريخ غير صالح، والنسخة القديمة كانت بترجّع NaN بلا رمي. صف واحد بتاريخ ناقص
  كان بيرمي جوّه `applyFilters()` — ودي بتتنده من دالة `async`، فالاستثناء
  بيتبلع في promise محدش بيقراها **والشاشة بتقف صامتة**. (اتمسك في
  `tests/zone-picker-flow.test.cjs` وقت الإصلاح نفسه.)
- 🔴 **فلتر تاريخ السجل بيتحوّل لحدود UTC بتاعة يوم القاهرة.** الصفوف متخزّنة
  UTC والموظف بيفكّر بتوقيت القاهرة — `substr(timestamp,1,10)` كانت بتشيل رفع
  الساعة ١:٣٠ بالقاهرة من فلتر «اليوم» (متسجّل ٢٣:٣٠ UTC امبارح)، **والصف ده
  هو الأثر الوحيد على إن الشحنة اتعملت**.
- ⚠️ **الرفع مستثنى من مهلة النداء عمدًا (`timeoutMs: 0`).** قطع النداء
  مابيوقّفش الـ Worker — الشحنات بتبقى اتعملت بفلوس والصفوف بتتعرض «فشل»،
  فالموظف يعيد الرفع ويطلّع شحنة تانية لكل أوردر. المهلة للقراءة بس.
- ⚠️ **`diag` و`get_config` بيعدّوا لما `WORKER_SECRET` يكون غايب** — دول مسار
  التشخيص، ومن غير الاستثناء ده رسالة «شغّل ?action=diag» بتبقى بتحيل على حاجة
  محجوبة وفحص السر جوّه `diag` بيبقى كود ميت.
- 🔴 **ترتيب السجل server-side، مش على الصفحة المحمّلة.** السجل بيتصفّح
  (`get_logs` بـ limit/offset)، فالترتيب client-side كان بيرتّب **١٠٠ صف** —
  والسهم في الترويسة شكله ترتيب عام. دلوقتي `sortBy`/`sortDir` بيتبعتوا للـ
  Worker، والضغط بيرجّع لصفحة ١ (الترتيب بيغيّر أنهي صف على أنهي صفحة).
  🔴 **ومفاتيح الواجهة لازم تطابق `LOG_SORT_COLUMNS` في الـ Worker حرفيًا** —
  مفتاح بره القايمة المقفولة بيرجع للافتراضي **في صمت**، فالعمود يبان مرتّب
  وهو مش مرتّب. و`ORDER BY` على عمود غير `timestamp` معاه **كاسر تعادل** إلزامي،
  من غيره الصف الواحد ممكن يظهر في صفحتين أو مايظهرش خالص.
- 🔴 **`wirePhone` قبل الإرسال — مش `normPhone`.** الأخيرة مفتاح **مقارنة** بس
  (بتشيل الصفر و20). المتجر فيه **تلات** أشكال تليفون مقيسة: `01…` · `+201…` ·
  و**`+20 12 71043044` بمسافات** (`#53849`). الكود كان بيبعت الحقل الخام، يعني
  نص فيه مسافات كان بيروح في `receiver.phone`.
- ⚠️ **`goodsInfo.amount` عليه تكلفة مباشرة:** بوسطة بتحسب
  `pricing.insuranceFee` = **١٪** منه تلقائيًا (مقيس: 2600 → 26).
  `bosta-api-helper` 8.9 كانت بتقول «مالوش أثر مالي تلقائي» — اتصحّحت في v2.0.0.
- 🔴 **`uniqueBusinessReference = رقم الأوردر` محجوز للأداة دي بالذات.**
  الفرادة عند بوسطة على **الحساب كله وعبر كل أنواع الشحنات**، و`terminate`
  بيحررها. أي أداة تانية ترفع شحنة على نفس الأوردر لازم قيمة مختلفة وإلا
  `400 · 11000` — أداة الاسترجاع/الاستبدال بتبعت `#12345-R{n}` / `#12345-EX{n}`.
- 🔴 **`Math.abs` على الـ `cod` صح هنا بس.** ده أوردر شحن عادي والسالب معناه
  العميل دفع زيادة. **ممنوع نسخ السطر لأي أداة استرجاع/استبدال** — هناك السالب
  معناه بوسطة بتدفع للعميل، والـ abs بتحوّل «رجّعله ٢٠٠٠» لـ«حصّل منه ٢٠٠٠».
- ⚠️ **مش كل فشل من بوسطة معاه `errorCode`** — شكل العنوان الغلط بيرجّع **500
  بلا كود خالص**. أي `humanizeBostaError` بيفترض وجود كود بيطلّع رسالة فاضية.
- ⚠️ **السر الناقص بيشيل الحماية مش بيشدّدها.** لو `WORKER_SECRET` غايب، القالب
  بينتج `"Bearer undefined"` وأي طلب بالهيدر ده **بيعدّي**. الحارس دلوقتي قبل
  فحص الـ auth نفسه.
- ⚠️ **غياب `read_all_orders` بيرجّع صفر نتيجة مش خطأ صلاحية.** الأداة بتفلتر
  من `START_DATE`، فأول ما التاريخ ده يعدّي الـ ٦٠ يوم الأوردرات القديمة بتختفي
  من القايمة في صمت. `?action=diag` هو الإشارة الوحيدة.
- ⚠️ **«رفع الكل» بيشتغل على `visibleRows` (الفلتر الحالي) مش على `allRows`.**
  الفرق ده هو كل الأمان في الزرار ده: الموظف بيفلتر، بيشوف العدد جوّه الزرار،
  وبيأكد. الربط بـ`allRows` كان هيرفع أوردرات مش ظاهرة على الشاشة أصلًا.
  وصفوف 🟠 **مستثناة** منه زيها زي «تحديد الكل». (v1.5.0)
- 🔴 **كاشف المدينة اقتراح — مش قرار.** أسماء المناطق بتتكرر بين المحافظات،
  فالتطبيق التلقائي هو **بالظبط** الفخ اللي جدول المحافظات المقفول اتكتب
  عشان يمنعه. الاقتراح بيتعرض والموظف بيضغط.
- 🔴 **التعديل اليدوي بيشمل المدينة، والتحقق بيوقف مش بيرجع.** منطقة مش تابعة
  للمدينة المختارة = **وقف الصف بخطأ صريح**، مش رجوع صامت للمطابقة التلقائية.
  ورجوع `3003` بيقرا من نفس كائن الخطة، فبيفضل ماسك المدينة المعدّلة — لو
  اتكتب في متغير جنبي كان هيبعت المدينة الأصلية الغلط في صمت.
- 🟡 **حالة «مفيش مطابقة منطقة» مش حالة فشل** — دي بالظبط سلوك البلجن الحالي.
  الشحنة بتدخل مسار *Unclear Addresses* بتاع بوسطة والأوبريشن بيحددوا المنطقة
  بمكالمة.
- 🟡 **بانر أحمر «فلتر الحالة مش متسق»** معناه `count(Confirmed)` +
  `count(Confirmed + Edit)` مش بيساوي العدّ المدمج. الأداة بتفضل شغّالة على
  الاستعلام المدمج، بس ده معناه إن فيه أوردرات ممكن تكون مش ظاهرة — بلّغ فورًا.
- ⚠️ **الأداة متلمسش `custom.manual_status` ولا `custom.zone`.** الانتقال
  `Confirmed → Ready` بيحصل عند **الطباعة** مش عند الرفع — تقديم الحالة من هنا
  بيكسر بوابة الطباعة. و`custom.zone` قرار بشري عن توفّر المندوب، مش استنتاج.
- ⚠️ **`allowToOpenPackage: true` على كل الأوردرات** — أثر مالي EGP 7/شحنة،
  قرار تشغيلي مقصود. **متتشالش كـ«تنضيف كود»**.
  ✅ **اتأكد تاني 13-09-2026 (أحمد): يتحط على كل الشحنات — بما فيها الاسترجاع
  والاستبدال.** ده بيقفل بند ب-٨ اللي كان مفتوح في
  `Bosta-Return-Exchange-Exporter` (كان السؤال: هل الرسم بيتحسب على شحنة CRP
  أصلًا؟). القرار: يتحط، والتكلفة مقبولة.
- ⚠️ **`flexShippingInfo` بقيمة 100 على كل الأوردرات** — المبلغ اللي العميل
  بيدفعه كمصاريف شحن لو رفض الاستلام.
- ⚠️ **مفيش `/deliveries/bulk`.** رد الـ bulk بيرجّع IDs بس من غير
  `trackingNumber`، وبيشتغل على العقد الموثّق اللي بيرفض أي شحنة من غير منطقة.
  التنفيذ حلقة نداءات فردية بتوازي متحفّظ — حدود استهلاك بوسطة **مش موثّقة في
  أي مصدر رسمي**، فالتوازي يتظبط بعد قياس حي مش بالتخمين.

## استرجاع النسخ القديمة

> ده بديل الـ tags — دفع الـ tags ممنوع من جلسات Claude Code السحابية.

```
النسخة الأولى (v1.0.0) على فرع claude/build-tool-from-file-94scbm
git show <sha>:index.js
```

## بصمة المهارات

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-worker-builder | v3.3.0 |
| ecommoda-html-builder | v7.1.0 |
| ecommoda-constants | v2.5.0 |
| bosta-api-helper | v2.0.0 |
| shopify-graphql-helper | v2.2.0 |
| ecommoda-order-lifecycle | v1.6.0 |

آخر مطابقة: 13-09-2026 · `index.js` v1.4.0 · `index.html` v1.6.0

> الجولة دي اتعملت بـ `skills-sweep` على الإصدارات الحالية، **والأربع بنود 🔴
> اللي طلعت اتقفلت كلها** (الوقت الثابت · الترتيب client-side · `parseInt` بلا
> حراسة · `ORDER BY` حرفي)، ومعاها التلات بنود 🟡 (`apiRequest` بلا مهلة ·
> حارس `WORKER_SECRET` · `read_all_orders` في `diag`).
>
> ⚠️ **البصمة دي بتقول «اتحاذت عند الإصدارات دي»، مش «مراجعة امتثال كاملة
> لكل بند».** اللي اتعمل: كل بند عنده **وصفة فحص** في الطبقة ② اتفحص واتقفل،
> وبنود `bosta-api-helper` v2.0.0 اتراجعت يدويًا بالكامل (هي ٩ بنود وكلها
> بتخص الأداة دي). الباقي — بنود الطبقة ① اللي مالهاش وصفة — **ما اتراجعش
> بندًا بندًا**، وده مكتوب هنا عن قصد بدل ما الجدول يدّعي أكتر مما حصل.

🔴 معلّقة: — لا شيء

## مسائل مفتوحة

1. ✅ **تسجيل `bosta_orders_upload` في `ecommoda-constants` §7 — اتعمل**
   (المهارة v2.5.0، 13-09-2026). البند ده كان حاجز على أول `writeLog` وبقى مقفول.
2. 🟠 **حالتين شك كاذب في كاشف المدينة** اتقاسوا على عناوين مرتجعات حقيقية
   (10-09-2026): `#53075` (الدقهلية + «الجماليه») و`#52888` (الإسكندرية +
   «Green Plaza»). الاتنين بيولّعوا 🟠 على صفوف سليمة — مش كاسر (الموظف بيراجع
   ويحدد) بس بيزوّد الشغل اليدوي.
   ⚠️ **وأي تعديل على المحرك أو اختباره يتعمل في الريبوين مع بعض** — النسخة
   التانية في `Bosta-Return-Exchange-Exporter` v6.0.0.
3. 🟡 **محرك مطابقة العنوان لسه نسختين.** `bosta-api-helper` v2.0.0 رقّاه لبلوك
   «انسخه حرفيًا» (8.6a)، بس **البلوك نفسه لسه ما اتنزّلش في السكيل** — لحد ما
   ينزل، النسخة المعتمدة هي بتاعة `Bosta-Return-Exchange-Exporter` v6.0.0.
4. 🟡 **بنود الطبقة ① اللي مالهاش وصفة فحص ما اتراجعتش بندًا بندًا** — راجع
   الملحوظة تحت جدول البصمة.
5. ⚪ **الجرد بيطلّع إيجابية كاذبة واحدة على الأداة دي** — وصفة
   `H5-clientside-sort-with-serverside-pages` بتطابق `applySort` مع
   `get_logs_count` في نفس الملف، وهي مابتفرّقش بين **جدول الأوردرات**
   (محمّل بالكامل من `get_orders` — الترتيب client-side صح فيه) و**سجل
   العمليات** (مصفَّح — واتحوّل server-side في v1.6.0). الأداة سليمة في
   الاتنين. المقترح في `skillsupdates20260913.md`.
6. 🟡 **الـ Worker `bosta-webhook` على حساب Cloudflare المهجور `ecommoda24`** —
   موجود ولسه شغّال ولا لأ؟ (SPEC §٩.١ · بند م-١). **مش حاجز على الأداة دي**
   لأنها مبتبعتش `webhookUrl` ومبتستقبلش ويبهوك، بس الشحنات اللي بترفعها
   هتبعت تحديثات حالتها للرابط ده تلقائيًا زيها زي أي شحنة تانية.
7. 🟡 **حدود استهلاك API بوسطة غير معروفة** — التوازي حاليًا `UPLOAD_CONC = 3`
   (متحفّظ). يتظبط بعد قياس حي، مش بالتخمين.
8. 🟡 **`CATALOG_KV` مش متعمّل.** الكاش شغّال على Cache API — كافي وشغّال من
   غير أي إعداد. لو اتعمل namespace، فك التعليق في `wrangler.toml` وحطّ الـ id
   والكود بيستخدمه تلقائيًا.
9. 🟠 **أنهي مدن تانية عندها نفس فرق العبور؟** المرشحين للفحص على الكتالوج
   الحي: الشروق · بدر · العاشر من رمضان · العاصمة الإدارية · الشيخ زايد.
   دلوقتي الكاشف بيلقطهم كاقتراح، بس المتكرر منهم يستاهل يبقى صف في الجدول.
   القياس من `city_overridden` في D1 بعد أول أسبوع تشغيل.
10. 🟡 **مطابقة الزون محتاجة قياس حي.** اتفعّلت في v1.3.0 وحلّت العبور. الباقي:
   نتابع كام صف بيطلّع اقتراح زون وكام منهم الموظف بيقبله (من `city_overridden`
   في D1). لو نسبة القبول عالية جدًا على مطابقة **تطابق كامل في خانة `city`**،
   نبص وقتها في تطبيق تلقائي للحالة دي بس — ومحتاج قرار صريح لأنه بيعدّل
   قرار ٣١ («اقتراح مش قرار»).
11. 🟡 **شكل رد `GET /cities/getAllDistricts` مش موثّق بدقة** — الكود بيطبّع أكتر
   من شكل محتمل، والفحص الذاتي بيعرض عدد المدن والمناطق المتاحة. لو العدد طلع
   صفر، ده معناه إن شكل الرد اتغيّر مش إن مفيش مناطق.

آخر تحديث: 13-09-2026

</div>
