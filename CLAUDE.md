<div dir="rtl" style="text-align: right;">

# رفع بوسطة (`Bosta-Orders-Upload`)

![version](https://img.shields.io/badge/version-v2.13.0-blue)

**بتعمل إيه:** كل شحنات بوسطة من أداة واحدة — **شحن عادي** (type 10) و**استرجاع**
(25) و**استبدال** (30). بتعرض الأوردرات المؤهَّلة لكل نوع، والموظف بيرفعها جماعيًا،
والأداة بتكتب النتيجة على شوبيفاي وبتسجّلها في D1.
**مين بيستخدمها:** العمليات · المخزن
**الإصدار:** Worker `v2.3.0` · الواجهة `v2.13.0`

> **v2.0.0 = الدمج.** `Bosta-Return-Exchange-Exporter` v6.0.0 اتنقلت هنا بالكامل.
> التكليف الأصلي في `MERGE-BRIEF.md`، وخطة تصفية الأداة القديمة في §«التصفية» تحت.
>
> **العقد المرجعي الكامل في `SPEC.md`** — كل قرار وفخ مشروح هناك بمصدره.

## الروابط

```
الواجهة    : https://ecommoda-dev.github.io/Bosta-Orders-Upload/
الـ Worker : https://bosta-orders-upload-worker.ecommoda-dev.workers.dev
اسم الـ Worker في الداشبورد: bosta-orders-upload-worker     ← لازم يطابق name في wrangler.toml
```

## الأوضاع التلاتة

| | 🚚 شحن عادي | ↩️ استرجاع | 🔄 استبدال |
|---|---|---|---|
| `jobType` | `s1` | `return` | `exchange` |
| نوع بوسطة | 10 | 25 (CRP) | 30 |
| الفلتر | `custom.zone = Other_Regions` + `manual_status` = `Confirmed` / `Confirmed + Edit` + `created_at >= START_DATE` | `status_2_r_e = Confirmed + RETURN` **و** `courier = Bosta` | `status_2_r_e = Confirmed + EXCHANGE` **و** `courier = Bosta` |
| عنوان العميل | `dropOffAddress` | **`pickupAddress`** | `dropOffAddress` |
| الـ `cod` | `Math.abs` | **بإشارته** | **بإشارته** |
| `uniqueBusinessReference` | `12345` | `#12345-R{n}` | `#12345-EX{n}` |
| بيكتب بعد الشحنة | `courier` · `bosta_tracking_number_s1` · تاج `Bosta_Uploaded_S1` | `bosta_tracking_number_s2` · تاج `Bosta_Uploaded_S2` · **`status_2_r_e = In-Return`** | `bosta_tracking_number_s2` · تاج `Bosta_Uploaded_S2` |
| بوليصة بتتطبع؟ | ✅ | ❌ **لأ** — بوسطة بتروح تجيب المرتجع من العميل | ✅ (فيه طرد خارج) |
| بيحرّك الحالة؟ | ❌ لأ — النقلة عند **الطباعة** | ✅ **`In-Return`** (v2.3.0) — **مفيش طباعة**، فالرفع آخر خطوة | ❌ لأ (v2.0.1) — النقلة عند **الطباعة** |
| الصف بيفضل في القايمة بعد الرفع؟ | ✅ (بادج «🔁 مرفوع») | ❌ **بيخرج** — الفلتر بيقرا الحالة | ✅ (بادج «🔁 مرفوع») |
| `tool` في D1 | `bosta_orders_upload` | `bosta_exchange_export` | `bosta_exchange_export` |

## الـ Endpoints

| `?action=` | بيعمل إيه |
|---|---|
| `get_orders` | أوردرات **الشحن العادي** + خطة العنوان لكل صف + حارس فلتر الحالة |
| `fetch_candidates` | أوردرات **الاسترجاع/الاستبدال** (POST · `jobType`) — نفس شكل الصف + الدورة المفتوحة والقطع والمبلغ بإشارته |
| `get_districts` | من غير `cityId`: كل مدن بوسطة · مع `cityId`: مناطقها (`dropOffAvailability === true` فقط) |
| `upload` | رفع دفعة شحن عادي (٢٥ أوردر كحد أقصى) + الكتابة على شوبيفاي + السجل |
| `upload_re` | 🔴 **بينشئ شحنات حقيقية بفلوس.** حارس الدورات والكوريَر → إعادة قراءة كاملة من شوبيفاي → الشحنة → رقم التتبع والتاج (**+ `status_2_r_e = In-Return` في الاسترجاع بس**) → السجل. الاستبدال **مابيحركش الحالة** (v2.0.1) |
| `cancel_re` | إلغاء شحنة R/E بالـ `trackingNumber`. الإلغاء **بيحرّر** المرجع الفريد فإعادة الرفع شغّالة. **مابيرجّعش S2 ولا بيمسح رقم التتبع** |
| `check_export_duplicates` · `record_export` · `confirm_upload` | مسار الإكسيل (الخطة البديلة) |
| `diag` · `get_config` | الفحص الذاتي · نسخة الـ Worker |
| `get_logs` · `get_logs_count` · `get_logs_export` | سجل العمليات — بيقرا **قيمتي `tool`** |

## D1

```
tool  : bosta_orders_upload     ← الشحن العادي
        bosta_exchange_export   ← الاسترجاع/الاستبدال (القيمة التاريخية)

type (شحن)  : uploaded · upload_failed · shopify_write_failed · skipped · login · logout
type (R/E)  : upload_re_return · upload_re_exchange · re_upload_failed ·
              re_shopify_write_failed · re_cancelled · cycle_block · scan ·
              export_return · export_exchange · confirm_return · confirm_exchange
```

> 🔴 **`extra.address_degree` (v2.1.0) — `district` · `zone` · `province`.**
> `contract_used` **لوحده بقى ناقص**: الزون والمحافظة الاتنين بيروحوا على العقد
> غير الموثّق، فالعمود مش بيفرّق بينهم (`bosta-api-helper` 8.10.4). من غير
> الدرجة مفيش طريقة نقيس بعدين كام شحنة راحت بأنهي درجة — وده **قياس مطلوب في
> أول أسبوع تشغيل**، ومعاه `extra.zone_sent` لاسم الزون.

> 🔴 **قيمتين `tool` في Worker واحد — استثناء معلن.** ده اختيار «أ» في
> `MERGE-BRIEF` §٥، **وقرار أحمد إنه يتأجّل لحد ما التجربة تكتمل**. المكسب:
> صفر هجرة · الـ ٥٦٦ صف التاريخية ما اتيتّمتش · الداشبوردات ما اتمستش ·
> وتاب السجل بيقرا الاتنين فالموظف بيشوف تاريخ متصل. تغييرها بعدين = سطر
> واحد + مهارة `ecommoda-tool-rename`.
> ⚠️ **لازم يتسجّل كاستثناء في `ecommoda-constants` §7** — بند مفتوح.

> 🔴 **قيم `type` بتاعة R/E لسه محتاجة تسجيل في `ecommoda-constants` §7.**
> القاعدة (`worker-builder` Rule 7) بتقول التسجيل **قبل** أول `writeLog` مش
> بعده — ده استثناء واعي متوارث من الأداة القديمة، مكتوب هنا عشان اللي هيقرا
> بعد كده مايفتكرش إن التسجيل اتعمل.

> كل نقلة S2 ناجحة بتكتب **كمان** صف في `tool = 'metafields_change'`
> (`type = 'update'`) — مطلوب عشان KPIs زمن الدورة (بتتقرا من `metafields_change`
> بس) تشوف التحديث.
> 🔴 **مصدرين من v2.3.0:** `confirm_upload` (مسار الإكسيل) **و**`upload_re`
> في وضع **الاسترجاع** — هو اللي بيعمل نقلة حالة فعلية دلوقتي.
> ⚠️ **والصف بيتكتب للنقلة المؤكَّدة بس** (`r.s2Written` — يعني شوبيفاي رجّعت
> القيمة الجديدة في نفس النداء)، مش لكل شحنة ناجحة: صف بيوثّق نقلة ما حصلتش
> بيدّي الـ KPIs تاريخ اتحرك فيه حاجة على الورق بس. والاستبدال **مابيكتبش**
> الصف ده من `upload_re` أصلًا — مفيش نقلة بتحصل هناك.

⚠️ **`upload_failed` منفصلة عمدًا عن `shopify_write_failed`** (وكذلك
`re_upload_failed` عن `re_shopify_write_failed`). الأولى = الشحنة ما اترفعتش
أصلًا فإعادة المحاولة آمنة. التانية = الشحنة **موجودة فعلًا** عند بوسطة ومعاها
رقم تتبع، والكتابة الرجعية هي اللي فشلت. خلطهم بيخلي أي إعادة محاولة تعمل
**شحنة مكررة بفلوس حقيقية**، مش مجرد صف سجل ناقص.

## المضبوط فعليًا في الداشبورد

```
Bindings : DB → ecommoda-dev-logs
           CATALOG_KV (اختياري — من غيره الكاش بيشتغل على Cache API تلقائيًا)
Secrets  : WORKER_SECRET · CLIENT_ID · CLIENT_SECRET · BOSTA_API_KEY
Vars     : SHOP_DOMAIN                    ← من [vars] في wrangler.toml
Build watch paths : * (الافتراضي)
```

> `BOSTA_API_KEY` **يتشارك** مع باقي أدوات بوسطة — EcomModa عندها حساب بوسطة
> واحد (`ecommoda-constants` §3). مفيش `_1ry`/`_2ry` هنا — دول بتوع Khiam Store.

⚠️ بعد إضافة أي سر: Deployments → Version History → ⋯ → **Promote version**.
من غيرها القيمة بتفضل `undefined` وكل نداء بيرجّع `Unauthorized`.

## CORS

`ALLOWED_ORIGINS` صارمة (`https://ecommoda-dev.github.io`) — الأداة **بتكتب**
على شوبيفاي وبتنشئ شحنات بفلوس حقيقية بتلات أنواع.

## الاختبارات

```
node tests/address-matching.test.cjs   # ترجيح مطابقة المنطقة — حالات حقيقية
node tests/row-state.test.cjs          # حالة الصف · استثناء «تحديد الكل» · تحرير صف خارج التغطية
node tests/zone-label.test.cjs         # عرض الزون و«بلا زون» الصريحة
node tests/zone-cross-city.test.cjs    # اقتراح زون العبور تحت القاهرة
node tests/zone-picker-flow.test.cjs   # رحلة الضغطة الواحدة · التلميحات الباقية · بانر التغطية المشال · رفض الدرجة الأقل
node tests/bulk-and-pin.test.cjs       # «رفع الكل» على الفلتر + تثبيت المنطقة المطابقة
node tests/cairo-time.test.cjs         # إزاحة القاهرة المحسوبة + حارس التاريخ غير الصالح
node tests/worker-logs.test.cjs        # ترتيب السجل (قايمة مقفولة) · clampInt · حدود يوم القاهرة
node tests/re-payload.test.cjs         # عقد شحنة R/E — الاتجاه · الـ cod · المرجع الفريد
node tests/upload-flow.test.cjs        # مسار رفع R/E على fetch مزيّف — 3003 · 11000 · 500 · الكتابة الرجعية
node tests/job-modes.test.cjs          # الأوضاع التلاتة في الواجهة — الأعمدة · اتجاه الفلوس · التاج/الميتافيلد · بادج حالة الرفع
node tests/re-endpoints.test.cjs       # الـ handler نفسه من الطلب للرد — الاتجاه · الحارس · الكتابة الرجعية
node tests/result-modal.test.cjs       # نافذة النتيجة · مربعات العدّادات · «حالة العنوان» اسم واحد · سياق نافذة المنطقة
node tests/coverage-and-degree.test.cjs # التغطية المقفولة · تحريرها بمنطقة يدوية · درجة المدينة · سلّم النزول · حراس القيمة · الإلغاء المكرر
node tests/upload-confirm.test.cjs     # نافذة تأكيد الرفع — نافذة واحدة للزرارين · مربعات حالة العنوان · التثبيت الأخضر
```

🔴 **`re-endpoints` هو الوحيد اللي بيشغّل الـ handler نفسه** (`fetch(request, env)`)
مش دالة جوّاه. موجود لأن `upload_re` **ما اشتغلش حي ولا مرة**: كل حاجة تحته
اتجربت كدوال منفصلة، والوصلات بينها (الـ routing · الحارس · الترتيب · الرد)
ما حدش شغّلها من الأول للآخر. وبيتحقق من ادعاء **«مفيش ولا شحنة اتعملت»**
بعدّ نداءات بوسطة الفعلية — الادعاء ده مكتوب في مودال الرفض، ولو كان غلط
الموظف هيسيب شحنة مدفوعة وراه.

كلها بتشتغل بـ Node عادي من غير أي تنصيب. اللي بيلمس الـ Worker بيحمّل
`index.js` في sandbox، واللي بيلمس الواجهة بيحمّل الـ `<script>` بتاع
`index.html` على DOM مزيّف.

🔴 **نسخة واحدة بس من محرك مطابقة العنوان** — ودي أكبر مكسب هيكلي من الدمج.
كان فيه نسختين متطابقتين بالنية، وبند مفتوح عن خطر افتراقهم. **وقت النقل طلع
إنهم كانوا افترقوا فعلًا**: نسخة الـ Exporter كان فيها إصلاحين مش موجودين هنا
(`findLocalZones(null)` بترمي · ومدينة مش في الكتالوج الحي بتتدهور لـ«مفيش
مطابقة» بدل ما توقف). الاتنين اتنقلوا، والاختبار اللي مسكهم هو
`re-payload.test.cjs` ⑨.

## 🔴 قاموس العرض — الاسم على الشاشة ≠ اسم الحقل

> **اتقرر 14-09-2026 · الواجهة v2.8.0 · التوثيق الكامل في `BOSTA-NAMING-20260914.md`**

داشبورد بوسطة نفسها بتسمّي حقولها غير ما الـ API بيسمّيها. نموذج إنشاء الأوردر
عندهم بيقول `Governorate and Area` / `المحافظة والمنطقة` — والقيمة اللي تحت
«المحافظة» هي الـ `city` في الـ API. يعني ترجمة اسم الحقل حرفيًا للموظف بتخترع
مصطلح **مش موجود عند بوسطة ولا عند شوبيفاي**.

| حقل الـ API | بوسطة (EN) | **العرض عندنا** | شوبيفاي |
|---|---|---|---|
| `city` · `cityId` | `Governorate` | **المحافظة** | `province` → المحافظة |
| `zone` · `zoneId` | `Zone` | **المدينة** | `city` → المدينة |
| `district` · `districtId` | `Area` | **المنطقة** | — |

```
🛍️ شوبيفاي   المحافظة │ المدينة │ (فاضية)
🚚 بوسطة      المحافظة │ المدينة │ المنطقة     ← بقوا يقابلوا بعض عمود بعمود
```

🔴 **أسماء الحقول والمعرّفات في الكود ما اتغيّرتش** — `cityId` · `cityName` ·
`dpCityId` · `dpZoneFilter` · `zoneId` كلها زي ما هي وبتتبعت على السلك زي ما هي.
التغيير **في طبقة العرض بس**.

🔴 **والتطابق بين اسم المتغيّر واسم الخانة اتكسر عن قصد — ده أخطر أثر جانبي:**
`cityId` بيتعرض **«المحافظة»** و`zoneId` بيتعرض **«المدينة»**. أي واحد يربط
`cityName` بخانة «المدينة» غلط بيعرض المحافظة في خانة المدينة — **فشل صامت
كامل**. عشان كده `DP_PICKS` هي **المصدر الوحيد** لتسمية الخانات التلاتة،
و`dpContextHTML` بتقرا منها بدل نص مكتوب بالإيد.

⚠️ **و«المحافظة» بقت توصف مصدرين** (`province` بتاع شوبيفاي · `city` بتاع
بوسطة) وهما **ممكن يختلفوا** — والفرق ده هو بالظبط اللي الموظف بيراجعه. فأي
شاشة بتعرض الاتنين **لازم توسم المصدر** (وسم الصف 🛍️/🚚 · أو لاحقة في اسم
العمود «محافظة بوسطة» مقابل «المحافظة (شوبيفاي)»).

🔴 **وممنوع أي `sed` أعمى على «المدينة».** ليها **تلات مصائر** حسب السياق:
بتتحوّل لـ«المحافظة» (سياق `cityId`) · بتفضل «المدينة» (سياق شوبيفاي
`addressCity`) · وبتتخلق من جديد (سياق `zoneId`).

⚠️ **وانتبه للانقلاب في أي نص قديم:** «نزلنا من المنطقة للمدينة» بقى معناها
`districtId → zoneId` — **مش** النزول للمحافظة.

## فخاخ الأداة دي

### 🔴 الأربعة اللي الدمج قايم عليهم

الأربعة دول هما السبب إن بلوكات `§UPLOAD` و`§RE-UPLOAD` فضلت **منفصلة** بدل ما
تتدمج في دالة واحدة بـ `if`. كل واحد فيهم «لو اتكتب `if` هنا هيبقى غلط».

- 🔴 **`Math.abs` على الـ `cod` — أخطر سطر في العملية كلها.** صح في الشحن العادي
  (السالب معناه العميل دفع زيادة) و**كارثة** في R/E (السالب معناه بوسطة بتدفع
  للعميل عند الباب). من ٤ أوردرات R/E مقيسة **٣ سالبين**. النسخ الحرفي بيحوّل
  «رجّعله ٢٠٠٠» لـ«حصّل منه ٢٠٠٠»، والعميل بيرفض الاستلام.
  ❌ **ممنوع نسخ سطر الـ cod من `buildDeliveryPayload` لـ`buildRePayload`.** ❌
- 🔴 **اتجاه العنوان بيتقلب حسب النوع.** 10 و30 → `dropOffAddress` · 25 →
  `pickupAddress`، وبوسطة بتملا ناحية المخزن لوحدها من `businessLocationId`.
  الغلط في الاتجاه بيرجّع **HTTP 500 بلا `errorCode`** — مش 400 واضح.
- 🔴 **`uniqueBusinessReference` قاعدتين في Worker واحد.** الفرادة عند بوسطة على
  **الحساب كله وعبر كل أنواع الشحنات**، و`terminate` بيحررها. `12345` محجوز
  لشحنة الشحن العادي؛ R/E بتبعت `#12345-R{n}`/`#12345-EX{n}` من اسم الدورة.
  نفس القيمة = `400 · errorCode "11000"`. و`businessReference` بيفضل `#12345`
  في **الحالتين** — كل سكانرات الستاك بتدوّر بيه.
- 🔴 **فلترين ومكنتين حالة — مش استعلام واحد مدموج.** كل وضع ليه `action` خاص
  (`get_orders` مقابل `fetch_candidates`)، والدمج كان هيخلي فلتر واحد غلط يفضّي
  الشاشتين مع بعض.

### 🔴 الميتافيلدات والتاجات (v2.0.0)

- 🔴 **`custom.bosta_tracking_number` اتوقف.** بدله `_s1` و`_s2` — الاتنين
  `number_integer` وعليهم **Unique values only**. القديم **لسه بيتقرا** في حارس
  الرفع المكرر (`previousTracking...`) عشان الـ ٣٥٦ شحنة اللي اترفعت قبل الدمج؛
  من غير القراءة دي كلها كانت هتبان «مش مرفوعة». **مفيش migration** — التاج
  `Bosta_Uploaded_S1` كان بيتكتب معاه من أول يوم فهو الحارس التاني.
  ⚠️ **مفتوح:** أي أداة تانية في الستاك بتقرا المفتاح القديم؟ (شوف «مسائل مفتوحة» ٢)
- 🔴 **النوع `number_integer` مش نص.** `metafieldsSet` بيطلب تطابق النوع بالحرف
  مع التعريف الحي، واختلافه **بيسقّط النداء كله** — بما فيه أي ميتافيلد تاني في
  نفس النداء. الأداة بتتحقق `/^\d+$/` قبل الكتابة وبتسقّط الحقل لوحده بتحذير.
  الفحص الذاتي بيقارن النوع الحي بالمتوقّع للخمس ميتافيلدات.
- 🔴 **التاج بيتقسم بالنوع** (`Bosta_Uploaded_S1` / `_S2`). تاج واحد للاتنين
  معناه إن حارس الرفع المكرر بتاع الشحن بيتلغي أول ما الأوردر يتعمل له استرجاع،
  والعكس.
- 🔴 **الاسترجاع بيحرّك الحالة وقت الرفع — استثناء معلن.** (v2.3.0 · طلب أحمد
  15-09-2026) شحنة استرجاع ناجحة بتكتب `custom.status_2_r_e = In-Return`،
  جوّه **نفس** نداء `metafieldsSet` بتاع رقم التتبع: نداء واحد بيعدّي كله أو
  يقع كله، فمستحيل تتحرك الحالة والشحنة مالهاش رقم تتبع مكتوب — أو العكس، وهو
  اللي بيخلي الموظف يدوّر على شحنة مش موجودة أو يعيد رفع شحنة موجودة بفلوس.
  🔴 **والسجل بيقرا من `r.s2Written`** (حقيقة مؤكَّدة من شوبيفاي) مش من نيّة
  الكتابة: الصف اللي شحنته نجحت وكتابته فشلت بيفضل `valueBefore == valueAfter`
  ومعاه `re_shopify_write_failed`.
  ⚠️ **النتيجة اللي لازم تتوقعها هنا:** أوردر الاسترجاع **بيخرج من القايمة**
  بعد الرفع والتحديث — فلتر الترشيح بيقرا `Confirmed + RETURN`. اللي بيحمي من
  الرفع المكرر لسه هو التاج ورقم التتبع وحارس `uniqueBusinessReference` عند
  بوسطة، مش وجود الصف أو اختفاؤه.
  🔴 **والسبب إن الاسترجاع مالوش طباعة أصلًا** (أحمد 15-09-2026): **مابنطبعش
  بوليصة لأوردر استرجاع** — بوسطة بتروح تجيب المرتجع من عند العميل على طول.
  يعني **مفيش بوابة طباعة** بعد الرفع تنقل الحالة، والرفع هو **آخر خطوة عندنا**
  في المسار ده. فقاعدة «النقلة عند الطباعة» **مالهاش محل هنا** — تطبيقها كان
  معناه إن الحالة **ماتتحركش أبدًا**، والأوردر يفضل `Confirmed + RETURN`
  للأبد ما لم يتدخّل حد بإيده.
  ✅ **وده كمان سبب إن `printing_time_s2` يفضل فاضي في الاسترجاع** — مش تأجيل
  لحد الطباعة، **مفيش طباعة**. والاستبدال مختلف: فيه طرد **خارج** للعميل،
  فالبوليصة بتتطبع وقاعدة الطباعة لسه سارية عليه.
- 🔴 **والشحن العادي والاستبدال لسه مابيحركوش الحالة.** (v2.0.1) الانتقال فيهم
  بيحصل عند **الطباعة** مش عند الرفع، وتقديم الحالة من هنا **بيكسر بوابة
  الطباعة**. `upload_re` كان بيكتب `status_2_r_e → In-Return`/`Ready`
  **و**`printing_time_s2` على طول بعد الشحنة، والاتنين اتشالوا بقرار أحمد بعد
  أول شحنة استبدال حية — v2.3.0 رجّعت **نص واحد** من ده (حالة الاسترجاع)،
  والباقي زي ما هو.
  🔴 و`printing_time_s2` **وقت طباعة** — كتابته وقت الرفع بتخلي أي تقرير مبني
  عليه يقول إن البوليصة اتطبعت وهي ما اتطبعتش. **مابيتكتبش في أي وضع.**
  ⚠️ **النتيجة اللي لازم تتوقعها في الوضعين دول:** الأوردر **بيفضل في القايمة
  بعد الرفع** ومعاه بادج «🔁 مرفوع» — لأن فلتر الترشيح بيقرا الحالة وهي ما
  اتغيّرتش، واللي بيحمي من الرفع المكرر هو التاج ورقم التتبع وحارس
  `uniqueBusinessReference` — مش اختفاء الصف.
  🔴 **وعشان كده «مرفوع» أسبق من «موقوف» في `rowUpState`** (واجهة v2.10.1).
  الصف بيرجع بعد التحديث، و`districtOverride` بيتمسح مع كل تحميل — فالصف اللي
  كان خارج التغطية بيرجع موقوف. لما «موقوف» كان بيكسب، البادج كان بيقول
  «⛔ موقوف» و**رقم التتبع يختفي من الشاشة خالص**: الصف يقرا «لسه ما اترفعش
  ومحتاج تصليح» وهو مرفوع فعلًا، والموظف يروح داشبورد بوسطة يتأكد بنفسه.
  القاعدة: «مرفوع» **حقيقة** عن شحنة موجودة بفلوس، و«موقوف» **توقع** عن رفع
  جاي — والحقيقة تكسب. ⚠️ **والمنع مابيضعفش**: عايش في `rowUploadable` (الصف
  الوردي · الشيك بوكس المقفول · الاستبعاد من «رفع الكل») مش في البادج، والصف
  اللي الاتنين عليه بياخد `⛔` جنب رقم التتبع والسبب في التلميح. متغطّى في
  `row-state` و`job-modes` ⑥.
  ⚠️ **و`confirm_upload`** (مسار الإكسيل) زي ما هو: هناك الشحنة بتتعمل من
  داشبورد بوسطة بالإيد، فتحديث الحالة خطوة يدوية منفصلة بمودال وchecklist، مش
  أثر جانبي للرفع. وهو **المصدر الوحيد لنقلة `Ready`** بتاعة الاستبدال — بس
  مابقاش المصدر الوحيد لصفوف `metafields_change` (شوف §D1 فوق).
- 🔴 **في R/E الأداة مابتكتبش `custom.courier` — بتتحقق منه.** شحنة استرجاع
  بتسحب من عند العميل، فكتابة الكوريَر معناها إننا بنعيّن مندوب بوسطة على أوردر
  مش بتاعنا بدل ما نتأكد إنه بتاعنا. التحقق في حارس الدورات (`COURIER_NOT_BOSTA`)
  **قبل** أي نداء لبوسطة. الكتابة بتخص الشحن العادي لوحده — هو اللي بيختار
  الكوريَر أصلًا.

### 🔴 الاسترجاع/الاستبدال

- 🔴 **سقف الاسترداد `-2000` حد من بوسطة** (`errorCode 3008`)، مش قرار داخلي.
  مقيس حيًا: ‎-2700 رجّع 400 **ومفيش شحنة اتعملت**. الأداة بتقص عنده **وبتعلن
  القص على الصف** (✂ + الباقي) — القص الصامت بتاع الإكسيل معناه إن العميل بياخد
  أقل من حقه ومحدش شايف. (`#53517`: مستحق 2700 · الملف 2000.)
- 🔴 **كل رقم في الصف متحسب من الدورة المفتوحة الواحدة.** الأوردر ممكن يكون عدّى
  بأكتر من دورة والـ S2 بيقول على الأخيرة بس. ده كان **الباج الأساسي لحد
  v5.2.0**، مقيس على `#51656`: تلات دورات طلّعت `Return #Items = 3` و
  `Goods Value = 6300` بدل قطعة واحدة و`1750` — قطع رجعت المخزن خلاص اتشحنت
  تاني. الحماية هيكلية: الـ Worker **مابيرجّعش `returns[]`** للواجهة أصلًا،
  فالتجميع القديم بقى **مستحيل** من الفرونت إند. (Rule 15 ②)
- 🔴 **`exchangeLineItems` الفاضية مش معناها استرجاع.** تعديل أوردر بيشيل قطعة
  الاستبدال اللي شوبيفاي عملها ويحط واحدة بالإيد بيفضّي الـ connection
  **نهائيًا** على استبدال حي — وشوبيفاي في الـ Admin لسه بتطبع «Exchange item
  for return #X» على السطر المشال، يعني الشاشة والـ API بيتناقضوا. مقيس على
  `#53531` و`#53701`: `Goods Value` رجع لسعر القطعة **الراجعة** (2600 بدل 2400).
  الاسترداد من سطور `currentQuantity > 0 && unfulfilledQuantity > 0`،
  **fallback مش merge** (الدمج بيعدّ القطعة مرتين على استبدال سليم)،
  و**مابيعيدش التصنيف** (Rule 8 بتتقرا من الدورة لوحدها).
- 🔴 **حارس الدورة الواحدة واقف قدام الشحنة مش قدام الميتافيلد**، وبيتعاد على
  القراءة الكاملة قبل أول نداء لبوسطة. مودال الواجهة مش البوابة الوحيدة.
  `CYCLE_OVERLAP_OPEN` · `NO_OPEN_CYCLE` · `CYCLES_TRUNCATED` ·
  `EXCHANGE_WITHOUT_ITEMS` · `ORDER_NOT_READABLE` · `COURIER_NOT_BOSTA` بيوقفوا
  الصف؛ `MULTI_CYCLE` · `CYCLE_OVERLAP` · `TYPE_MISMATCH` ·
  `EXCHANGE_ITEMS_RECOVERED` تحذير بس (flag it, never move it).
- 🔴 **دورات `CANCELED`/`DECLINED` بتتشال قبل الترتيب.** الـ `closedAt` بتاعهم
  `null`، والـ null بيتقرا «لسه مفتوحة» في فحص التداخل — فسيبانهم بيفبرك تداخل
  وهمي ويوقف صفوف سليمة.
- 🔴 **الإلغاء بيحرّر `uniqueBusinessReference`** فإعادة الرفع بعد التصحيح
  بتعدّي — ده سبب وجود `cancel_re` بدل زيارة الداشبورد. ⚠️ ومابيرجّعش S2
  ومابيمسحش رقم التتبع من الميتافيلد: دول قرارات تانية، وتخمين اللي الموظف
  قصده بيعيد كتابة حالة حية.
- ⚠️ **الأوردر بقى ليه أكتر من شحنة عند بوسطة تحت نفس الـ `businessReference`**
  (شحنة الشحن + شحنة R/E). أي أداة بتعمل `search` وتاخد `[0]` بقت بتقرا شحنة
  عشوائية. **الحالة دي قايمة من الإكسيل أصلًا** — الـ API مش بيخلقها. الفلترة
  تبقى على `type.code` (10/25/30) مش على النص.
- ⚠️ **`flexShippingInfo` بيتبعت في الشحن العادي بس** (بقيمة 100). بوسطة بتحطها
  لوحدها على R/E وبتعلّمها `status: "Not Applicable"`، فبعتها مابيغيّرش حاجة.

### 🔴 الرفع عمومًا

- 🔴 **«تم جزئيًا» (أصفر) مش فشل.** معناها الشحنة **موجودة عند بوسطة فعلًا**
  ومعاها رقم تتبع، وحاجة بعدها ما تمّتش. إعادة الرفع بتعمل شحنة تانية بفلوس.
  الحل: تصحيح يدوي، أو **إلغاء** وإعادة رفع مصححة. ده ينطبق على الأنواع التلاتة.
- 🔴 **`goodsInfo.amount` محبوسة بين `100` و`50000` — والحد مش موثّق في الـ
  spec خالص.** (v2.1.0 · `bosta-api-helper` 8.4، مقيس حيًا 14-09-2026: `1`
  بيرجّع 400 · `errorCode 41591`) ورسالة بوسطة بتتكلم عن «قيمة الطرد» من غير ما
  تقول إن فيه حد أصلًا، فمن غير حارس عندنا الموظف بيقرا رفض مبهم على أوردر
  سليم. الحارس في `§BOSTA::goodsProblems` وبيتنده من `validateOrder`
  **و**`validateReOrder`.
  🔴 **والحالة أقرب بكتير في R/E**: القيمة هناك متحسبة من سعر القطع اللي بتسافر
  (`valueItems`) مش من subtotal الأوردر — فقطعة راجعة بـ٨٠ جنيه بتدّي
  `amount: 80` وبوسطة بترفض. وده على المسار اللي **لسه ما اشتغلش حي**.
  ⚠️ والحد بيتطبّق **وقت الإنشاء بس** — `PUT` بيقبل `1` (8d ④). لو اتضاف مسار
  تعديل، الحارس **يتكرر** هناك مش يتورّث.
- 🔴 **في الشحن العادي السالب معناه «مفيش تحصيل» — يعني `0`، مش `Math.abs`.**
  (v2.1.0) لحد v2.0.1 كان السطر `Math.abs` وده **بيناقض تعليقه نفسه**: العميل
  دافع زيادة، والـ abs بتحوّل `-200` لـ«حصّل منه 200» — تحصيل من عميل دافع
  زيادة أصلًا.
  ✅ **مؤكَّد من أحمد 14-09-2026: السالب مستحيل هنا و`0` وارد.** فلو قيمة سالبة
  ظهرت فهي **بيانات غلط**، والحارس بيمنعها من تتحوّل لتحصيل بدل ما يعكسها.
  و`cod: 0` عدّى عند بوسطة فمحتاجش معالجة خاصة.
  ⚠️ وبوسطة **بتقبل `cod` سالب على `type 10`** (مقيس 14-09-2026: `-500` عدّى
  200، و`3008` بيمسك تحت `-2000`) — يعني **مفيش أي حاجة عندها بتمسك الحالة
  دي**، والحارس الوحيد هو السطر ده.
  🔴 **وحد `-2000` مش خاص بـ R/E** كما كان مكتوب — بيتطبّق على `type 10` برضه،
  فالحارس الوحيد هو كودنا.
- 🔴 **الإلغاء المكرر نجاح، مش فشل — والفرق في الـ HTTP status.**
  (v2.1.0 · 8.7، مقيس على ٦ شحنات) `terminate` تاني مرة بيرجّع **404** بينما
  `GET`/`PUT` على نفس الشحنة بيرجّعوا **400** — التلاتة بـ`errorCode 1066`.
  فالـ 404 + 1066 **مميَّز**: `cancel_re` بيرجّع `ok: true` ومعاه
  `alreadyGone: true` وصف السجل بيتكتب `result: 'already'`. من غير التفرقة دي
  الموظف بيقرا «فشل الإلغاء» على شحنة اتلغت فعلًا، ويروح داشبورد بوسطة يدوّر
  على حاجة **مش موجودة أصلًا**.
  🔴 **والتحقق من الإلغاء = الاختفاء نفسه** — مفيش `state` نهائي زي
  `48 Terminated` تقدر تقراه، الشحنة بتتشال من النتايج خالص.
- 🔴 **النجاح بيتفحص بـ `res.ok && body.success` — ممنوع `=== 201`.** التوثيق
  الرسمي بيقول `200` والتجربة الحية رجّعت `201`. التحقق برقم صريح معناه شحنة
  اترفعت فعلًا واتحسبت فشل، والموظف يعيد الرفع.
- 🔴 **`errorCode` نص مش رقم** (`String(body.errorCode) === '3003'`). المقارنة
  بالرقم معناها إن الرجوع التلقائي لمسار المحافظة **عمره ما هيشتغل**.
- 🔴 **`businessReference` بالهاش و`uniqueBusinessReference` بدونه — الاتنين مع
  بعض دايمًا.** من غير الهاش الشحنة بترفع بنجاح بس **كل** أدوات EcomModa مش
  هتلاقيها. فشل صامت كامل، مش error.
- 🔴 **`dropOffAddress.city` مش `cityName`.** الڤاليديتور مش شايف `cityName`
  أصلاً وبيتجاهله بصمت — أي نسخ حرفي من داشبورد بوسطة بيقع في الفخ ده.
- ⚠️ **مش كل فشل من بوسطة معاه `errorCode`** — شكل العنوان الغلط بيرجّع **500
  بلا كود خالص**. أي `humanizeBostaError` بيفترض وجود كود بيطلّع رسالة فاضية.
- ⚠️ **الرفع مستثنى من مهلة النداء عمدًا (`timeoutMs: 0`)** — للتلات أوضاع.
  قطع النداء مابيوقّفش الـ Worker: الشحنات بتبقى اتعملت بفلوس والصفوف بتتعرض
  «فشل»، فالموظف يعيد الرفع ويطلّع شحنة تانية لكل أوردر. المهلة للقراءة بس.
- ⚠️ **`goodsInfo.amount` عليه تكلفة مباشرة:** بوسطة بتحسب
  `pricing.insuranceFee` = **١٪** منه تلقائيًا (مقيس: 2600 → 26).
  `bosta-api-helper` 8.9 كانت بتقول «مالوش أثر مالي تلقائي» — اتصحّحت في v2.0.0.
  🔴 **والصيغة نفسها اتصحّحت تاني في v5.0.0: `clamp(1% , 10 , 50)` مش ١٪ مجردة**
  (القياس القديم 2600 → 26 صادف إنه جوه المدى فالحدين ما بانوش · المقيس:
  100 → 10 · 4500 → 45 · 49000 → 50، وبوسطة بترجّع الحدين حرفيًا في
  `insurancePlanInfo`). و**`shipmentFees` شامل التأمين أصلًا** — أي حساب تكلفة
  بيجمع الاتنين بيعدّ التأمين مرتين.
- ⚠️ **`allowToOpenPackage: true` على كل الشحنات** — أثر مالي EGP 7/شحنة، قرار
  تشغيلي مقصود. **متتشالش كـ«تنضيف كود»**. ✅ اتأكد 13-09-2026 (أحمد): يتحط على
  كل الشحنات بما فيها الاسترجاع والاستبدال — وده بيقفل بند ب-٨ اللي كان مفتوح.
- ⚠️ **مفيش `/deliveries/bulk`.** رد الـ bulk بيرجّع IDs بس من غير
  `trackingNumber`، وبيشتغل على العقد الموثّق اللي بيرفض أي شحنة من غير منطقة.
  التنفيذ حلقة نداءات فردية بتوازي متحفّظ — حدود استهلاك بوسطة **مش موثّقة في
  أي مصدر رسمي**، فالتوازي يتظبط بعد قياس حي مش بالتخمين.

### 🔴 مطابقة العنوان

- 🔴 **جدول المحافظات حتمي ومقفول** (`ecommoda-constants` §3.5). ممنوع أي مطابقة
  نصية على اسم المحافظة كخطة بديلة — فيه خمس محافظات على الأقل اسمها مختلف بين
  المنصتين (Beheira/Behira · Qalyubia/El Kalioubia · …). المحافظة اللي مش في
  الجدول **بتوقف الصف بتاعها برسالة صريحة**، مش بتخمين.
- 🔴 **ومدينة الجدول اللي مش في الكتالوج الحي بتوقف الصف كمان** — برسالة بتسمّي
  السبب («كتالوج بوسطة اتغيّر»). الرجوع الصامت لـ«مفيش مطابقة منطقة» كان معناه
  شحنة على **المدينة الغلط**. (اتنقل من نسخة الـ Exporter وقت الدمج · اتمسك في
  `tests/re-payload.test.cjs` ⑨)
- 🔴 **ترجيح المطابقة مش «الأطول يكسب» — والقاعدة القديمة كانت بتنتج منطقة غلط
  بعلامة خضرا.** `كفر الشيخ مركز قلين` كانت بترفع على **Kafr ElSheikh** بدل
  **Qalin** لأن اسم المحافظة أطول. الترتيب دلوقتي: المنطقة اللي اسمها اسم
  المحافظة **بتتأخّر** ← خانة `city` تغلب `address1` ← التطابق الكامل يغلب
  الجزئي ← الطول بيفصل **بس** عند الاحتواء (مدينة نصر / نصر). ومطابقتين
  **منفصلتين** = **غموض معلَن**، مش اختيار بالطول. (SPEC §٥.٤.١)
- 🔴 **المدينة الغلط مش fallback محايد زي المنطقة الناقصة.** «🏙️ المحافظة بس»
  = المدينة صح والمنطقة ناقصة. «🟠 المدينة مشكوك فيها» = **المدينة نفسها غلط**،
  والمدينة بتحدد **الفرع والتسعيرة**. الصفوف دي **مستثناة من «تحديد الكل»**
  و«رفع الكل» في **التلات أوضاع**. (SPEC §٥.٤.٢)
- 🎯 **الزون هو اللي بيحلّ العبور.** اسم المنطقة عند بوسطة تسمية إدارية مركّبة
  (`المنطقة 01 (العبور)`) محدش بيكتبها، لكن **الزون اسمه `العبور`** وهو اللي
  العميل كاتبه. متحقَّق حيًا 07-09-2026: تغطية الزون **١٠٠٪** (القاهرة 590/590 ·
  القليوبية 207/207). 🔴 **الزون بيحسم المدينة مش المنطقة** — زون العبور فيه ٦
  مناطق، واختيار واحدة تلقائيًا تخمين. و`localZones` (زون في **نفس** المدينة)
  **مش حالة شك** — دي اختصار. (SPEC §٥.٤.١ب)
- 🔴 **مطابقة المنطقة بتسأل «هل العنوان فيه اسم المنطقة؟» — والاتجاه ده هو
  حدّها.** بتنجح لما اسم المنطقة اسم مكان بسيط العميل بيكتبه، وبتفشل لما يكون
  تسمية إدارية مركّبة. الحد ده اتعالج بمطابقة الزون، **مش بتغيير اتجاه
  المطابقة** — قلبه كان هيخلي أي كلمة في العنوان تطابق عشرات المناطق.
- 🔴 **كاشف المدينة اقتراح — مش قرار.** أسماء المناطق بتتكرر بين المحافظات،
  فالتطبيق التلقائي هو **بالظبط** الفخ اللي جدول المحافظات المقفول اتكتب عشان
  يمنعه. الاقتراح بيتعرض والموظف بيضغط.
- 🔴 **التعديل اليدوي بيشمل المدينة، والتحقق بيوقف مش بيرجع.** منطقة مش تابعة
  للمدينة المختارة = **وقف الصف بخطأ صريح**، مش رجوع صامت للمطابقة التلقائية.
  ورجوع `3003` بيقرا من نفس كائن الخطة، فبيفضل ماسك المدينة المعدّلة — لو اتكتب
  في متغير جنبي كان هيبعت المدينة الأصلية الغلط في صمت.
- 🔴 **اسم الزون بيتعرض كامل — القص كان بيخفي اللي بيفرّق.** والزون هو اللي
  بيحسم المدينة، فقراءته ناقصة = قرار مدينة على معلومة ناقصة. (v1.4.0)
- 🔴 **المنطقة المطابقة بتتثبّت أول القايمة** بشريط أخضر وبادج «✅ المطابقة
  الحالية» وسبب المطابقة تحتها. كانت مدفونة في مكانها الأبجدي وسط ٤٠٠ صف
  بتمييز لوني باهت، فالموظف كان بيدوّر **بالبحث** على اللي المطابقة لقتها له
  أصلًا. (v1.5.0)
- 🟡 **حالة «مفيش مطابقة منطقة» مش حالة فشل** — الشحنة بتدخل مسار
  *Unclear Addresses* بتاع بوسطة والأوبريشن بيحددوا المنطقة بمكالمة.
- 🔴 **وواجهة v2.12.0 (قرار أحمد 15-09-2026): «مفيش مطابقة منطقة» بقت محتاجة
  تأكيد يدوي قبل الرفع — مش بس رسالة إعلامية.** الحالة نفسها زي ما هي (مش
  فشل)، بس الرفع بقى **موقوف** ليها لحد ما الموظف يفتح نافذة اختيار المنطقة
  ويختار منطقة أو يضغط «ارفع على المحافظة فقط»/«ارفع على المدينة فقط» —
  نفس القاعدة تنطبق على ❓ أكتر من مطابقة و🟠 المحافظة مشكوك فيها. قبل كده
  الصفوف دي كانت تترفع بمجرد تحديد الشيك بوكس (أو حتى في «رفع الكل» لحالتي
  المحافظة فقط والغموض) من غير أي قرار صريح عن المنطقة. الحارس
  (`rowNeedsManualAddressConfirm`) في الواجهة بس — الـ Worker نفسه ما اتغيّرش.
- 🔴 **المنطقة المقفولة للتسليم (`dropOffAvailability: false`) بتتعرض معلّمة —
  ماتتخفيش.** (v2.1.0 · `bosta-api-helper` 8.11) القاعدة **طبقتين**: متتبعتش
  لبوسطة أبدًا ✅، **وتفضل معروضة في نافذة الاختيار** بشريط أحمر ومش قابلة
  للضغط. الإخفاء الصامت اللي كان لحد v2.0.1 بيخلّي الموظف يشوف ٧ مناطق بدل ٩
  ومش عارف إن عنوان العميل أصلًا **برّه تغطية بوسطة**، فيضغط «ارفع على المحافظة
  بس» فاكرها مسار احتياطي سليم — والشحنة بتتشحن بفلوس وترجع بعد أيام بـ
  *outside Bosta's delivery coverage area*.
  🔴 **والعنوان اللي بيطابق واحدة منها بيوقف الصف** (`mode = coverageBlocked` ·
  الحالة في الواجهة «🚫 خارج التغطية») بدل ما ينزل للمحافظة — المسار الصح إن
  الأوردر مايترفعش ويتحوّل لخدمة العملاء (عنوان بديل · كوريَر تاني · إلغاء).
  ده **قرار تشغيلي بشري**، وشغل الأداة إنها تخلّي المعلومة ظاهرة قدامه وقت القرار.
  🔴 **والرسالة الطويلة اتشالت من الجدول، والمنع فضل** (v2.1.1). `buildRow`
  بترجّع قايمتين: `all` بتحكم `uploadable`، و`problems` (اللي بتتعرض) بتستبعد
  رسالة التغطية. السبب إن الجدول بيقول نفس المعلومة **تلات مرات تانية** —
  بادج «حالة العنوان» · اسم المنطقة المقفولة في عمود المنطقة · حالة الرفع
  «موقوف». ⚠️ **والخطر إن اللي يشيل الرسالة يشيل المنع معاها** — متغطّى في
  `tests/coverage-and-degree.test.cjs` ⑥ب.
  ⚠️ والحقل موجود على مستوى **المدينة** كمان مش المنطقة بس. **وعدد** المناطق
  المقفولة **مابيتعرضش** (قرار أحمد 14-09-2026): الرقم مالوش أثر على أي قرار،
  اللي بيفرق إن **المنطقة دي بالذات** مقفولة وده باين عليها في القايمة.
  🔴 **والوقف بيتحرّر بحاجة واحدة بس: منطقة الموظف بيختارها بإيده** (v2.2.0 ·
  واجهة v2.10.0). الوقف قايم على نتيجة **المطابقة التلقائية**؛ الموظف اللي فتح
  النافذة وحدد منطقة تانية **بدّل النتيجة دي بالكامل** والـ payload بيبعت الـ
  `districtId` بتاعه هو. قبل كده كان الصف بيفضل ⛔ **موقوف للأبد**: بادج «حالة
  العنوان» يخضرّ ويقول «عنوان مظبوط» والرفع مايعدّيش ومفيش أي رسالة تقول ليه —
  والنافذة نفسها **بتوعد بالبديل** («اختار بديل لو فيه») والأنبوب مش معترف بيه.
  ⚠️ **و«ارفع على الزون فقط»/«على المحافظة فقط» مابيحرّروش** — دول بيغيّروا
  **درجة** العنوان مش العنوان، وبوسطة بتفضل بتوصّل على نفس النص اللي طابق
  المنطقة المقفولة. والزرارين بيرفضوا **بصوت** على الصف ده (رسالة بتسمّي المسار
  الشغّال) بدل ما يسجّلوا تثبيت أخضر والصف يفضل موقوف — ده كان هيدّي نفس الحيرة.
  🔴 **ومفيش باب خلفي**: المنطقة المختارة بتتحقق من `availableDistricts` اللي
  **مابترجّعش المقفولة أصلًا**، فاختيار منطقة مقفولة = وقف الصف برسالة صريحة
  و**صفر نداء لبوسطة**.
  🔴 **والترتيب جوّه `uploadOne` هو نص الإصلاح**: `validateOrder` كانت بتتنده
  وبترجع **قبل** السطور اللي بتقرا `override` أصلًا — يعني الدالة تبقى سليمة
  والاختيار يتبلع في صمت. الـ `override` بيتبعت للڤاليديشن دلوقتي.
  ⚠️ **والواجهة مش بتعيد حساب `validateOrder`**: الـ Worker بيرجّع علم
  `coverageOnly` (موقوف بسبب التغطية **وبس**) و`rowUploadable()` بتقرا منه.
  أي محاولة تخمّن ده من `problems` في الفرونت إند = نسخة تانية من الڤاليديشن
  بتفترق بصمت. متغطّى في `coverage-and-degree` ⑥د و⑧ · `row-state` ·
  `zone-picker-flow` ⑮.
- 🔴 **درجة الزون — السلّم `منطقة ← زون ← محافظة`.** (v2.1.0 · 8.5 درجة ٤ ·
  8.10) الأوردر اللي مالوش مطابقة منطقة وعنوانه طابق **زون واحد** بيترفع
  بـ`zoneId` بدل ما ينزل على طول للمحافظة. الفرق **مقيس بمقارنة بوليصتين** (نفس
  المحافظة · نفس اليوم · نفس الراسل، والفرق الوحيد الزون):
  `city` بس → `G-02 · OCTOBER HUB` · زون ٦ أكتوبر → `G-08 · NEW OCTOBER HUB`.
  «المحافظة بس» **مش** بلا فرز — بتاخد **هب افتراضي للمحافظة**، فالمقارنة هي
  «هب محدد ضد هب افتراضي»، والعنوان اللي زونه بعيد عن الافتراضي بياخد تحويلة.
  🔴 **شرطين إلزاميين قبل الرفع بالزون تلقائيًا:** زون **واحد بالظبط** (أكتر
  من واحد = غموض، والاختيار بينهم تخمين) · و**مفيش شك في المدينة** (الزون
  بيحسم المدينة، فرفعه فوق شك قايم بيثبّت المدينة المشكوك فيها بدل ما الموظف
  يراجعها).
  🔴 **والموظف يقدر يختارها بنفسه** — زرار «🗺️ ارفع على المدينة فقط» (v2.1.2)
  جنب «🏙️ ارفع على المحافظة فقط». الدرجة كانت **تلقائية بس**، يعني اللي شايف
  المطابقة مش مظبوطة و**عارف الزون** ماكانش قدامه غير المحافظة — فبينزل
  **درجتين بدل واحدة** (هب افتراضي بدل هب محدد).
  ⚠️ **والزون بيتبعت بالـ `zoneId` مش بالاسم** — الاسم بيتكرر بين المدن.
  🔴 **والـ Worker بيتحقق منه، مابيصدّقهوش** (`§BOSTA::resolveZoneOverride`):
  لازم يكون في **مدينة الرفع** ومن مناطق متاحة للتسليم، وإلا **وقف الصف** —
  نفس قاعدة المنطقة المختارة يدويًا بالظبط. الموظف ممكن يكون غيّر المدينة بعد
  ما اختار الزون، ورفع من غير تحقق = شحنة على زون مدينة تانية خالص.
  ⚠️ **والتثبيت على الزون (أو المحافظة) بيلغي المنطقة** — الـ payload بيبعت
  **درجة واحدة بس**، ولو الخانتين فضلوا مليانين الموظف بيقرا منطقة خضرا
  والشحنة بتترفع على الزون.
  🔴 **وزون من غير `zoneId` مايترفعش عليه** — الاسم للعرض، والـ id هو اللي
  بيترفع. الكتالوج كان بيرمي الـ id لحد v2.0.1، وده اللي كان معطّل الدرجة دي
  بالكامل. `?action=diag` فيه فحص صريح للحالة دي.
- 🔴 **العقد بيتغيّر مع الدرجة، والخلط بيرجّع 400.** `zoneId` على العقد الموثّق
  (`?apiVersion=1`) بيرجّع 400 `must contain at least one of [districtId,
  districtName]`، و`districtName` على غير الموثّق بيرجّع 400 `Zone Not Found`
  · `3002` **حتى لو الاسم زون صحيح ١٠٠٪**. مصدر الشكل الوحيد في الأداة هو
  `§BOSTA::buildAddressObject` — التلات أوضاع بيقروا منها.
- 🔴 **النزول درجة بيحصل لما اللي فوقه يترفض بس، ومصدره `errorCode`.**
  (`§BOSTA::nextAddressDegree`) منطقة + `3003` → زون لو فيه وإلا محافظة ·
  زون + `3002`/`3000` → محافظة. أي كود تاني **مابينزلش درجة** — النزول على
  `11000` مثلًا بيخفي السبب الحقيقي (مرفوع قبل كده) ويطلّع شحنة بعنوان أقل دقة.
  🔴 **والزون الاحتياطي بيتصفّر لو الموظف عدّل المدينة** — الزونات المحسوبة
  ساعتها بتاعة المدينة **الأصلية**، والنزول عليها بيرجّع الشحنة للمدينة اللي
  الموظف رفضها في صمت.

### ⚠️ الواجهة والوقت والسجل

- 🔴 **تلميحات نافذة اختيار المنطقة اتشالت — ما عدا تلاتة** (واجهة v2.8.0 ·
  قرار أحمد). الباقي هو **شرح الخانات التلاتة** (المحافظة · المدينة · المنطقة)
  للسبب المشروح في «تلات ألوان بتلات معاني» تحت. متغطّى في `zone-picker-flow`
  ⑬ **من الناحيتين**: التلاتة موجودين، ومفيش أي `title=` رجع على صفوف القايمة.
- 🔴 **بانر «خارج تغطية بوسطة» اتشال من النافذة — والمنع فضل** (واجهة v2.8.0).
  الحالة لسه باينة في مكانين: بادج «🚫 خارج التغطية» في عنوان النافذة، والمنطقة
  نفسها في القايمة بشريط أحمر مشطوبة ومش قابلة للضغط. ⚠️ **والخطر إن اللي يشيل
  الرسالة يشيل المنع معاها** — المنع عايش في الـ Worker
  (`§BOSTA::coverageProblems`) ومتغطّى في `coverage-and-degree` ⑥ب **و**
  `zone-picker-flow` ⑭.
- ⚠️ **شريط نوع الرفع بقى الأزرار التلاتة في النص وبس** (واجهة v2.8.0) —
  التسمية «نوع الرفع:» وسطر الشرح اتشالوا. 🔴 **و`justify-content:center` مش
  تجميل**: من غيره الأزرار بتتراص من أول الشريط وتسيب تلتينه فاضيين بعد ما
  الشرح اتشال.

- ⚠️ **تسميات «حالة العنوان» اتغيّرت** (واجهة v2.9.0): «بالمدينة» ← **«المدينة
  فقط»** · «بالمحافظة بس» ← **«المحافظة فقط»** · «محافظة مش في الجدول» ←
  **«محافظة خارج الجدول»**. المصدر واحد (`ADDR_MODE_ITEMS`) فالتلات شاشات
  بتتغيّر مع بعض. 🔴 **والقيم نفسها ما اتغيّرتش** (`zone` · `province` ·
  `blocked`) — الفلتر بيقارن بالقيمة، فأي مقارنة بالنص المعروض بترجّع صفر صف
  من غير أي خطأ.
- 🔴 **`zoneName` (محافظة اتلغت إداريًا — ٦ أكتوبر/حلوان) بقت بتتعرض زي `zone`
  بالظبط** (واجهة v2.13.0 · قرار أحمد 15-09-2026): بادجها المستقل «🗺️ محافظة
  تابعة — بتترفع كمدينة» اتشال، وبقت تاخد «🗺️ المدينة فقط» في البادج والفلتر
  والتصدير وعنوان نافذة اختيار المنطقة — عن طريق `rowAddrModeGroup()` اللي
  بتلف على `rowAddrMode()` وتدمج القيمتين للعرض بس. ⚠️ **الدمج بصري فقط** —
  التصنيف الحقيقي في `index.js` (`addressDegree()`) لسه بيسجّل `zoneName` في
  D1 كدرجة `district` كاملة، مش `zone`؛ وتلميح خانة «المنطقة» وخانة «المدينة»
  جوّه نافذة الاختيار (`addrModeInfo` · `dpPickCurrent('zone')`) لسه بيفرّقوا
  بينهم صراحة (`districtName` على العقد الموثّق، مش `zoneId`). لو حد فكّر
  يوحّد التصنيف الحقيقي كمان بعد كده، لازم يراجع القرار ده الأول.
- 🔴 **ونافذة اختيار المنطقة بقت بتفتح على مدينة ٦ أكتوبر/حلوان تلقائي** لصف
  `mode: 'zoneName'` (واجهة v2.13.0) — كانت بتفتح على «الكل» رغم إن الأداة
  عارفة المدينة أصلًا وقت الرفع (`row.zoneOnly` في الـ Worker)، فالموظف كان
  بيدوّر يدويًا على حاجة محسومة. الإصلاح في `loadDpCity`: نفس منطق قصر الزون
  التلقائي بتاع `mode: 'zone'`، بس بيقرا من `r.districtName` مش `r.zoneName`
  (الحقل ده فاضي دايمًا في وضع `zoneName`).
- 🔴 **التثبيت على المحافظة بياخد نفس أخضر التثبيت على المدينة** (واجهة
  v2.9.0). الاتنين قرار يدوي صريح من الموظف؛ رمادي على واحد منهم بيخلي الصف
  يتقرا كأن محدش لمسه فيعيد التثبيت. ⚠️ **والعلامة دي مش `set`**: `set` معناها
  «فيه مفتاح عنوان هيتبعت لبوسطة» والتثبيت على المحافظة مابيبعتش لا
  `districtId` ولا `zoneId`، فالعلم منفصل (`pinned`) و`districtCell` بتقرا
  الاتنين. خلطهم بيخلي خانة المنطقة في النافذة (اللي بتقرا `set`) تعرض
  «المحافظة · مثبّت» كأنها منطقة متحددة. متغطّى في `tests/upload-confirm.test.cjs` ⑥.
- 🔴 **نتيجة الرفع نافذة مستنية ضغطة — مش توست ومش جدول دايم.** (واجهة v2.2.0)
  التوست كان بيختفي لوحده بعد ٨ ثواني: بصّة بعيدة لحظة = الرقم الوحيد اللي بيقول
  إن فيه صف **فشل** أو **«تم جزئيًا»** بيروح، والجدول تحته بيفضل مفتوح على شاشة
  مليانة صفوف خضرا. 🔴 **والتفاصيل بتتفتح لوحدها في أي حالة مش نجاح**
  (`resultsNeedDetails`) — «تم جزئيًا» معناها شحنة **موجودة عند بوسطة بفلوس**،
  وإخفاؤها ورا ضغطة زيادة بيخلي الموظف يعيد الرفع ويطلّع شحنة تانية.
  ⚠️ **وصف `logged === false` بيتحسب حالة مش نجاح** — العملية تمت والسجل ناقص،
  وده بالظبط الصف اللي بيولّد إعادة رفع بعدين.
  ⚠️ **زرار «✕ إلغاء الشحنة» عايش جوّه التفاصيل دي**، وبيبان على صفوف R/E
  **الناجحة** كمان — فزرار «▾ عرض التفاصيل» لازم يفضل موجود حتى في النجاح
  الكامل، ومايتشالش كـ«تنضيف».
  🔴 **ونافذة الرفض (`cycleBlockOverlay`) أسبق منها.** فتح النتيجة فوقها بيغطّي
  سطر «✅ مفيش ولا شحنة اتعملت»، فالنتيجة بتتأجّل في `resultsPending` وبتتفتح من
  `closeCycleBlockModal`. أي مسار جديد يقفل نافذة الرفض لازم يعدّي من هناك.
- 🔴 **عدّادات نافذة النتيجة مربعات — الرقم فوق والاسم تحته** (واجهة v2.3.0)،
  ومصدرها **قايمة واحدة** (`RESULT_STATS`) الإجمالي جوّاها. كانت بادجات في سطر
  واحد بنفس حجم أي بادج تانية في الصفحة، فرقم **فشل** و**«تم جزئيًا»** كان
  بيتقرا كأنه وسم عادي — وهو الرقم الوحيد اللي بيقول إن فيه شحنة بفلوس محتاجة
  تدخّل. ⚠️ **والصفر بيفضل رمادي بدون كلاس `on`** — تلوين «فشل 0» بالأحمر بيخلي
  شاشة كلها نجاح تبان فيها فشل. ⚠️ وأي حالة تتضاف في `summarize` لازم يبقى ليها
  صف في `RESULT_STATS`، وإلا الإجمالي بيبقى أكبر من مجموع المربعات من غير أي
  خطأ (متغطّى في `tests/result-modal.test.cjs` ⑥).
- ⚠️ **زرار «▾ عرض التفاصيل» وزرار «⬇ تصدير النتيجة (Excel)» ليهم تصميم خاص**
  (`.res-ftr-btn`) مش `btn-ghost` الرمادي — **زرار «✕ إلغاء الشحنة» عايش جوّه
  التفاصيل**، فالزرار اللي بيفتحها مايصحّش يبقى الأبهت في النافذة.
- 🔴 **«حالة العنوان» اسم واحد لتلات شاشات** (واجهة v2.2.0): **الفلتر** ·
  **عمود الجدول** · **عنوان نافذة اختيار المنطقة** — والقيمة نفسها من
  `ADDR_MODE_LABEL` بالحرف. كان الفلتر «شكل العنوان» والعمود جنبه «نوع المطابقة»
  لنفس القيمة بالظبط، والموظف بيفتكرهم حاجتين. ⚠️ وأي حالة جديدة تتضاف في
  `ADDR_MODE_ITEMS` بتظهر في التلاتة تلقائيًا — **ماتكتبش القيمة بالإيد في أي
  واحدة فيهم**.
- 🔴 **صف بوسطة تلات قوايم منسدلة ببحث: المدينة · الزون · المنطقة**
  (واجهة v2.5.0). `<select>` العادي مالوش بحث، و**٢٠٧ منطقة في القليوبية
  لوحدها** (والجيزة ٣٣١) — يعني التمرير مش وسيلة اختيار. التلاتة **نفس
  المكوّن** بمصدر بيانات مختلف: تلات قوايم بشكل مختلف معناها إن الموظف يتعلّم
  تلات حركات لنفس الفعل، وإن أي إصلاح في واحدة مايوصلش للتانيتين.
  🔴 **والزون بيقصّر القايمة — مابيختارش منطقة.** الزون جواه مناطق كتير (زون
  العبور = ٦)، واختيار واحدة منهم تلقائيًا تخمين. ده نفس عقد `chooseZone`
  بتاع اقتراح المدينة التانية بالظبط — الاتنين بيكتبوا `dpZoneFilter` وبس.
  ⚠️ و«الكل» بترجّع `dpZoneFilter` لـ`null` **صريح**: كل الفحوص تحت
  `if (dpZoneFilter)`، والنص الفاضي بيعدّي كـ«مفيش قصر» بالصدفة بس.
  🔴 **وخانة الزون هي المكان الوحيد للمعلومة دي** (واجهة v2.6.0). كان فيه
  **تلاتة**: الخانة · بانر «الشحنة هتترفع على زون …» · وصف «اضغط عشان تشوف
  مناطقه بس». البانر والصف اتشالوا، **والقايمة تحت بقت تتقصر على الزون
  المطابق تلقائيًا** أول ما النافذة تفتح (في مدينة الصف بس — المدينة المعدّلة
  يدويًا زونها حاجة تانية). تكرار نفس المعلومة في تلات أماكن بيخلي الموظف
  يدوّر على الفرق بينهم.
  ⚠️ **واقتراحات المدن التانية (`crossCity`) فضلت** — دي حاجة مختلفة خالص:
  بتغيّر **المدينة**، ومش معروضة في أي مكان تاني.
  🔴 **وخانة الزون بتتحدّث من المنطقة المختارة** (واجهة v2.6.1). المنطقة جوّه
  زون **واحد بالظبط**، فأول ما منطقة تتحدد (تلقائيًا أو بالإيد) الزون بقى
  **محسوم** — عرض «الكل» جنبها كان بيخلّي التلات خانات تقرا كأن العنوان ناقص
  درجة وهو كامل. والترتيب: **المنطقة المختارة أسبق من كل حاجة** ← قصر الزون ←
  درجة الزون ← «الكل».
  ⚠️ **والزون المشتقّ بياخد 🟢 مش 🔵**: الـ payload بيبعت `districtId` بس
  وبوسطة بتحطّ الزون لوحدها منه — بس هو **جزء من العنوان اللي هيتسجّل على
  الشحنة فعلًا**، مش ترشيح عرض. و«بلا زون» بتتقال صريحة (حالة حقيقية في
  الكتالوج) بدل «الكل» اللي بيقرا كأن فيه اختيار لسه مطلوب.
  🔴 **ومصدر «المنطقة السارية» واحد** (`dpChosenDistrictId`) — خانة المنطقة
  **وخانة الزون** بيقروا منه. لما كان محسوب جوّه فرع المنطقة لوحده، خانة الزون
  ماكانتش شايفة المنطقة المختارة أصلًا.
- 🔴 **البحث بيلغي قصر الزون — القصر تصفّح والبحث وصول** (واجهة v2.6.1).
  البحث كان بيتطبّق **بعد** القصر، فالموظف يكتب اسم منطقة **موجودة فعلًا في
  المدينة** ويقرا «مفيش مناطق مطابقة» — طريق مسدود من غير أي خطأ. والفخ ده بقى
  أقرب بكتير من v2.6.0 لأن القصر بقى **بيتطبّق لوحده** على صف الزون أول ما
  النافذة تفتح، فالموظف ممكن مايكونش عارف إنه مقصور أصلًا.
  ⚠️ **والقصر مابيتمسحش** — بيرجع أول ما خانة البحث تفضى، والبانر بيقول
  صراحةً إن البحث خرج برّه القصر. سكوت هنا بيخلّي الموظف يفتكر إنه بيدوّر
  جوّه الزون. ونفس القاعدة بالظبط في **القايمة المنسدلة** بتاعة المنطقة.
  🔴 **واللوحة بعرض الصف كله، مش عرض الخانة** (واجهة v2.7.0). الحاوية
  الموضعية هي **الصف** (`.dp-src{position:relative}`) والخانة `static`،
  واللوحة `inset-inline:0`. ده بيحل مشكلتين بحاجة واحدة:
  ① أسماء المناطق **سطر واحد طويل** (`B1 Factories (10th of Ramadan) — B1
  مصانع (العاشر من رمضان)`) وعرض الخانة تُلت النافذة، فالاسم كان بيتلف على
  **تلات سطور** والقراءة تبقى تخمين.
  ② وخانة المنطقة (أقصى الشمال في RTL) لوحتها كانت بتمتد لبرّه حافة النافذة —
  و`.eco-modal-body` عليها `overflow-y:auto` (يعني الأفقي بيتحسب `auto` كمان)
  فكانت بتطلّع **شريط تمرير أفقي** والمحتوى يزحف.
  ⚠️ **لو الخانة رجعت `relative`، اللوحة بترجع لعرض الخانة من غير أي خطأ** —
  الأسماء تتلف والشريط الأفقي يرجع. متغطّى في `zone-picker-flow` ⑨.
  🔴 **والأعمدة التلاتة بتقابل بعضها عمود بعمود** — مش مجرد زيادة خانة:

  ```
  شوبيفاي   المحافظة  │  المدينة  │  (فاضية)
  بوسطة     المدينة   │  الزون    │  المنطقة
  ```

  أول عمودين **كل واحد فيهم مصدر القيمة اللي تحته**: المحافظة بتتحول لمدينة
  بوسطة من الجدول المقفول · وخانة `city` بتاعة شوبيفاي هي اللي العميل بيكتب
  فيها اسم الزون («العبور»). والترجيح نفسه بيرتّب الخانات كده (`city` تغلب
  `address1`).
  🔴 **والخانة التالتة في صف شوبيفاي فاضية عن قصد** (واجهة v2.5.1) — كان فيها
  نص العنوان وهو **مكرر حرفيًا** مع الصف الأخضر فوق. اتشال، **والخانة فضلت**
  لأن الصفين لازم يفضلوا نفس عدد الأعمدة، وإلا خانة بوسطة مابتقعش فوق خانة
  شوبيفاي اللي بتقابلها والمقارنة (اللي هي **كل** الشغل في النافذة دي) بتبقى
  غلط بصريًا. وفاضية **بلا `—`**: الشرطة بتقرا «القيمة مش موجودة»، وهنا مفيش
  قيمة أصلًا مفروض تكون موجودة. متغطّى في `tests/result-modal.test.cjs` ⑤.
- 🔴 **`overflow:hidden` ممنوع على `.dp-tbl`** (واجهة v2.5.1). كان موجود
  لتقويس الأركان، وبقى **بياكل نُص القايمة المنسدلة** أول ما القوايم اتحطّت
  جوّه الجدول — الموظف يفتح قايمة المنطقة ويشوف صفين ونُص. تقويس الأركان اتنقل
  للخانات الطرفية بخصائص منطقية (`border-start-start-radius` …) عشان يفضل صح
  في RTL. ⚠️ **باج بصري بحت**: مفيش خطأ في الكونسول ومفيش سلوك بيتغيّر، فأي
  «تنضيف» بيرجّع السطر ده بيخفي القوايم تاني **في صمت** — عشان كده فيه اختبار
  بيقرا الـ CSS نفسه (`tests/zone-picker-flow.test.cjs` ⑦).
- 🔴 **تلات ألوان بتلات معاني في القوايم — والخلط بينهم بيغلط في «إيه اللي
  هيتبعت»** (واجهة v2.5.1):

  | | المعنى |
  |---|---|
  | 🟢 `set` | قيمة **هتتبعت لبوسطة فعلًا** — منطقة متحددة، أو زون الشحنة هترفع عليه |
  | 🟠 `changed` | **تدخّل يدوي** غيّر اللي كان هيتبعت — المدينة اتبدّلت |
  | 🔵 `filter` | **ترشيح عرض بس** — قصر القايمة على زون، مابيتبعتش ومابيغيّر حاجة |

  الزون كان بياخد 🟠 لما يتقصر عليه، والموظف يفتكر إنه عدّل الشحنة — وهو أصلًا
  بيدوّر بس. ⚠️ **واللون لوحده مابيكفيش**: كل خانة عليها شرح على الـ hover
  بيقول إيه اللي هيتبعت بالظبط. متغطّى في `zone-picker-flow` ⑥.
  🔴 **والشروح التلاتة دي هي الوحيدة الباقية في النافذة** (v2.8.0) — باقي
  التلميحات اتشالت بقرار أحمد، ودول فضلوا **بالظبط** للسبب اللي فوق. شيلهم
  معناه إن 🟢 و🔵 يبقوا لونين على نفس النص بلا أي تفرقة.
- 🔴 **سياق نافذة اختيار المنطقة جدول واحد بأعمدة ثابتة** (واجهة v2.2.0):
  **العنوان بالكامل** (أخضر، `fullAddressText` — نفس نص عمود الجدول) فوق، وتحته
  صف شوبيفاي وصف بوسطة **من غير فاصل**. عرض عمود التسمية **مكتوب رقم ثابت
  (128px) في التلات صفوف عن قصد**: كل صف `grid` مستقل، فـ`auto` بيتحسب لكل صف
  لوحده والأعمدة بتزحف — والمقارنة بين خانة شوبيفاي وخانة بوسطة اللي فوق بعض هي
  **كل** الشغل في النافذة دي.
  🔴 **والقوايم التلاتة جوّه صف بوسطة — أي إعادة رسم للسياق بتمسحهم.**
  `renderDistrictState` بتعيد كتابة `#dpContext` بالكامل مع كل اختيار، فلازم
  `renderDpControls()` بعدها على طول. من غيرها القوايم بتفضل **فاضية من غير أي
  خطأ** بعد أول اختيار، والموظف يفتكر إن مفيش بدايل. (الفخ ده كان على قايمة
  المدن لوحدها لحد واجهة v2.4.0 — من v2.5.0 بقى على التلاتة.)
  ⚠️ **وحالة القوايم (`dpPickOpen` · `dpPickSearch`) عايشة برّه الـ DOM** عن
  قصد لنفس السبب — حالة محفوظة جوّه `#dpContext` كانت هتتمسح بصمت مع كل رسمة.
- 🔴 **أعمدة الجدول بتتبني من `tableColumns()` — مش مكتوبة في الـ HTML.**
  الترويسة والجسم و`colspan` بتاع الصف الفاضي كلهم بيقروا من نفس المصدر. الأعمدة
  بتختلف بالوضع، وترويسة ثابتة مع جسم متغيّر معناها أعمدة بتزحف على بعضها **في
  صمت**. (نفس الباج كان كامن في جدول تفاصيل النتيجة: `colspan="8"` مكتوب رقم،
  وعمود «إجراء» خلّاه 9 — العدد بيتقرا من الترويسة نفسها دلوقتي.)
- 🔴 **المبلغ السالب بيتعرض «↩ استرداد» مش رقم.** ده الاتجاه اللي `Math.abs`
  بيضيّعه، بس في العرض بدل الـ payload — الموظف لازم يشوف الاتجاه قبل ما يضغط.
  ⚠️ **ولونه الأحمر جزء من الإشارة.** كلاسات R/E اللي اتنقلت وقت الدمج كانت
  بتقرا متغيّرات لون **مش معرّفة في الصفحة دي** (`--danger` · `--success`)،
  فـ«↩ استرداد» وبادج الدورة الموقوفة وسطر «✅ مفيش ولا شحنة اتعملت» كانوا
  بيتعرضوا **بلون عادي** من غير أي خطأ. اتصلحوا في واجهة v2.1.0 على لوحة
  الصفحة (`--red` · `--green`). ⚠️ أي كلاس جديد يتنقل من أداة تانية: اتأكد إن
  المتغيّر موجود في `:root` هنا — الـ CSS مابيشتكيش.
- 🔴 **عمود «الدورة المفتوحة» سطرين: بادج الحالة فوق، واسم الدورة وعددها تحت.**
  (واجهة v2.1.0) الاسم لوحده كان بيخلي أوردر عدّى بتلات دورات شكله زي اللي ليه
  دورة واحدة — والصف كله متحسب من الدورة المفتوحة بس، فالعدد هو الإشارة الوحيدة
  على إن ورا الأوردر تاريخ تاني. وأكواد التوقيف بتتعرض **بالعربي**
  (`CYCLE_CODE_LABEL`) والكود الخام بيفضل في الـ tooltip.
  🔴 **والترجمة دي مصدر واحد لتلات شاشات** (واجهة v2.1.1): عمود الدورة ·
  **نافذة الرفض** (`openCycleBlockModal`) · **سبب الصف في تفاصيل نافذة النتيجة**
  (وتصديره). مصدرين معناهم إن الموظف يقرا «أكتر من دورة مفتوحة» في الجدول
  و`CYCLE_OVERLAP_OPEN` في النافذة لنفس الصف، فيفتكرهم مشكلتين. ⚠️ وأي كود
  جديد يتضاف في الـ Worker لازم يتضاف في `CYCLE_CODE_LABEL` — الكود اللي مش
  في القايمة بيتعرض زي ما هو (مقصود: أحسن من «—» اللي بيخفي السبب).
- ⚠️ **عمود «القطع» اتشال من الجدول** (واجهة v2.1.0) — وصف القطع الراجعة
  والخارجة **لسه بيتبعت لبوسطة زي ما هو** (هو اللي المندوب بيقراه) ولسه في
  تصدير XLSX وفي ملف بوسطة. العرض في الجدول بس هو اللي اتشال.
- 🔴 **`apiRequest` بيرفق جسم الرد بالخطأ (`err.data`).** الـ `409 CYCLE_BLOCKED`
  جاي معاه قايمة الأوردرات الموقوفة وسبب كل واحد؛ رمي الرسالة لوحدها كان بيرمي
  ده كله ويسيب الموظف مع «فشل» من غير ما يعرف يصلّح إيه.
- 🔴 **مودال الرفض بيقول بالأخضر «مفيش ولا شحنة اتعملت».** من غير السطر ده
  الموظف بيروح داشبورد بوسطة يدوّر على حاجة يلغيها.
- 🔴 **`wirePhone` قبل الإرسال — مش `normPhone`.** الأخيرة مفتاح **مقارنة** بس
  (بتشيل الصفر و20). المتجر فيه **تلات** أشكال تليفون مقيسة: `01…` · `+201…` ·
  و**`+20 12 71043044` بمسافات** (`#53849`).
- 🔴 **الوقت: إزاحة القاهرة بتتحسب، مابتتكتبش ثابت.** مصر بتوقف التوقيت الصيفي
  **29-10-2026**، ومن ساعتها كل وقت في الأداة كان هيغلط بساعة **من غير أي رسالة**
  وأي فلتر تاريخ هيزحف. الدوال حرفية من `ecommoda-constants` §13 (`Intl` بـ
  `Africa/Cairo`). 🔴 **ومعاها حارس إلزامي:** `Intl.formatToParts` **بترمي**
  `RangeError` على تاريخ غير صالح — وصف واحد بتاريخ ناقص كان بيرمي جوّه
  `applyFilters()` اللي بتتنده من دالة `async`، فالاستثناء بيتبلع في promise
  محدش بيقراها **والشاشة بتقف صامتة**.
- 🔴 **فلتر تاريخ السجل بيتحوّل لحدود UTC بتاعة يوم القاهرة.** الصفوف متخزّنة
  UTC والموظف بيفكّر بتوقيت القاهرة — `substr(timestamp,1,10)` كانت بتشيل رفع
  الساعة ١:٣٠ بالقاهرة من فلتر «اليوم»، **والصف ده هو الأثر الوحيد على إن
  الشحنة اتعملت**.
- 🔴 **ترتيب السجل server-side، مش على الصفحة المحمّلة.** ومفاتيح الواجهة لازم
  تطابق `LOG_SORT_COLUMNS` في الـ Worker **حرفيًا** — مفتاح بره القايمة المقفولة
  بيرجع للافتراضي **في صمت**، فالعمود يبان مرتّب وهو مش مرتّب. و`ORDER BY` على
  عمود غير `timestamp` معاه **كاسر تعادل** إلزامي، من غيره الصف الواحد ممكن يظهر
  في صفحتين أو مايظهرش خالص.
- 🔴 **قيم فلتر نوع العملية في الواجهة لازم تطابق اللي الـ Worker بيكتبه
  حرفيًا** — قيمة مكتوبة غلط بتدّي فلتر بيرجّع **صفر صف من غير أي خطأ**. متغطّى
  في `tests/job-modes.test.cjs` ④.
- ⚠️ **«رفع الكل» بيشتغل على `visibleRows` (الفلتر الحالي) مش على `allRows`.**
  الفرق ده هو كل الأمان في الزرار: الموظف بيفلتر، بيشوف العدد جوّه الزرار،
  وبيأكد.
- 🔴 **نافذة تأكيد واحدة للزرارين — «رفع الكل» و«رفع المحدد»** (واجهة v2.9.0).
  الاتنين بيعملوا **نفس الحاجة بالظبط** (شحنات بفلوس على بوسطة) والفرق بينهم
  **مصدر الصفوف** مش خطورة العملية: `bulkConfirmRows('all')` = المعروض
  الصالح، `('sel')` = المحدد الصالح. وقفة على واحد من غير التاني كانت بتعلّم
  الموظف إن الرفع من «المحدد» مالوش مرحلة مراجعة.
  🔴 **والأرقام لازم تبقى الصفوف اللي هتترفع فعلًا** — نفس الفلترة اللي
  `startUpload` بيعملها بعدها. رقم أكبر معناه الموظف بيأكّد دفعة متخيّلة،
  ورقم أقل معناه شحنات بفلوس مخفية عن التأكيد.
  🔴 **والجسم مربعات «حالة العنوان» من `ADDR_MODE_ITEMS` نفسها** — الرقم اللي
  بيفرق قبل الضغط هو «كام أوردر بعنوان مظبوط وكام على المحافظة فقط»، مش
  إجمالي مجرد: الدرجة الأقل دقة بتاخد **هب افتراضي** وبتدخل مسار العناوين غير
  الواضحة. ⚠️ وأي حالة برّه القايمة بتتعرض **بمفتاحها الخام** في الآخر — من
  غير الذيل ده الإجمالي بيبقى أكبر من مجموع المربعات من غير أي خطأ (نفس فخ
  `RESULT_STATS`). متغطّى في `tests/upload-confirm.test.cjs` ②.
  🔴 **ونوع الشحنة اتنقل لترويسة النافذة** — الجسم بقى أرقام بس والأداة تلات
  أوضاع، فمن غيره الموظف ممكن يأكّد «استرجاع» وهو فاكر إنه «شحن».
  ⚠️ **و`confirmBulkUpload` بيقف لو `bulkConfirmSource` فاضي** — من غير الحارس
  ده أي نداء والنافذة مقفولة بيرفع التحديد الحالي بلا وقفة.
- ⚠️ **تبديل الوضع بيمسح التحديد والتعديلات اليدوية بالكامل.** تعديل منطقة اتعمل
  على صف شحن مالوش أي معنى على صف استرجاع لنفس الأوردر.
- ⚠️ **`diag` و`get_config` بيعدّوا لما `WORKER_SECRET` يكون غايب** — دول مسار
  التشخيص، ومن غير الاستثناء ده رسالة «شغّل ?action=diag» بتبقى بتحيل على حاجة
  محجوبة وفحص السر جوّه `diag` بيبقى كود ميت.
- ⚠️ **السر الناقص بيشيل الحماية مش بيشدّدها.** لو `WORKER_SECRET` غايب، القالب
  بينتج `"Bearer undefined"` وأي طلب بالهيدر ده **بيعدّي**.
- ⚠️ **غياب `read_all_orders` بيرجّع صفر نتيجة مش خطأ صلاحية.** الأداة بتفلتر من
  `START_DATE`، فأول ما التاريخ ده يعدّي الـ ٦٠ يوم الأوردرات القديمة بتختفي من
  القايمة في صمت. `?action=diag` هو الإشارة الوحيدة.
- 🟡 **بانر أحمر «فلتر الحالة مش متسق»** معناه `count(Confirmed)` +
  `count(Confirmed + Edit)` مش بيساوي العدّ المدمج. الأداة بتفضل شغّالة على
  الاستعلام المدمج، بس ده معناه إن فيه أوردرات ممكن تكون مش ظاهرة — بلّغ فورًا.
  (الحارس ده في وضع الشحن بس: فلتر S2 قيمة واحدة فمفيش مجموع يتفحص.)
- ⚠️ **الأداة متلمسش `custom.manual_status` ولا `custom.zone`.** الانتقال
  `Confirmed → Ready` بيحصل عند **الطباعة** مش عند الرفع — تقديم الحالة من هنا
  بيكسر بوابة الطباعة. و`custom.zone` قرار بشري عن توفّر المندوب، مش استنتاج.
- ⚠️ **`In-Return` بتتكتب يدوي مش من مزامنة بوسطة تلقائية.** استثناء مقصود اتأكد
  مع أحمد 06-08-2026 — الأصل حسب `ecommoda-order-lifecycle` إن `In-Return`
  «بوسطة بس». لسه مفيش auto-sync حي، فالتأكيد اليدوي هو المُحفّز الوحيد.
  يتراجع لو/لما الـ sync ده يتعمل.
- ⚠️ **`checklist` في `confirm_upload` audit-only** — مش بتمنع الكتابة
  سيرفر-سايد؛ البوابة الحقيقية مودال الواجهة. (⚠️ ده **مابينطبقش** على حارس
  الدورات — ده بيتفحص سيرفر-سايد فعليًا وبيرفض قبل الكتابة.)

## خط الأساس بعد الدمج

```
الشحن العادي: شغّال حيًا — 356 شحنة مرفوعة لحد 13-09-2026.
الاسترجاع/الاستبدال: 🔴 **لسه ما اشتغلش حي ولا مرة** (صفر صف upload_re_* في D1).
المخزن كان لسه على الإكسيل — 80 صف export_exchange، آخرهم 13-09.

📌 ملحوظة تستاهل الانتباه: export_exchange = 80 مقابل confirm_exchange = 39.
يعني **نص التصديرات مش بيتأكّد** — الشحنة بتروح لبوسطة والحالة بتفضل
Confirmed + EXCHANGE. الدمج بيقفل ده هيكليًا في مسار الرفع المباشر
(الشحنة والحالة في نداء واحد)، بس **مسار الإكسيل لسه عنده نفس الفجوة**.
أول أسبوع: قيس الفرق بين upload_re_* و confirm_* — لو الفجوة قفلت، ده دليل
إن المخزن اتحوّل فعلًا على الرفع المباشر.

أول تشغيل: سجّل هنا عدد الأوردرات المؤهَّلة لكل وضع وقت الفتح، ونسبة الصفوف
اللي مطابقة المنطقة نجحت فيها (mode = district)، و**عدد صفوف 🟠 المدينة مشكوك
فيها** (اقراه من `city_overridden` في D1).
مرجع وقت كتابة SPEC: 19 أوردر شحن مؤهَّل (06-09-2026) · 34 صف (07-09-2026).
```

## 🔴 تشغيل مراقَب — إلزامي قبل التوسّع

✅ **أول شحنة استبدال حية اتعملت 13-09-2026 ونجحت** — الشحنة اترفعت مظبوطة على
بوسطة. وطلّعت باج واحد اتصلح في v2.0.1 (الرفع كان بيقدّم الحالة ويكتب وقت طباعة).
🔴 **و v2.3.0 رجّعت نقلة الحالة للاسترجاع لوحده بطلب أحمد** — يعني أول شحنة
استرجاع حية بتتراجع عليها **حاجتين** دلوقتي: الشحنة نفسها، والنقلة على شوبيفاي.
🔴 **باقي:** شحنة **استرجاع** واحدة (اتجاه العنوان فيها مقلوب — `pickupAddress` —
وده الجزء اللي لسه ما اتجربش حي)، وزرار الإلغاء.

```
① ?action=diag → اتأكد إن bosta.keySet سليم وإن أنواع الميتافيلدات الخمسة متطابقة
   🔴 الميتافيلدين الجداد (_s1 · _s2) اتعملوا على المتجر — الفحص ده بيأكد النوع
② وضع الاسترجاع: **صف واحد**، يترفع، ويتراجع على داشبورد بوسطة:
   · العنوان راح pickupAddress ولا لأ
   · المبلغ بإشارته الصح (استرداد يبان استرداد)
   · businessReference = #12345 و uniqueBusinessReference = #12345-R{n}
   · custom.bosta_tracking_number_s2 اتكتب · تاج Bosta_Uploaded_S2
   · 🔴 custom.status_2_r_e بقى **In-Return** (v2.3.0) · custom.printing_time_s2
     **لسه فاضي** — مفيش بوليصة استرجاع بتتطبع أصلًا
   · custom.courier ما اتغيّرش
   · 🔴 والأوردر **بيخرج من القايمة** بعد التحديث — ده صح من v2.3.0، مش باج.
     (في الاستبدال والشحن العادي بيفضل ومعاه بادج «🔁 مرفوع».)
   · وصف `metafields_change` واحد اتكتب في السجل جنب صف `upload_re_return`
③ زرار «✕ إلغاء الشحنة» على نفس الصف — في **تفاصيل نافذة النتيجة** (اضغط
   «▾ عرض التفاصيل» — الصف الناجح مش بيفتحها لوحده). يتأكد إن الشحنة اختفت
   وإن إعادة الرفع بتعدّي
④ نفس الحاجة لوضع الاستبدال (صف واحد) — **بس هنا `status_2_r_e` لازم يفضل
   `Confirmed + EXCHANGE`**، والصف يفضل في القايمة. أي تحرك في الاستبدال = باج
⑤ 🆕 (v2.1.0) **درجة الزون** — صف مالوش مطابقة منطقة وعنوانه فيه اسم زون:
   · بادج «حالة العنوان» بيقول 🗺️ **بالزون** (مش 🏙️ المحافظة بس)
   · بعد الرفع: قراية راجعة بـ `POST /deliveries/search` على رقم التتبع —
     🔴 **رد الإنشاء مابيعرضش الزون خالص** (بيرجّع `district: "N/A"`)، فالحكم
     من الرد **غلط**. التأكيد إن `dropOffAddress.zone` راجع فيه اسم الزون.
   · و`GET /deliveries/business/{tn}` بيقول `assignedHub` — قارنه بصف محافظة
     في نفس المحافظة ونفس اليوم: المفروض هب مختلف (`bosta-api-helper` 8.10.2)
⑥ 🆕 (v2.1.0) **المنطقة المقفولة** — افتح نافذة الاختيار على مدينة فيها
   مقفولة (جنوب سيناء: Taba · Saint Catherine) واتأكد إنها **باينة** بشريط
   أحمر ومش قابلة للضغط. ولو لقيت أوردر عنوانه طابق واحدة منها، لازم يبان
   🚫 **برّه تغطية بوسطة** و**الرفع موقوف**
⑦ بعدها بس: التوسّع لدفعات
```

## التصفية — `Bosta-Return-Exchange-Exporter`

🔴 **متمسحش حاجة قبل ما الجديدة تشتغل حي وتستقر** (MERGE-BRIEF §٨).

```
① ✅ الدمج اتعمل — 13-09-2026 (Worker v2.0.0 · واجهة v2.0.0)
② ⬜ تشغيل مراقَب حسب القايمة فوق          ← التالي
③ ⬜ أسبوع تشغيل فعلي بالتوازي، والقديمة زي ما هي
④ ⬜ صفحة الـ Exporter تتحول لصفحة تحويل على الجديدة — **متتمسحش**
      🔴 الموظفين حافظين الرابط. مسحه = صفحة ميتة بلا رسالة
⑤ ⬜ الـ Worker القديم يتساب شغّال **شهر** (رخيص) — أي نداء متأخر يلاقي رد
⑥ ⬜ بعد الشهر: الـ Worker يتشال، والريبو يتأرشف (**مش يتمسح** — فيه تاريخ ٤ شهور)
```

⚠️ **ومتنساش:** endpoints الـ Exporter مش هيبقى ليها مستهلك — بس **صفوف D1
بتاعتها بتفضل**، والأداة دي بتقراها. أي تغيير في قيمة `tool` لازم يمر بمهارة
`ecommoda-tool-rename`.

## استرجاع النسخ القديمة

> ده بديل الـ tags — دفع الـ tags ممنوع من جلسات Claude Code السحابية.

```
النسخة الأولى (v1.0.0) على فرع claude/build-tool-from-file-94scbm
آخر نسخة قبل الدمج (v1.4.0 / واجهة v1.6.0): commit 6e748f6
git show <sha>:index.js
```

## بصمة المهارات

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-worker-builder | v3.3.0 |
| ecommoda-html-builder | v7.1.0 |
| ecommoda-constants | v2.5.0 |
| bosta-api-helper | v5.0.0 |
| shopify-graphql-helper | v2.2.0 |
| ecommoda-order-lifecycle | v1.6.0 |

آخر مطابقة: 15-09-2026 · `index.js` v2.3.0 · `index.html` v2.13.0
> ⚠️ **v2.3.0 (نقلة حالة الاسترجاع وقت الرفع) اتعملت من غير مراجعة مهارات
> جديدة** — البند المتأثر هو `ecommoda-order-lifecycle` Rule 15، وهو مسجّل
> كبند مفتوح (١٨) مش كامتثال متحقَّق.

> ⚠️ **البصمة دي بتقول «اتحاذت عند الإصدارات دي»، مش «مراجعة امتثال كاملة لكل
> بند»** — وبعد الدمج هي **أضعف** من كده: الكود الجديد اتنقل من أداة كانت
> بصمتها متأخرة عن دي، واتحاذ يدويًا على البنود اللي لمسها بس
> (`worker-builder` ④ ⑩ ⑪ ⑬ ⑭ · `order-lifecycle` Rules 8/13/14/15 ·
> `bosta-api-helper` Step 8/8b/8.3 · `html-builder` Step 3C).
> 🔴 **محتاج `skills-sweep` كامل على الأداة المدموجة** — بند مفتوح.
>
> ✅ **بصمة `bosta-api-helper` v5.0.0 (14-09-2026) — مراجعة كاملة بندًا بندًا**
> على الخمس إصدارات (v2.0.0 → v5.0.0). اتطبّق: حارس `goodsInfo` (8.4) ·
> التغطية المقفولة بطبقتين (8.11) · درجة الزون وسلّم النزول (8.5 · 8.10 ·
> 8.10.4) · درجة العنوان في D1 · أكواد `41591`/`3007`/`1066`/`3000`/`3009`
> وفرع الـ403 · الإلغاء المكرر (8.7) · صيغة التأمين `clamp(1%,10,50)` (8.9) ·
> حقول الكتالوج الجديدة (8.6).
> **ومتحقَّق إنه مش منطبق:** Step 8c (البوليصة) · Step 8d (`PUT`) ·
> Step 1a (حساب واحد) · فخ `businessReferences` بالجمع وفلترة `type.code`
> (الأداة **مابتنادي `deliveries/search` ولا `GET` ولا `PUT` ولا `mass-awb`
> ولا مرة** — مسار القراءة الوحيد عندها هو الكتالوج).
> ✅ ومتحقَّق إنه **كان مطبّق أصلًا**: مخرج محرك المطابقة بيطلّع `districtId`
> (مطلب v5.0.0 🔴) — الناقص كان `zoneId` بس، واتضاف.

🔴 معلّقة: تسجيل قيم `type` و`tool` بتاعة R/E في `ecommoda-constants` §7

## مسائل مفتوحة

1. 🔴 **تسجيل قيم R/E في `ecommoda-constants` §7.** `upload_re_return` ·
   `upload_re_exchange` · `re_upload_failed` · `re_shopify_write_failed` ·
   `re_cancelled` · `cycle_block` — **ومعاهم استثناء «أداة واحدة، قيمتين
   `tool`»**. القاعدة بتقول التسجيل قبل أول `writeLog`؛ ده استثناء واعي متوارث.
2. 🔴 **أنهي أدوات تانية في الستاك بتقرا `custom.bosta_tracking_number`؟**
   الأداة دي وقفت الكتابة عليه وبقت تكتب `_s1`/`_s2`. المرشحين للفحص:
   `bosta-order-lookup-worker` · `order-printer-worker` · `mass-awb` · أي داشبورد
   أو Power Query. **الحقل القديم لسه فيه بيانات ٣٥٦ شحنة وما اتمسحش**، فأي أداة
   بتقراه هتفضل شغّالة على التاريخ — بس مش هتشوف أي شحنة جديدة. ⚠️ **ولسه محتاج
   قرار: نعمل backfill من القديم لـ`_s1` ولا لأ؟**
3. 🔴 **`upload_re` ما اشتغلش حي ولا مرة** — شوف «تشغيل مراقَب» فوق. حاجز على
   التوسّع، مش على الدمج.
4. 🟠 **حالتين شك كاذب في كاشف المدينة** اتقاسوا على عناوين مرتجعات حقيقية
   (10-09-2026): `#53075` (الدقهلية + «الجماليه») و`#52888` (الإسكندرية +
   «Green Plaza»). الاتنين بيولّعوا 🟠 على صفوف سليمة — مش كاسر (الموظف بيراجع
   ويحدد) بس بيزوّد الشغل اليدوي. ✅ **والدمج شال نص البند ده**: مابقاش فيه
   «يتعمل في الريبوين مع بعض» — محرك واحد واختبار واحد.
5. ✅ **اتقفل 14-09-2026 — `cod` السالب في الشحن العادي.** أحمد أكّد إن
   **السالب مستحيل** في الشحن العادي و**`0` وارد**. يعني `Math.max(0, …)`
   (v2.1.0) هو الصح نهائيًا: لو قيمة سالبة ظهرت فهي **بيانات غلط**، والحارس
   بيمنعها من تتحوّل لتحصيل. و`cod: 0` عدّى عند بوسطة فمحتاجش معالجة خاصة.
   ⚠️ بوسطة نفسها **بتقبل السالب على `type 10`** — فالسطر ده هو الحارس الوحيد.
6. 🟡 **قرار §٥ بتاع `MERGE-BRIEF` (قيمة `tool`) مؤجَّل بقرار أحمد** لحد ما
   التجربة تكتمل. التنفيذ الحالي اختيار «أ» وهو **قابل للرجوع بالكامل**: صفر
   هجرة، وتاب السجل بيقرا الاتنين. القرار النهائي بعد أسبوع التشغيل.
7. 🟡 **`skills-sweep` كامل على الأداة المدموجة** — راجع الملحوظة تحت البصمة.
8. 🟡 **الـ Worker `bosta-webhook` على حساب Cloudflare المهجور `ecommoda24`** —
   موجود ولسه شغّال ولا لأ؟ (SPEC §٩.١). **مش حاجز** لأن الأداة مبتبعتش
   `webhookUrl` ومبتستقبلش ويبهوك، بس الشحنات اللي بترفعها هتبعت تحديثات حالتها
   للرابط ده تلقائيًا. مقيس 10-09-2026: `bosta_order_type` و`bosta_last_sync_time`
   **فاضيين على آخر ١٠ أوردرات `Shipped`** — قرينة قوية إنه مش بيكتب، مش إثبات.
9. 🟡 **حدود استهلاك API بوسطة غير معروفة** — التوازي حاليًا `UPLOAD_CONC = 3`
   (متحفّظ). يتظبط بعد قياس حي، مش بالتخمين. ⚠️ والدمج **زوّد** الحمل المحتمل:
   تلات أوضاع ممكن تترفع من نفس اليوم.
10. 🟡 **ب-١٢ — طباعة البوليصة بعد الرفع بالـ API.** الموظف كان بيطبع من داشبورد
   بوسطة بعد رفع الملف؛ دلوقتي الشحنة بتتعمل من غير ما حد يفتح الداشبورد. إما
   يفضل يطبع من هناك، أو `mass-awb` (أداة تانية — `bosta-awb-api.md`).
11. 🟡 **ب-٥ — جرد الشحنتين على الستاك.** الأوردر بقى ليه شحنتين تحت نفس
   `businessReference`. `bosta-order-lookup-worker` و`order-printer-worker`
   محتاجين مراجعة: بياخدوا `[0]`؟ الفلترة على `type.code` مش النص.
12. 🟠 **أنهي مدن تانية عندها نفس فرق العبور؟** المرشحين للفحص على الكتالوج
   الحي: الشروق · بدر · العاشر من رمضان · العاصمة الإدارية · الشيخ زايد.
   دلوقتي الكاشف بيلقطهم كاقتراح، بس المتكرر منهم يستاهل يبقى صف في الجدول.
   القياس من `city_overridden` في D1 بعد أول أسبوع تشغيل.
13. 🟡 **مطابقة الزون محتاجة قياس حي.** اتفعّلت في v1.3.0 وحلّت العبور،
   و**v2.1.0 حوّلتها لدرجة رفع فعلية** (`zoneId`) مش اقتراح واجهة بس. الباقي:
   نتابع كام صف بيطلّع اقتراح زون وكام منهم الموظف بيقبله. لو نسبة القبول عالية
   جدًا على مطابقة **تطابق كامل في خانة `city`**، نبص وقتها في تطبيق تلقائي
   للحالة دي بس — ومحتاج قرار صريح لأنه بيعدّل قرار «اقتراح مش قرار».
14. 🟡 **`CATALOG_KV` مش متعمّل.** الكاش شغّال على Cache API — كافي وشغّال من
   غير أي إعداد. لو اتعمل namespace، فك التعليق في `wrangler.toml` وحطّ الـ id
   والكود بيستخدمه تلقائيًا.
15. 🟡 **شكل رد `GET /cities/getAllDistricts` مش موثّق بدقة** — الكود بيطبّع أكتر
   من شكل محتمل، والفحص الذاتي بيعرض عدد المدن والمناطق المتاحة. لو العدد طلع
   صفر، ده معناه إن شكل الرد اتغيّر مش إن مفيش مناطق.
16. 🔴 **`skillsupdates20260910.md`** (في ريبو الـ Exporter) — تصحيحات مهارات لسه
   ما اتطبقتش، منها ٦ بنود 🔴. الملف مكتفي بذاته وفيه Checklist تطبيق.
   ⚠️ **يتنقل هنا أو يتطبّق قبل ما ريبو الـ Exporter يتأرشف** (خطوة ⑥ في التصفية).
17. ✅ **اتقفل 15-09-2026 — «بوابة الطباعة» مالهاش محل في الاسترجاع.** السؤال
   كان: الرفع بقى بينقل لـ`In-Return` **قبل** الطباعة، فهل ده بيكسر بوابة
   طباعة؟ الإجابة من أحمد: **مابنطبعش بوليصة لأوردر استرجاع أصلًا** — بوسطة
   بتروح تجيب المرتجع من عند العميل على طول. فمفيش خطوة طباعة بعد الرفع تنقل
   الحالة، والرفع هو **آخر خطوة عندنا**. ⚠️ **الباقي منه بند ضيق:** أي تقرير
   أو داشبورد بيعدّ «مرتجعات مستنية شحن» بـ`status_2_r_e = Confirmed + RETURN`
   هيشوف العدد بيقلّ بعد الرفع — مش عطل، بس **معنى العدّاد اتغيّر** (بقى
   «مستنّي رفع» بدل «مستنّي وصول بوسطة»). يتراجع على: أي Power Query أو
   داشبورد بيعدّ S2. (`order-printer-worker` و`mass-awb` **مش متأثرين** — مفيش
   بوليصة استرجاع بتتطبع منهم.)
18. 🔴 **تسجيل استثناء v2.3.0 في `ecommoda-order-lifecycle`** — الوثيقة بتقول
   إن نقلة S2 بتحصل عند الطباعة، والاسترجاع بقى استثناء **بسبب مادي**: مفيش
   بوليصة بتتطبع له، فمفيش حدث طباعة يحمل النقلة. قرار أحمد 15-09-2026،
   ومحتاج يتكتب هناك زي ما `In-Return` اليدوية اتكتبت.

آخر تحديث: 15-09-2026 (Worker v2.3.0 · واجهة v2.13.0)
</div>
