<div dir="rtl" style="text-align: right;">

# تكليف: دمج رفع الاسترجاع/الاستبدال في `Bosta-Orders-Upload` وتصفية الأداة التانية

![status](https://img.shields.io/badge/الحالة-جاهز_للتنفيذ-green)

> **الملف ده مكتفي بذاته.** الجلسة اللي هتنفّذه مش محتاجة تقرا أي جلسة قبلها.
> كل رقم هنا **مقيس** — من D1 أو من الكود نفسه، 13-09-2026.
>
> **الريبوهات المطلوبة في الجلسة:** `ecommoda-dev/Bosta-Orders-Upload` ·
> `ecommoda-dev/Bosta-Return-Exchange-Exporter`

---

## ١ · الطلب في سطرين

كل عمليات الرفع على بوسطة تبقى في أداة **واحدة** — `Bosta-Orders-Upload` —
تشحن الأوردرات العادية **والاسترجاع والاستبدال**. وبعد ما تشتغل وتستقر،
`Bosta-Return-Exchange-Exporter` **تتصفّى بالكامل** (كانت أداة تصدير Excel،
وبقت زيادة).

🔴 **مش دمج ملفين.** الجزء التقني أسهل حاجة فيه — المخاطرة الحقيقية في
**سجل D1** و**تصفية الأداة القديمة**، والاتنين موصوفين تحت.

---

## ٢ · الحالة الحالية — الاتنين شغّالين حيًا

| | `Bosta-Orders-Upload` | `Bosta-Return-Exchange-Exporter` |
|---|---|---|
| النسخ | Worker `v1.4.0` · واجهة `v1.6.0` | Worker `v6.0.0` · واجهة `v6.0.0` |
| الحجم | `index.js` 1,861 · `index.html` 3,742 | `index.js` 3,243 · `index.html` 3,917 |
| بتعمل إيه | شحنة عادية (`type 10`) | استرجاع (`25`) واستبدال (`30`) **+ تصدير Excel** |
| `tool` في D1 | `bosta_orders_upload` — **390 صف** | `bosta_exchange_export` — **566 صف من 05-05-2026** |
| شغّالة حيًا؟ | ✅ **356 شحنة مرفوعة**، آخرها 13-09 | ✅ بتتستخدم يوميًا — بس **لسه على مسار الإكسيل** |
| الاختبارات | 8 مجموعات | 3 مجموعات |
| الـ Worker | `bosta-orders-upload-worker` | `bosta-return-exchange-exporter-worker` |

🔴 **مهم:** `upload_re` (الرفع المباشر) اتبنى في الـ Exporter واتدمج، بس
**صفر صفوف** من نوع `upload_re_*` في D1 لحد 13-09 — يعني **ما اشتغلش حي ولا
مرة**. راجع §٧ قبل ما تفترض إنه متجرَّب.

**الاتنين بيشاركوا:** نفس قاعدة D1 (`ecommoda-dev-logs`) · نفس جدول
`employees` (فمفيش هجرة دخول) · نفس `ALLOWED_ORIGINS`
(`https://ecommoda-dev.github.io`) · نفس سر `BOSTA_API_KEY`.

---

## ٣ · اللي هيتنقل — جرد بالبلوكات

### ٣.١ مكرر بالفعل في الاتنين — **يتشال من النسخة المنقولة، مايتنقلش**

```
§BOSTA::catalog … §BOSTA::resolveAddress     ~450 سطر — محرك مطابقة العنوان
§HELPERS::normPhone · wirePhone · normText
§CONSTANTS::provinces (PROVINCE_TABLE · NORTH_COAST)
§SHARED · §CORS · getCatalog · availableDistricts
tests/address-matching.test.cjs               نسخة حرفية في الاتنين
```

✅ **ده أكبر مكسب من الدمج:** النسختين دول اتكتبوا عشان يفضلوا متطابقين،
وفيه بند مفتوح عن خطر افتراقهم. الدمج بيلغي البند من أصله.

### ٣.٢ فريد في الـ Exporter — **ده اللي بيتنقل فعلًا**

```
§SHOPIFY::returnCycles          ترتيب الدورات + استبعاد CANCELED/DECLINED
§SHOPIFY::outgoingItems         القطع الخارجة (الدورة أولًا، وبعدين سطور الأوردر)
§SHOPIFY::discoveryAndDetails   استعلام مرشّحي S2
§SHOPIFY::assertCyclesConfirmable  حارس الدورة الواحدة المفتوحة
§SHOPIFY::writeAndVerifyS2      كتابة status_2_r_e + printing_time_s2 + التحقق
§RE-UPLOAD::*                   كل البلوك (returnItems · uniqueRef · buildRePayload ·
                                resolveCod · validateReOrder · createDelivery ·
                                terminateDelivery · humanizeBostaError · uploadOne ·
                                runUploadBatch)
tests/re-payload.test.cjs · tests/upload-flow.test.cjs
```

**الواجهة:** `selectJobType` (استرجاع/استبدال) · `cycleCellHtml` ·
`openCycleReviewModal` · `addressCellHtml` · `openDistrictModal` ·
`uploadCurrentOrders` · `cancelShipment`.

### ٣.٣ فريد في الـ Exporter وخاص بالإكسيل — **قرار مطلوب (§٦ بند ٢)**

```
endpoints : record_export · check_export_duplicates · confirm_upload · record_manual_confirmation
الواجهة   : buildExcelExportFile · openStatusConfirmModal · ExcelJS CDN
```

---

## ٤ · 🔴 أخطر أربع نقاط في الدمج

الأربعة دول **مايتقروش في الكود** — لازم يتقروا هنا قبل ما حد يفتح الملفين.

### ٤.١ `Math.abs` على الـ `cod` — أخطر سطر في العملية كلها

```js
// S1 — صح:  الأوردر العادي، والسالب معناه العميل دفع زيادة
const cod = Math.abs(Number(order.totalOutstandingSet?.presentmentMoney?.amount || 0));

// S2 — كارثة لو اتنسخ:  السالب معناه بوسطة بتدفع للعميل عند الباب
const cod = Math.max(raw, COD_REFUND_MIN);   // -2000 حد بوسطة (errorCode 3008)
```

بعد الدمج **الاتنين في ملف واحد**، على بعد دالة من بعض. `Math.abs` على صف
استرجاع بتحوّل **«رجّعله ٢٠٠٠» لـ«حصّل منه ٢٠٠٠»** — فلوس حقيقية في الاتجاه
الغلط، والعميل بيرفض الاستلام. من ٤ أوردرات R/E مقيسة، **٣ سالبين**.

### ٤.٢ اتجاه العنوان بيتقلب حسب النوع

| | عنوان العميل | المخزن |
|---|---|---|
| `type 10` (شحن) | `dropOffAddress` | — |
| `type 25` (استرجاع) | **`pickupAddress`** | بوسطة بتملاه من `businessLocationId` |
| `type 30` (استبدال) | `dropOffAddress` | بوسطة بتملاه |

🔴 الغلط في الاتجاه بيرجّع **HTTP 500 بلا `errorCode`** — مش 400 واضح.

### ٤.٣ `uniqueBusinessReference` — قاعدتين في Worker واحد

الفرادة عند بوسطة **على الحساب كله وعبر كل أنواع الشحنات**، و`terminate`
بيحرّرها.

```
S1  →  "12345"                       ← محجوز للشحنة العادية
S2  →  "#12345-R{n}" / "#12345-EX{n}"  ← من اسم دورة شوبيفاي
```

لو الاتنين بعتوا نفس القيمة → `400 · errorCode "11000"` والصف بيفشل.
و`businessReference` بيفضل `#12345` في **الحالتين** — كل سكانرات الستاك
بتدوّر بيه.

### ٤.٤ فلترين ومكنتين حالة — **مش استعلام واحد مدموج**

```
S1 : metafields.custom.zone:'Other_Regions'
     AND (manual_status:'Confirmed' OR manual_status:'Confirmed + Edit')
     → بتكتب: custom.courier · custom.bosta_tracking_number · تاج Bosta_Uploaded_S1

S2 : metafields.custom.status_2_r_e:'Confirmed + RETURN' (أو EXCHANGE)
     AND metafields.custom.courier:Bosta
     → بتكتب: custom.status_2_r_e · custom.printing_time_s2
     → **ومابتكتبش رقم تتبع في أي ميتافيلد** (قرار أحمد المؤجَّل — §٦ بند ٣)
```

الأداة المدموجة محتاجة **مُبدِّل وضع** (شحن / استرجاع / استبدال) زي
`selectJobType` بتاع الـ Exporter — مش فلتر واحد.

---

## ٥ · 🔴 سجل D1 — القرار الأخطر، ومحتاج إجابة قبل أي كود

`bosta_exchange_export` فيه **566 صف من 05-05-2026**، وكمان **48 صف**
`metafields_change` بـ `extra.sourceTool = 'bosta_exchange_export'` (ودي اللي
KPIs زمن الدورة بتتبني منها — **4 أدوات** بتكتب في `metafields_change`).

لو كل حاجة بقت `bosta_orders_upload`، الـ 566 صف **بيتيتّموا** تحت اسم محدش
بيكتبه تاني — وأي فلتر أو داشبورد بيقرا الاسم القديم بيرجّع ناقص.

**تلات اختيارات — أحمد بيقرر:**

| # | الاختيار | المكسب | التمن |
|---|---|---|---|
| **أ** | Worker واحد بيكتب **قيمتين `tool`** حسب الوضع | صفر هجرة · التاريخ سليم · الداشبوردات ما تتمسّش | بيكسر «أداة واحدة = قيمة واحدة» — استثناء يتوثّق في `ecommoda-constants` §7 |
| **ب** | رينيم كامل: الـ 566 صف → `bosta_orders_upload` | قيمة واحدة نضيفة | `UPDATE` على D1 · تعديل §7 · **وأي أداة تانية بتقرا الاسم القديم بتقع** — ومحتاج جرد |
| **ج** | الجديد `bosta_orders_upload`، والقديم يتساب زي ما هو | أبسط تنفيذ | تاب السجل بيبقى ناقص تاريخ، والـ KPIs بتشوف قطع |

🔴 **لو الاختيار (ب): استخدم مهارة `ecommoda-tool-rename`** — القيمة عايشة في
تلات أماكن لازم يتحركوا مع بعض، والرينيم في الكود لوحده بييتّم الصفوف في صمت.

📌 **توصيتي (ومحتاجة موافقته):** الاختيار **(أ)**. أقل مخاطرة، وبيحافظ على
الفصل اللي فعلًا بين مكنتين حالة مختلفتين — الأداة واحدة للموظف، والسجل بيفضل
بيفرّق بين عمليتين مختلفتين بطبيعتهم.

---

## ٦ · قرارات لازم تتاخد قبل البداية

| # | القرار | الحالة |
|---|---|---|
| ١ | **قيمة `tool` في D1** — أ / ب / ج من §٥ | 🔴 **حاجز** |
| ٢ | **تصدير Excel يفضل ولا يتشال؟** الـ Exporter v6.0.0 ساب الإكسيل عمدًا كخطة بديلة (عقد بوسطة يتغيّر · المفتاح مش متاح · أوردر الـ API رافضه). لو اتشال، مفيش خطة بديلة | 🔴 **حاجز** |
| ٣ | **رقم تتبع الـ S2 يتكتب في ميتافيلد؟** `custom.bosta_tracking_number` واحد وبتاع شحنة S1. قرار مؤجَّل سابقًا. لو اتقرر: `custom.bosta_tracking_number_s2` نوع `number_integer` **يتعمل في داشبورد شوبيفاي الأول** | 🟡 |
| ٤ | **الأداة القديمة تتصفّى إمتى؟** مقترح: تشتغل الجديدة أسبوع بالتوازي، وبعدين §٨ | 🟡 |
| ٥ | **اسم الأداة المعروض** بعد ما بقت بترفع كل حاجة | ⚪ |

---

## ٧ · 🔴 تحذير: مسار `upload_re` ما اشتغلش حي ولا مرة

D1 بيقول **صفر صفوف** `upload_re_return` / `upload_re_exchange` لحد 13-09،
والمخزن لسه بيصدّر إكسيل (**80 صف `export_exchange`** آخرهم النهاردة).

يعني الكود المنقول **متجرَّب باختبارات بس** (76 تأكيد على `fetch` مزيّف)،
ومتحقَّق منه على **عقد بوسطة الحي** (8 شحنات تجريبية اتعملت واتلغت 10-09) —
بس **العملية الكاملة من شاشة الموظف لحد الشحنة** ما اتعملتش.

⚠️ **الخلاصة العملية:** الدمج **مايتحسبش** تشغيل حي. أول شحنة استرجاع حقيقية
لازم تتعمل **مراقَبة** — صف واحد، وتتراجع على داشبورد بوسطة قبل التوسّع.
وسبب مباشر: `BOSTA_API_KEY` على Worker الـ Exporter ممكن يكون أصلًا مش
متظبّط — اتأكد من `?action=diag` → `bosta.keySet`.

📌 **وملحوظة تستاهل الانتباه:** `export_exchange` = 80 مقابل
`confirm_exchange` = 39. يعني **نص التصديرات مش بيتأكّد** — الشحنة بتروح
لبوسطة والحالة بتفضل `Confirmed + EXCHANGE`. ده بالظبط اللي الدمج بيقفله
هيكليًا (الرفع والحالة في نداء واحد).

---

## ٨ · تصفية الأداة القديمة — ترتيب إلزامي

🔴 **متمسحش حاجة قبل ما الجديدة تشتغل حي وتستقر.**

```
① الجديدة تشتغل بالتوازي — والقديمة زي ما هي
② تشغيل مراقَب (§٧): صف واحد، يتراجع على بوسطة
③ أسبوع تشغيل فعلي على الجديدة
④ صفحة الـ Exporter تتحول لصفحة تحويل على الجديدة — متتمسحش
   🔴 الموظفين حافظين الرابط. مسحه = صفحة ميتة بلا رسالة
⑤ الـ Worker يتساب شغّال شهر (رخيص) — أي نداء متأخر يلاقي رد
⑥ بعد الشهر: الـ Worker يتشال، والريبو يتأرشف (مش يتمسح — فيه تاريخ ٤ شهور)
```

⚠️ **ومتنساش:** `record_export` · `confirm_upload` وباقي endpoints الـ Exporter
مش هيبقى ليها مستهلك — بس **صفوف D1 بتاعتها بتفضل**، فأي فلتر في تاب السجل
لازم يفضل يعرف القيم القديمة (شوف §٥).

---

## ٩ · المهارات اللازمة للجلسة الجديدة

```
ecommoda-worker-builder     v3.3.0   ← ⑩ ترتيب الأفعال حوالين الفعل اللي مافيش رجوع منه
                                       ④ أربع حالات + التحقق لكل كيان
                                       ⑪ سلسلة السقوف التلاتة · ⑬ عقد الترتيب · ⑭ type بالأثر
bosta-api-helper            v2.0.0   ← Step 8 + Step 8b (عقد 25/30) · 8.3 (uniqueRef)
ecommoda-order-lifecycle    v1.6.0   ← Rule 8 · Rule 15 (الدورة المفتوحة) · Rule 16
ecommoda-constants          v2.5.0   ← §3 (بوسطة) · §7 (قيم tool/type) · §13 (التوقيت)
shopify-graphql-helper      v2.2.0   ← §2.1 حقول shippingAddress لأي رفع شحن
ecommoda-html-builder       v7.1.0   ← Step 3C أربع حالات · تأكيد قبل فعل لا رجعة فيه
ecommoda-tool-rename                 ← **لو** الاختيار (ب) في §٥
skills-sweep                         ← جولة جرد بعد الدمج
```

**الأداتين متحاذيتين على الإصدارات دي** (آخر جرد 13-09-2026) — فمفيش دين
مهارات متراكم قبل ما تبدأ.

---

## ١٠ · فخاخ متوثّقة — اقراها قبل ما تلمس الكود

كل واحد من دول اتكلّف مرة قبل كده. المصدر:
`Bosta-Orders-Upload/CLAUDE.md` § «فخاخ الأداة دي» و
`Bosta-Return-Exchange-Exporter/CLAUDE.md` § «فخاخ الرفع المباشر».

- 🔴 **«تم جزئيًا» (أصفر) مش فشل** — الشحنة موجودة عند بوسطة ومعاها رقم تتبع.
  إعادة الرفع = شحنة تانية بفلوس. ده ينطبق على النوعين التلاتة.
- 🔴 **النجاح = `res.ok && body.success`** — ممنوع `=== 201`.
- 🔴 **`errorCode` نص مش رقم** — المقارنة بالرقم بتخلي الرجوع على `3003` عمره
  ما يشتغل.
- 🔴 **`dropOffAddress.city` مش `cityName`** — الڤاليديتور بيتجاهل `cityName` بصمت.
- 🔴 **جدول المحافظات مقفول** — المحافظة اللي مش فيه بتوقف الصف، مفيش مطابقة نصية.
- 🔴 **`custom.bosta_tracking_number` نوعه `number_integer`** — اختلاف النوع
  بيسقّط نداء `metafieldsSet` كله بما فيه `custom.courier`.
- 🔴 **الترتيب في السجل server-side** — ومفاتيح الواجهة لازم تطابق
  `LOG_SORT_COLUMNS` في الـ Worker حرفيًا.
- 🔴 **التوقيت `Africa/Cairo` يتحسب بـ `Intl`** — ومصر بتوقف التوقيت الصيفي
  **29-10-2026**. ومعاه حارس التاريخ غير الصالح (`formatToParts` بترمي).
- 🔴 **حارس الدورة الواحدة المفتوحة يقف قدام الشحنة مش قدام الميتافيلد** —
  ويتعاد على القراءة الكاملة قبل أول نداء لبوسطة.
- ⚠️ **الرفع مستثنى من مهلة النداء** — قطع النداء مابيوقّفش الـ Worker.
- ⚠️ **`goodsInfo.amount` عليه تأمين ١٪ تلقائي** عند بوسطة.
- ⚠️ **`allowToOpenPackage: true` على كل الشحنات** — قرار أحمد 13-09-2026،
  بما فيها الاسترجاع والاستبدال. EGP 7/شحنة.
- 🟠 **صفوف «المدينة مشكوك فيها» مستثناة من «تحديد الكل»** في النوعين.

---

## ١١ · معيار «خلصت»

```
[ ] أداة واحدة بتشحن (10) وبترجّع (25) وبتستبدل (30)
[ ] الاختبارات: 8 من Orders-Upload + 2 منقولين (re-payload · upload-flow)
    و address-matching **نسخة واحدة بس**
[ ] مفيش نسخة تانية من محرك العنوان في أي مكان
[ ] قرار §٥ متنفّذ وموثّق في CLAUDE.md و ecommoda-constants §7
[ ] `?action=diag` بيفحص: شوبيفاي · D1 · بوسطة (المفتاح + الكتالوج) · أنواع الميتافيلد
[ ] تشغيل مراقَب: شحنة استرجاع واحدة + شحنة استبدال واحدة، متراجعين على بوسطة
[ ] skills-sweep نضيف
[ ] CLAUDE.md محدّث: الفخاخ من الاتنين · البصمة · المسائل المفتوحة
[ ] خطة §٨ مكتوبة في CLAUDE.md بتواريخ
```

---

## ١٢ · البرومبت المقترح للجلسة الجديدة

> عايز أدمج رفع الاسترجاع والاستبدال من `Bosta-Return-Exchange-Exporter`
> جوّه `Bosta-Orders-Upload`، وبعدين أصفّي الأداة التانية.
> **اقرا `MERGE-BRIEF.md` في ريبو `Bosta-Orders-Upload` الأول** — فيه الجرد
> الكامل والقرارات المطلوبة والفخاخ.
> قراري في §٥ هو: **[أ / ب / ج]**
> وقراري في §٦ بند ٢ (تصدير Excel): **[يفضل / يتشال]**

---

آخر تحديث: 13-09-2026

</div>
